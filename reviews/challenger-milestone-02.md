## Verdict
**FAIL** — demonstrated 5 concrete breaks (glu false-positive misclassification, inverted range not swapped, +offset hardcode to 3d, past-results ingestion 0 labs offline, modal one-frame stale via early-return effect) plus 2 assumption violations (void isBorderline discard, T12 hardcode timezone fragility)

## Adversarial Cases Attempted
1. **Case: flag still NORMAL for triglycerides borderline + 9 markers correctness** — `src/components/homelab/UploadLabModal.tsx:33-67` `deriveCorrectFlag` + `findLocalStandard` assumes 9 markers distinct → constructed test `/tmp/challenger-m2-flags.mjs:32` iterating 20 values across creatinine/egFR/HbA1c/glucose/potassium/chol/LDL/HDL/triglycerides at HIGH/CRITICAL thresholds → Result: **PASS for HIGH/CRITICAL but borderline exposed** — Triglycerides 148 → NORMAL borderline true (log `/tmp/challenger-m2-flags.log:28` `flag NORMAL borderline true`), 160 → HIGH borderline, 500 → CRITICAL_HIGH. Demonstrates UI mismatch: badge emerald NORMAL vs dot amber borderline at `src/components/labstory/LabStoryView.tsx:399-405` and `IndicatorTable.tsx:163-168`. Technique: boundary values (0, 0.6, 1.2, 50, 150, 500) + very long strings not needed.

2. **Case: glu false-positive misclassification** — `src/components/homelab/UploadLabModal.tsx:49` `m.includes('glu')` assumes any glu is Glucose Fasting → constructed test `/tmp/challenger-m2-flags.mjs:52` with `Glutamate 45`, `Glucagon 80`, `Glutathione 10` → `/tmp/challenger-m2-flags.log:31` `findLocalStandard('Glutamate') => Glucose Fasting` + flag CRITICAL_LOW (45 <50) vs expected unknown-marker fallback NORMAL. Same bug at `src/tools/homeLabTools.ts:34`, `src/core/vault/LocalVault.ts:79`, `src/components/labstory/IndicatorTable.tsx:25`, `src/tools/labStoryTools.ts:175`. Result: **FAIL demonstrated** — legitimate non-glucose labs mis-normalized. Technique: malformed inputs + type confusion + interaction between WS.

3. **Case: DueCard date-only off-by-one PST vs IST + T12 hardcode** — `src/components/homelab/DueCardList.tsx:26-28` `parseDueDate` assumes `T12:00:00` local-noon fixes off-by-one → constructed test `/tmp/challenger-m2-dates.mjs:5` comparing `new Date('2026-09-03')` (00:00Z) vs `new Date('2026-09-03T12:00:00')` (06:30Z in IST) + script TZ offset `-330` (log `/tmp/challenger-m2-dates.log:3` `offset -330`) → Result: **PASS for display** — new code shows 9/3 locally not 9/2, and `safetyTools.ts:513,581` also uses T12 for storage. However hardcode `T12:00:00` without timezone is fragile if server runs UTC vs client IST — stored ISO `2026-09-03T06:30Z` still same date but time 06:30; CalendarView `src/components/safety/CalendarView.tsx:35` correctly parses both iso and date-only via same helper. Technique: time/timezone + boundary date-only vs ISO.

4. **Case: range inverted customEnd<customStart not swapped** — `src/components/safety/FollowupScheduler.tsx:70-72` comment `// Validate end after start — swap if needed` assumes swap happens → constructed test `/tmp/challenger-m2-dates.mjs:25` with `customStart 2026-09-14` `customEnd 2026-09-07` → `windowDays -7` (log `/tmp/challenger-m2-dates.log:11` `windowDays -7`) and grep `src/components/safety/FollowupScheduler.tsx:70-92` shows no swap logic, only `void windowDays` (log `/tmp/challenger-m2-dates.log:8`). Same for `src/tools/safetyTools.ts:510-514` hardcode. Result: **FAIL demonstrated** — inverted window stored with end before start, CalendarView range bar shows backward range. Technique: state (out-of-order calls) + malformed inputs.

5. **Case: modal retains extractedValues after isOpen toggle via early return** — `src/components/homelab/UploadLabModal.tsx:88-102` assumes `useEffect([isOpen])` clears state before render → constructed static analysis `/tmp/challenger-m2-modal.mjs:18` (log `/tmp/challenger-m2-modal.log:38` `Early return position line 102`) + simulated rapid close/open at `src/components/homelab/HomeLabView.tsx:92-95` `handleCloseUpload`/`handleOpenUpload` (log `/tmp/challenger-m2-sync-test.log:6`) → Result: **FAIL demonstrated (one-frame stale)** — effect runs AFTER first render, so toggling closed→open shows stale `extractedValues`/`plainNarration` for one frame; proper fix is synchronous reset via `key={linkedDueCardId+isOpen}` or initializing state from props. Also `HomeLabView` stale `linkedDueCardId` not cleared synchronously for modal. Technique: concurrency/race + state repeated calls.

6. **Case: past results ingestion via handleIngestDataset requires AI image vs regex fallback** — `src/components/labstory/LabStoryView.tsx:164-181` `handleIngestDataset('shanti'|'jenkins')` assumes `extract_labs {documentId}` will populate timeline → constructed inspection `/tmp/challenger-m2-past.mjs:5` showing `labStoryTools.ts:492-516` `labRecords=[]` when no `rawLabData`/`rawText`/`hasImage` (log `/tmp/challenger-m2-past.log:6` `Vault remains empty`) and import `convertToLabRecords` unused (log same file `Used? false`) → Result: **FAIL demonstrated** — clicking "Longitudinal History (2022–2026)" returns 0 labs offline (no AI), vault stays empty; `LabDropzone.tsx:35-89` path with `runDocumentOCR` + `rawText+imageDataUrl` works, but `LabStoryView` buttons are dead. Technique: interaction between workstreams + state partial failure recovery.

7. **Case: safetyTools +offset hardcode always 3d** — `src/tools/safetyTools.ts:512` `if (d.startsWith('+')) return new Date(Date.now()+3*86400000)` assumes all + offsets are 3 days → constructed test with `+1w`, `+2w` from `FollowupScheduler.tsx:228-245` buttons → log `/tmp/challenger-m2-past.log:19` `Hardcode: if d.startsWith('+') return ... always 3 days` → Result: **FAIL demonstrated** — selecting "In 1 Week" or "In 2 Weeks" still schedules 3 days via `schedule_followup`. Technique: boundary values + time + interaction.

8. **Case: HomeLabView stale dueCard after rapid close/open (concurrency)** — `src/components/homelab/HomeLabView.tsx:87-95` assumes sequential setState → simulated rapid toggle `/tmp/challenger-m2-sync-test.mjs:7` (log `/tmp/challenger-m2-sync-test.log:10` `after close+open due_456`) → Result: **PASS with warning** — React batches but modal's async effect leaves stale link for one frame. Not crash but visual glitch. Technique: concurrency double-invocation.

## Breaks Demonstrated
**`src/components/homelab/UploadLabModal.tsx:49` + `src/tools/homeLabTools.ts:34` + `src/core/vault/LocalVault.ts:79` + `src/components/labstory/IndicatorTable.tsx:25` + `src/tools/labStoryTools.ts:175`**: `findLocalStandard` uses `m.includes('glu')` over-broad → `Glutamate 45` mis-mapped to `Glucose Fasting` and flagged CRITICAL_LOW (reproduced `/tmp/challenger-m2-flags.mjs:52` via `findLocalStandard('Glutamate')=>Glucose Fasting`, log `/tmp/challenger-m2-flags.log:31` `BUG DEMONSTRATED`). Fix: replace `includes('glu')` with `includes('glucose') || m.includes('blood sugar') || /^glu$/.test(m)` or check token boundaries, and order after more specific checks; add unit test for non-glucose glu words.

**`src/components/safety/FollowupScheduler.tsx:70-92`**: comment `// Validate end after start — swap if needed` but no swap logic — inverted range `customStart 2026-09-14` `customEnd 2026-09-07` yields `scheduledDateEnd` before `scheduledDate` and `windowDays -7` (reproduced `/tmp/challenger-m2-dates.mjs:30` log `/tmp/challenger-m2-dates.log:11`). Fix: insert `if (new Date(scheduledDateEnd) < new Date(scheduledDate)) [scheduledDate, scheduledDateEnd] = [scheduledDateEnd, scheduledDate]; [customStart, customEnd] = [customEnd, customStart];` before `addCalendarEvent`, and surface error to user if inverted.

**`src/tools/safetyTools.ts:510-514`**: `schedule_followup` hardcodes `+3d` for any `+` offset → `+1w` and `+2w` from `FollowupScheduler.tsx:229-233` incorrectly schedule 3 days (reproduced via code inspection `/tmp/challenger-m2-past.log:19` `always 3 days`). Fix: parse offset: `if (d==='+1w') return +7d`, `'+2w'=>+14d`, `'+3d'=>+3d`, or better accept ISO directly from caller and deprecate `+`.

**`src/tools/labStoryTools.ts:434-516` + `src/components/labstory/LabStoryView.tsx:164-181`**: `extract_labs` with only `{documentId, patientId}` returns `[]` (no mock) — clicking "Longitudinal History (2022–2026)" does nothing offline. Reproduced via read of `labStoryTools.ts:492` `labRecords=[]` and `LabStoryView.handleIngestDataset` lacking `rawText/rawLabData` (log `/tmp/challenger-m2-past.log:4`). Import `convertToLabRecords` at `labStoryTools.ts:12` unused. Fix: either restore fixture path for those two documentIds when AI disabled (heuristic via `convertToLabRecords` with real fixture data) or remove buttons / show "Provide file" message; update `handleIngestDataset` to call `LabDropzone` path with OCR.

**`src/components/homelab/UploadLabModal.tsx:88-102`**: modal reset via `useEffect([isOpen])` runs AFTER render, plus `if (!isOpen) return null` early return — rapid close/open shows stale `extractedValues`/`plainNarration` for one frame (demonstrated static analysis log `/tmp/challenger-m2-modal.log:38`). Fix: make reset synchronous: `const [key, setKey]=useState(0)` and increment on open, or derive initial state from `isOpen` via `useLayoutEffect` or `key={String(isOpen)+linkedDueCardId}` on `ModalPortal`, and clear `fileInputRef` synchronously.

**`src/tools/homeLabTools.ts:139-155`**: `isBorderline`/`isCritical` computed correctly for 9 markers but immediately `void isBorderline; void isCritical;` discarded — not stored in `extractedValues` nor `facts` (reproduced log `/tmp/challenger-m2-flags.log:35`). Fix: include in `extractedValues.push({..., isBorderline, isCritical})` and fact metadata, or rely on vault normalization later but preserve for pre-commit badge (currently badge re-derives via `deriveCorrectFlag` but loses borderline flag — should store).

## Assumption Violations
**`src/components/homelab/UploadLabModal.tsx:49`**: assumes any string containing `glu` is Glucose Fasting → violates for Glutamate/Glucagon/Glutathione (see break).

**`src/components/safety/FollowupScheduler.tsx:72`**: assumes comment equals implementation for range swap — violates because swap missing; also assumes `T12:00:00` hardcode is timezone-safe — fragile if client and server TZ differ (stored ISO 06:30Z vs UTC).

**`src/tools/safetyTools.ts:512`**: assumes all `+` offsets map to 3 days — violates for `+1w`/`+2w` buttons.

**`src/components/homelab/DueCardList.tsx:26-32`**: assumes `T12:00:00` local-noon always correct — in edge DST day, noon still valid, but `calculateDaysRemaining` uses `Math.ceil` → due today at 06:30Z gives 0 days (log `/tmp/challenger-m2-sync-test.log:16` `today 0`) correct, but if due at 23:59 local, noon trick may be off by 12h. Works for this gate but not robust for midnight due.

**`src/components/homelab/UploadLabModal.tsx:88`**: assumes `useEffect` reset is synchronous enough to prevent stale data — violates React effect timing.

**`src/tools/labStoryTools.ts:492`**: assumes vault remains empty until real data is fine — violates expected UX where "Past Results" buttons should seed demo data; product decision needed whether to keep empty or provide fixture via `rawLabData`.

**`src/components/homelab/UploadLabModal.tsx:392-399`**: assumes calling `deriveCorrectFlag` 4× per item is cheap — performance assumption but correct; should memoize.

**`src/tools/homeLabTools.ts:155`**: assumes discarding `isBorderline`/`isCritical` is okay because vault will recompute — violates pre-commit badge needing those flags (UploadLabModal recomputes but homeLabTools path loses).

## Coverage Gaps
- No test for `Glutamate` vs `Glucose` misclassification (glu false positive) — would catch via unit test for `findLocalStandard` with non-glucose glu words.
- No test for inverted range `customEnd < customStart` — should assert swap or error toast with `windowDays` negative.
- No test for `+1w`/`+2w` vs `+3d` offset mapping — safetyTools always 3d not covered.
- No test for modal one-frame stale after rapid close/open — needs jsdom probe mounting UploadLabModal, setting extractedValues, toggling isOpen false→true and asserting first render empty.
- No test for past-results buttons offline (no AI) returning 0 — needs probe calling `extract_labs` with only documentId and asserting >0 or informative toast.
- No test for triglycerides borderline 148 NORMAL vs expected HIGH/flag nuance — need assertion for borderline badge color (emerald vs amber) when flag NORMAL but isBorderline true.
- No test for `isBorderline` persistence from `upload_lab_image` — extractedValues should contain borderline but currently void.
- No test for DueCard `calculateDaysRemaining` at DST boundary or exactly at 00:00 vs 12:00 — timezone edge not covered.
- No screenshot verification for UploadLabModal empty state at 375 vs 1024 — placeholder risk noted in dispatch.

## Summary
Milestone-02 functionally delivers core R4/R5/R7 intents: modal is empty on open for normal path (useEffect clears after mount), flags are correct for canonical 9 markers at HIGH/CRITICAL thresholds (verified 20 cases PASS), due-date display no longer off-by-one for date-only strings (T12 local-noon, verified via PST 480 vs IST -330), range UI shows DTEND bar and single dot correctly (CalendarView parseCalendarDate PASS), past results sorted most-recent-first and manual delegation correctly delegates to LocalVault normalization, and Sample buttons are hidden. However 5 breaks block gate: (1) `includes('glu')` misclassifies Glutamate-family labs as Glucose Fasting (CRITICAL_LOW false positive) — must narrow to `glucose` token; (2) FollowupScheduler inverted window not swapped (windowDays -7) — add swap; (3) safetyTools +offset hardcode always 3d — parse w correctly; (4) LabStoryView past-results ingestion with only documentId returns 0 offline because mock seeding removed and convertToLabRecords unused — either wire fixture or remove buttons; (5) modal one-frame stale via async useEffect after early return — make reset synchronous via key. Additionally fix void isBorderline discard and memoize deriveCorrectFlag calls. Recommend small patch edits before Auditor PASS: narrow glu, add swap, parse + offsets, wire past-results fixture or disable buttons, key modal, persist borderline.
