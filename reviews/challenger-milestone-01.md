## Verdict
**FAIL** — demonstrated crashes + deduplication false-positives block gate (IndicatorTable plainExplanation + details JSX crash on unknown marker with undefined referenceRange, over-broad `includes('glu')` collapsing Glucose vs Glucose Random vs Glucosamine into one row, FactStreamView header count vs displayed rows mismatch, dead WebMCPInspector state, ProfileIndicator version-churn). Header Activity gating, PDF hidden-by-default, and mobile overflow survive.

## Adversarial Cases Attempted
1. **Case: header Activity still reachable via direct DOM query header region** — `src/App.tsx:376-470` assumes Activity moved inside Connect — constructed `/tmp/challenger-02-header-pdf.js:4` (slice header region, grep `aria-label.*Activity` + `setIsInspectorOpen(true)`) → Result: **PASS** — `hasActivityInHeader false`, `Terminal icon false`, `hasConnectInHeader true` (2 buttons mobile+desktop single logical), `countConnectButtons 2` — no header Activity remains. Evidence `/tmp/challenger-02-header-pdf.js:0` log.

2. **Case: pendingCount tamper and badge caps** — `src/App.tsx:107-117` + `src/components/common/ConnectWebMCPModal.tsx:50-95` assumes `pendingCount` derived vault `pendingFacts+pendingProps+engine pendingApprovals` and capped `99+` — constructed `/tmp/challenger-05-deep.js:6` (grep badge `>99 ? '99+'`, prop injection via `ProfileIndicator` fallback) → Result: **PASS** — `App pendingCount sum present`, `Connect header caps true`, `ProfileIndicator pendingDisplay propPendingCount !== undefined ? propPendingCount : vaultStats.pending` at `ProfileIndicator.tsx:162` — tamper via prop still capped, completeness unaffected (`docs/meds/labs` only). No fabrication possible.

3. **Case: PDF auto-show via eventBus without tap** — `src/components/vault/MyRecordsView.tsx:18-75` assumes preview hidden by default `isPreviewOpen=false` and shown only on explicit gallery tap or `eventBus.onHighlightDocument` — constructed `/tmp/challenger-02-header-pdf.js:28` + `/tmp/challenger-03-check-preview-bug.js:4` (grep `useState(false)`, `{isPreviewOpen &&`, `handleDocumentAdded` snippet, gallery `onClick` sets preview, highlight listener) → Result: **PASS** — `isPreviewOpen false` true, `previewConditional true`, `galleryTapSetsPreview true`, `handleDocumentAdded sets preview false` (correctly does `setSelectedDocId` only, no `setIsPreviewOpen`, comment `R2: do NOT auto-show`), `highlightSetsPreview true` (Source tap path). BoundingBoxViewer `onClose` + Escape at `BoundingBoxViewer.tsx:72-79,158-166` closes correctly. No auto-show on mount/upload.

4. **Case: indicator dedup collapse Glucose vs Glucose Fasting incorrectly** — `src/components/labstory/IndicatorTable.tsx:20-31` + `src/core/vault/LocalVault.ts:74-85` assumes `m.includes('glucose')||m.includes('glu')` safely canonicalizes to `Glucose Fasting` — constructed `/tmp/challenger-01-indicator-dedup.js:28` (replicate `findLocalStandard`, group 3 glucose variants, Glucosamine, cholesterol/lipids, K, empty, very long) → Result: **FAIL** — `Case1 3 glucose variants collapsed to 1 group` log `FAIL: Glucose Random incorrectly merged`, `Case2 Glucosamine canonical Glucose Fasting — false positive` log `FAIL: Glucosamine false positive glu`, `Case5 Creatine (no n) => Creatinine` also false positive. Demonstrates over-broad `includes('glu')` and `includes('creat')` at `IndicatorTable.tsx:22,25`. Parity `LOCAL_BIOMARKER_STANDARDS` 9 vs 10 count matches but heuristic divergence remains.

5. **Case: indicator details crash on unknown marker undefined referenceRange** — `src/components/labstory/IndicatorTable.tsx:103-116` `plainExplanationFor` + `265` details JSX assume `referenceRange`/`optimalRange` always defined via vault fallback — constructed `/tmp/challenger-06-crash-test.js:9` (simulate `plainExplanationFor` with `referenceRange: undefined`) → Result: **FAIL (demonstrated crash)** — `CRASH: Cannot read properties of undefined (reading 'low')` reproduced `/tmp/challenger-06-crash-test.js:18`, and details JSX `activeGroup.referenceRange.low` without `?.` at `IndicatorTable.tsx:268`同样 crashes. Vault fallback `LocalVault.ts:89-102` mitigates for `addLab` path but legacy persisted labs or direct `labs.set` bypass still crash overview→details.

6. **Case: details keyboard not accessible** — `src/components/labstory/IndicatorTable.tsx:176-184` assumes rows `role="button" tabIndex0 focus-visible:ring-primary` + `onKeyDown Enter/Space` — constructed `/tmp/challenger-04-factstream-accessibility.js:28` (grep role, tabIndex, onKeyDown, 44px, aria-label) → Result: **PASS** — `role button true`, `tabIndex true`, `Enter/space true` with `preventDefault`, `focus-visible true`, `height 44px true`, `aria-label tap for details true`. LabStoryView past results also `role button` true at `LabStoryView.tsx:420-421`. Modal close `X` + `Escape` at `IndicatorTable.tsx:235-242` accessible, `Min-h 44px` preserved. No keyboard trap blocked.

7. **Case: FactStreamView still shows duplicate labs** — `src/components/vault/FactStreamView.tsx:58-69` assumes `isLabCategory === 'lab'` delegated to IndicatorTable, no scattered duplicates — constructed `/tmp/challenger-04-factstream-accessibility.js:5` (grep `isLabCategory`, `pendingLabsCount`, `filteredApprovedFacts` excludes lab, category variants `'labs'`/`'Lab Result'` ) → Result: **PASS** for exact `'lab'` but **WARN** for mismatch — header `pendingFacts.length` 5 hits still counts labs while table maps `pendingFactsDisplay` (excluded) → `pendingFacts.length 3 to review` vs 1 row + banner `2 labs now in Indicators` mismatch at `FactStreamView.tsx:146,201-206,222`. Category `'labs'` plural or `'lab_result'` would bypass filter (`labs false`) and show duplicate. `vaultTools` category exact `'lab'` currently safe but narrow.

8. **Case: mobile 375 overflow + ProfileIndicator completeness** — `src/components/labstory/IndicatorTable.tsx:148` `min-w-[680px]` inside `overflow-x-auto` + `src/App.tsx:510` `overflow-x-hidden`, `ProfileIndicator.tsx:169-188` responsive `truncate hidden sm:inline` — constructed `/tmp/challenger-04-factstream-accessibility.js:35` (grep min-w, overflow, main hidden, ProfileIndicator truncate) → Result: **PASS** — `min-w 680 true`, `overflow-x-auto true`, `main overflow-x-hidden true`, `ProfileIndicator responsive truncation true`. No horizontal blowout at 375; tools grid `max-h-[320px] overflow-y-auto` at `ConnectWebMCPModal.tsx:236` handles 40 tools.

## Breaks Demonstrated
**`src/components/labstory/IndicatorTable.tsx:25`** — `if (m.includes('glucose') || m.includes('glu'))` over-broad dedup collapses distinct biomarkers: `Glucose`, `Glucose Fasting`, `Glucose Random` all map to single `Glucose Fasting` key (reproduced `/tmp/challenger-01-indicator-dedup.js:12` `1 groups` vs expected 2-3), and `Glucosamine` false-positive to `Glucose Fasting` (log `Glucosamine canonical: Glucose Fasting`). Similarly `m.includes('creat')` maps `Creatine` → `Creatinine` at `:22`. Severity **medium** — correctness violation of "one row per unique marker" guarantee; `cholesterol`/`ldl`/`hdl` order correct but `glu` swallows any `glu*` string.

**`src/components/labstory/IndicatorTable.tsx:103-116,268`** — `plainExplanationFor` accesses `ref.low`/`ref.high`/`opt.low`/`opt.high` without null guard; details JSX `activeGroup.referenceRange.low` without `?.` at `:268` crashes when `latest.referenceRange` undefined (unknown marker not via `normalizeLabRecord` fallback). Reproduced `/tmp/challenger-06-crash-test.js:18` `CRASH: Cannot read properties of undefined (reading 'low')`. Severity **high** — unmounted details modal throw breaks drill-down for legacy labs; vault fallback at `LocalVault.ts:92-94` prevents crash for newly `addLab`-ed labs but not for directly persisted `labs.set` or pre-fallback data.

**`src/components/vault/FactStreamView.tsx:146,222`** — header badge `pendingFacts.length` counts labs while table iterates `pendingFactsDisplay` (labs filtered) at `:222` `pendingFactsDisplay.map`, leading to header `N to review` vs displayed rows `N - pendingLabsCount` mismatch. Reproduced `/tmp/challenger-04-factstream-accessibility.js:22` `pendingFacts.length 5 hits` vs `pendingFactsDisplay.map 1`. Severity **low-medium** — UX inconsistency: user sees "3 to review" but only 1 actionable row + banner; not duplicate but confusion.

**`src/components/carecircle/ProfileIndicator.tsx:112-138`** — `useEffect` deps `[activeProfile.userId, version]` where `bump` does `setVersion(v+1)` causes effect churn: every `fact_added` increments version → cleanup + re-subscribe 9 listeners. Reproduced `/tmp/challenger-07-vault-normalization.js:20` `deps includes version true`. With 10k labs rapid `lab_added` events, re-subscription overhead + double `refreshStats` call degrades header. Severity **low** — no leak (cleanup `u1()..u9()` correct) but performance anti-pattern.

**`src/App.tsx:68-72,746`** — dead `WebMCPInspector` state: `isInspectorOpen` defined at `:68` but header no longer calls `setIsInspectorOpen(true)` (count `2 occurrences` = definition + never-called), yet component still imported and rendered at `:746` `<WebMCPInspector isOpen={isInspectorOpen} />`. Reproduced `/tmp/challenger-04-factstream-accessibility.js:43` `setIsInspectorOpen never called ... dead`. Severity **low** — not block but violates single Connect entry point claim (header Activity header0 but file Activity 2 workaround via dead state).

## Assumption Violations
**`src/components/labstory/IndicatorTable.tsx:47-52`** — assumes heuristic `findLocalStandard` mirrors `LocalVault` exactly and `includes` is safe. Violated by `glu` swallowing `Glucosamine`/`Gluten` and `creat` swallowing `Creatine`.

**`src/components/vault/FactStreamView.tsx:59`** — assumes lab facts category always exact `'lab'` lowercased trimmed. Violated if tool ever emits `'labs'`, `'lab_result'`, `'Laboratory'` — those bypass `=== 'lab'` and would duplicate in saved table.

**`src/components/common/ConnectWebMCPModal.tsx:50-95`** — assumes `localStorage.getItem('carecanvas_active_user')` always valid JSON and `pid` non-empty before merging `vaultFacts` into `pendingApprovals`. Violated when `pid=''`, `JSON.parse` throws, or vaultFacts contain duplicate `id` already in `pending`; merging uses `Set` but `pendingCount` sum `pf+pp` may diverge from `pendingApprovals.length`.

**`src/components/carecircle/ProfileIndicator.tsx:144-159`** — assumes `activeProfile.name` is non-whitespace and `email.includes('@')` sufficient; completeness `5%` floor for empty vault hides pendingCount tamper but `initials` fallback `charAt(0)` on whitespace `'   '` still returns `' '` not `'P'` (no `trim` before charAt, though later `trim()` on name check at 145 not initials at 163).

**`src/core/vault/LocalVault.ts:88-105`** — assumes all labs enter via `addLab` normalization fallback. Violated for labs inserted via direct `labs.set` (e.g., hydration, import) lacking `referenceRange` → downstream IndicatorTable crash.

## Coverage Gaps
- No test for `Glucose` vs `Glucose Fasting` vs `Glucose Random` distinctness after dedup — worker test uses only `Creatinine`/`eGFR` samples, not `glu` variants.
- No test for unknown marker lab drill-down crash (history + chart + plainExplanation) with `referenceRange undefined` — existing tests only use known 9 standards.
- No test for `FactStreamView` header `pendingFacts.length` vs `pendingFactsDisplay.length` mismatch when pendingLabsCount>0.
- No test for `ConnectWebMCPModal` Activity tab `pendingCount` live update after `fact_confirmed` + `proposal_submitted` while modal open (poll not interval, only eventBus).
- No test for `ProfileIndicator` version-churn at 10k rapid `lab_added` events — no performance probe.
- No test for `isLabCategory` plural `'labs'` or `'lab_result'` category leak — category mutation not fuzzed.
- No test for `BoundingBoxViewer` `truncate max-w-[60%]` long `documentTitle` at 320 after MyRecordsView `isPreviewOpen` gate — placeholder screenshots not real.

## Summary
Overall robustness **FAIL** for gate but not catastrophic: header gating (`Activity` removal → single `Connect` with Activity nested `activeTab connect|activity`), PDF hidden-by-default (`isPreviewOpen &&` + gallery tap `setIsPreviewOpen(true)` while `handleDocumentAdded` correctly stays hidden), keyboard accessibility (`role button 44px focus-visible`), and mobile overflow (`overflow-x-auto` + `overflow-x-hidden`) all survive adversarial probes. Two concrete breaks require fix before Auditor: (1) narrow `findLocalStandard` heuristic at `IndicatorTable.tsx:25` from `includes('glu')` to regex `/\bglucose\b/` or exact `glucose`/`glucose fasting` map to avoid `Glucosamine`/`Glucose Random` false collapse — mirror narrow change in `LocalVault.ts:74`; (2) guard `plainExplanationFor` and details JSX at `IndicatorTable.tsx:103-116,268` with `ref?.low ?? 0` fallback and `opt?.low ?? 0` to prevent crash on legacy unknown marker labs, or enforce vault fallback for all `labs.set` paths. Remaining `FactStreamView` count mismatch, dead `isInspectorOpen` import, and `ProfileIndicator` version churn are low-medium but should be patched (`pendingFactsDisplay.length` header, remove dead inspector import, dep `[activeProfile.userId]` only). Fixing the two medium/high crashes makes milestone PASS.
