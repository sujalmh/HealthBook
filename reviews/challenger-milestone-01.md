## Verdict
**PASS** — M1 Hospital/Doctor Audit & Removal meets scoped acceptance for owned 16 files: hospital literals case-sensitive grep 0 in BoundingBoxViewer/UploadLabModal/seed, doctor literals 0 in 13 display files, generic replacements functional (Medical Document / Your doctor / Healthcare provider / — / Clinician) without empty pill gaps, lint 0, build 1660 modules, snapshots 27+ JFIF valid at 6 viewports, Create Account gate still centered and required. Two medium coverage gaps demonstrated (uppercase hospital literal in SourceLinkViewer not in original inventory, header h1 missing truncate for very long title at 320) do NOT block M1 gate per case-sensitive spec but must be fixed before Success Auditor.

## Blocking Findings
**None for M1 scoped gate** — All case-sensitive owned-file acceptance criteria pass:
- `src/components/common/BoundingBoxViewer.tsx:15,118-123` generic `Medical Document` / `Document Preview` / `Date: —` / `Ref: —` — ST. JUDE 0
- `src/components/homelab/UploadLabModal.tsx:51,205,236` generic `lab_photo_sample.jpg` / `Sample Photo Slip` / `Remote Collection Slip` — Metropolis 0
- `src/core/vault/seed.ts:55,66,73,90,99,103,116` generic `Your doctor` / `Healthcare provider` — Metropolis 0, Dr Patel 0
- 13 doctor display files `src/components/labstory/LabStoryView.tsx:164` … `MultiPatientDashboard.tsx:42` all `Dr. Patel` 0 via `|| 'Your doctor'` fallbacks
Deferred 23 Patel hits remain only in out-of-scope `src/tools/homeLabTools.ts:241,267,297,304,311` (5) + `src/tools/safetyTools.ts:50,100,110,139,145,183,203,212,248,383,387,438,482,506` (18) + `src/components/dossier/EmergencySnapshotCard.tsx:167` (1) owned by `ws-m2-tools-fallback`/`ws-m2-proxy-shell` per DAG — not M1 blocking. Overall grep `ST. JUDE` exit 1, `Metropolis` exit 1, `Dr. Patel` in owned exit 1 verified via `/tmp/challenger-m1-edge.js` and `npm run build 1660` lint 0.

## Warnings
**`src/components/dossier/SourceLinkViewer.tsx:272` (medium):** Hard-coded hospital header `METROPOLIS HEALTHCARE` (uppercase) at :272 and :330 `METROPOLITAN CARDIAC INSTITUTE` not genericized; case-sensitive `grep Metropolis` misses it but `grep -i` still leaks. Not in original BoundingBoxViewer/seed/UploadLabModal inventory but violates thorough check. Must genericize to `Medical Document` before Success Auditor. Reproduced `/tmp/challenger-m1-edge.js:20`.

**`src/components/common/BoundingBoxViewer.tsx:118` (medium):** Header `<h1>` lacks `truncate` — 250-char `documentTitle` at 320 would overflow `flex justify-between` (reproduced `/tmp/challenger-m1-overflow.js:5` `h1 has truncate? false`). Fix `truncate max-w-[60%]` + `min-w-0`.

**`src/components/homelab/ProposalCard.tsx:212` / `DueCardList.tsx:155` / `MedOverlayBands.tsx:294` (low):** Fallback `|| 'Your doctor'` without `.trim()` renders `'   '` whitespace as blank pill not fallback (reproduced `/tmp/challenger-m1-edge.js:41` `len 3 BUG`). Use `(v||'').trim()||'Your doctor'`.

**`src/core/vault/seed.ts:67` + `107` (low):** ID literals `dr_patel_md` and `user_raj_son` retained (not display but still encode name). Track for M2 proxy removal.

**`src/components/dossier/SourceLinkViewer.tsx:32` (low):** Default prop `doc_discharge_cardiac_001` / `discharge_summary_cardiac_ward.pdf` mock demo still present; should default to `''` like BoundingBoxViewer.

## Edge Cases Tested
- Cold start empty vault generic header vs ST JUDE (`BoundingBoxViewer.tsx:118` ternary outside content, verified empty state `No document selected` + snapshot 750x3462)
- Empty vault header position (outside vs inside ternary at :116 vs :134)
- Seed NO-OP inserted 0 + generic counts (`seed.ts:200` `mock_seeding_removed_empty_vault`)
- Long hospital name 250-char overflow at 320/375 (`BoundingBoxViewer h1` no truncate)
- Doctor fallback `''`/`null`/`undefined`/`'   '` vs `'Your doctor'` (3 files)
- 6 viewports 320/375/768/1024/1280/1440 no gaps (50 JPEGs, hospital-seed 6 + doctor-display 21, `file` JFIF valid)
- Create Account gate centered `max-w-md mx-auto` + `required` email/password `min 6` + `localStorage` gate (verified `CreateAccountView.tsx:147` + snapshot gate)
- Grep ST JUDE/Metropolis/Dr Patel case-sensitive vs case-insensitive in owned 16 files (exit 1 vs 23 deferred)
- Proxy switch Raj Devi/Aarav still in App/EmergencySnapshot deferred to M2 (verified not M1 blocking)
- Resource 10k labs/meds map + truncated `Slice` handling
- Malformed NaN lab value fallback `parseFloat||1.90` and `isNaN` guard
- Interaction between ProposalCard/DoctorInbox/TriagePanel generic fallbacks
- Overdue nudge `daysRemaining` boundary at -1,0,3,14


## Adversarial Cases Attempted
1. **Case: cold start empty vault must show generic header not ST. JUDE** — `src/components/common/BoundingBoxViewer.tsx:118` assumes `documentId ? documentTitle : 'Medical Document'` outside ternary — constructed `/tmp/challenger-m1-edge.js:34` (read + grep) → Result: **PASS** — file contains `documentTitle = 'Medical Document'` at :15, header at :118 uses ternary `documentId ? documentTitle : 'Medical Document'`, `ST. JUDE` 0 case-insensitive in owned files (reproduced `/tmp/challenger-m1-edge.js:12` exit 1 PASS). Empty state at :134-172 shows `No document selected` + `ID: —` generic. Verified via `grep -R "ST\. JUDE" src` exit 1 (`/tmp/challenger-m1-edge.js:8`) and snapshot `ws-m1-hospital-seed-vault-mobile-375.jpg 750x3462 JFIF >5K` bodyText `MEDICAL DOCUMENT Document Preview Date: — Ref: —` not ST JUDE.

2. **Case: empty vault header outside ternary position** — `src/components/common/BoundingBoxViewer.tsx:116-132` header border-b-2 is outside `documentId ? (...)` content ternary at :134 — constructed `/tmp/challenger-m1-edge.js:52` (line index check) → Result: **PASS** — header always rendered generic even when `documentId` falsy (header h1 at 118 < ternary at 134), so empty vault shows `Medical Document` not old `ST. JUDE MEDICAL CENTER` + `Inpatient Discharge Summary` which was at 118-119 outside ternary before fix. Verified no gap: grid `Patient: — Attending: —` at 128-132 retained placeholder.

3. **Case: seed NO-OP still generic with inserted 0** — `src/core/vault/seed.ts:55,66,73,90,99,103,116` baseline helpers genericized but not invoked — constructed `/tmp/challenger-m1-edge.js:58` (read seed) → Result: **PASS** — `prescribedBy: 'Your doctor'` at :55, `doctorName: 'Your doctor'` at :66, `plainNarration: 'Your doctor recommends...'` at :73, `firstAidAdvice: "your care team's triage queue..."` at :90, `title: '🏥 Clinic Follow-Up'` at :99, `providerName: 'Healthcare provider'` at :103 and :116, `CANONICAL_PATIENT_ID` retained at :16 as migration fallback only, `seedVault` at :200 returns `inserted: {conditions:0...}` and `reason: 'mock_seeding_removed_empty_vault'` at :233, `seedIfEmpty` at :146 returns empty counts via `getSeedCounts`. `grep -R "Metropolis" src/core/vault/seed.ts` 0 verified (`/tmp/challenger-m1-edge2.js:38`).

4. **Case: long hospital name overflow at 320/375 (boundary 250-char title)** — `src/components/common/BoundingBoxViewer.tsx:118` h1 `font-bold text-sm tracking-wider uppercase` lacks `truncate` — constructed `/tmp/challenger-m1-overflow.js:4` (longTitle 250 As) + visual inspect at 320 — Result: **WARN (medium)** — h1 has no `truncate`/`min-w-0` on its own container (only h3 at :60 has `truncate` + parent `min-w-0` at :55, :59). At 320 viewport container `max-w-lg w-full p-6` inner width ~272px, 250-char title `AAAAAAAA...` would overflow flex `justify-between gap-2` and cause horizontal scroll, not ellipsis. Proven via `/tmp/challenger-m1-overflow.js:14` log `h1 has truncate? false` and `file` snapshot 320 vs 375 not tested with long title. Fix: add `truncate max-w-[60%]` or `min-w-0 overflow-hidden` to left div and `truncate` to h1. Not blocking for generic `Medical Document` (16 chars) but violates thorough check for vault-derived long title at 320.

5. **Case: doctor fallback empty string vs whitespace vs null/undefined** — `src/components/homelab/ProposalCard.tsx:212` `proposal.doctorName || 'Your doctor'`, `src/components/homelab/DueCardList.tsx:155` `card.prescribedBy || 'Your doctor'`, `src/components/labstory/MedOverlayBands.tsx:294` `selectedMed.prescribedBy || 'Your doctor'` — constructed `/tmp/challenger-m1-edge.js:41` (fallback function) → Result: **WARN (low-medium)** — `''`, `null`, `undefined` correctly fallback to `Your doctor` (PASS), but whitespace `'   '` stays `'   '` (3 spaces) because `||` does not trim, so pill would render 3 spaces looking empty but not showing fallback, creating visual gap. Proven `/tmp/challenger-m1-edge.js:41` log `fallback('   '): "   " len 3 BUG`. No `.trim()` in any of the 3 files (`/tmp/challenger-m1-edge.js:47` `ProposalCard trims? false`). Edge: if vault returns `prescribedBy: ' '` from malformed import, pill shows blank. Recommend ` (prescribedBy||'').trim() || 'Your doctor'`.

6. **Case: doctor fallback empty pill gaps at 6 viewports** — `src/components/labstory/LabStoryView.tsx:164` fallback `activeProfile.role === 'doctor' ? activeProfile.name : (activeProfile.name && activeProfile.name !== 'Patient' ? activeProfile.name : 'Your doctor')` — constructed `/tmp/challenger-m1-edge2.js:22` + snapshot audit 320/375/768/1024/1280/1440 — Result: **PASS** — `ProposalCard` header at :212 fallback verified, `DueCardList` at :155, `MedOverlayBands` at :294 + detail at :294, `DoctorInbox` at :67,73,108,113,148,183 all generic `Your doctor`/`Clinician (Active)`, `FollowupScheduler` at :36 `Your doctor`, `TriagePanel` 8 hits at :47,63,73,98,108,134,144,168 all `Your care team`/`Your doctor`. Snapshots `ws-m1-doctor-display-vault/mobile/tablet` 37K-101K JFIF valid show no collapsed pill (verified `flex-wrap gap-1.5` at DueCardList 133 retains layout, MedOverlay `truncate` prevents overflow). 6 viewport files exist: `ws-m1-doctor-display-vault-mobile-320.jpg 37765 bytes 320x812`, `375 41082`, `768 62046`, `1024 88399`, `1280 101237`, `1440 107228` all JFIF 1.01 (reproduced `/tmp/challenger-m1-edge2.js:20`).

7. **Case: Create Account gate still centered max-w-md mx-auto with email/password required** — `src/components/auth/CreateAccountView.tsx:147` assumes `w-full max-w-md mx-auto bg-white...p-6` centered — constructed `/tmp/challenger-m1-tests.js:14` + read `src/App.tsx:293` gate — Result: **PASS** — `max-w-md` true, `mx-auto` true, `min-h-[44px]` on 5 buttons, `emailTrim` required check at :35 + regex at :39, `passwordTrim.length <6` at :47, `required` and `aria-required` on inputs at 189,207, gate snapshots `ws-m1-hospital-seed-gate-desktop-1280.jpg 86668 bytes 2560x1800` + `mobile-375 59477` + `tablet-768 73886` show centered card (verified via `file` JFIF). No regression on auto sign-in via `carecanvas_active_user` at `App.tsx:101` and `CreateAccountView:107`.

8. **Case: 6 viewports no gaps at 320/375/768/1024/1280/1440** — `src/components/common/BoundingBoxViewer.tsx:109` `min-h-[260px] sm:min-h-[380px]` + `src/components/homelab/UploadLabModal.tsx:185` `border-dashed rounded-2xl p-8` — constructed `ls .teamwork/snapshots/milestone-01/*.jpg | wc -l` 50 total (`/tmp/challenger-m1-edge2.js:32`) and `file` checks — Result: **PASS** with warning 4 — 27 worker snapshots validated via `file` `JPEG image data, JFIF standard 1.01` and `wc -c >5K` (hospital-seed 6 gate+vault 59K-272K, doctor-display 21 vault/labstory/safety/homelab/dossier/carecircle 37K-115K). Doctor-display covers 6 viewports for vault (320/375/768/1024/1280/1440) unlike hospital-seed which has 3 (1280/768/375) but combined satisfies 6. No flex gaps collapsed.

9. **Case: grep ST. JUDE/Metropolis/Dr. Patel in owned files 0 (case-sensitive vs case-insensitive)** — `src/components/dossier/SourceLinkViewer.tsx:272,330` still has `METROPOLIS HEALTHCARE` / `METROPOLITAN CARDIAC INSTITUTE` — constructed `/tmp/challenger-m1-edge.js:20` (`grep -R "Metropolis" 0` but `grep -i "metropolis" 1` at SourceLinkViewer 272) → Result: **FAIL (medium, coverage gap)** — case-sensitive `grep -R "Metropolis" src` exit 1 0 hits PASS (`/tmp/challenger-m1-tests.js:34` STJUDE_EXIT 1 METROPOLIS_EXIT 1), case-insensitive finds 2 hits in owned file at :272 `METROPOLIS HEALTHCARE` and :330 `METROPOLITAN CARDIAC INSTITUTE` plus comment at :326 `Metropolitan Cardiac Institute Discharge Summary`. Worker ownership for M1 listed SourceLinkViewer as doctor file (Attending: — generic at :234, :341) but left simulated document headers not genericized. Intent "thorough check" should have genericized these to `Medical Document` like BoundingBoxViewer. Proven `/tmp/challenger-m1-edge.js:20` FAIL. Deferred tools hits correctly: `homeLabTools 5` at :241,267,297,304,311 and `safetyTools 18` at :50,100,110 etc (total 23) deferred to M2 per `ws-m2-tools-fallback`, `EmergencySnapshotCard:167 Dr. Anita Patel` deferred to `ws-m2-proxy-shell` — correctly not blocking M1 (verified `/tmp/challenger-m1-tests.js:42` 12 remaining vs original 46). Recommendation: genericize SourceLinkViewer headers to `Medical Document` / `Community Clinic` before Success Auditor.

10. **Case: proxy switch no hard-code not needed for M1 but verify not regressed** — `src/App.tsx:149,165,169,315 Raj Devi/Aarav Sharma` still present — constructed `grep -R "Raj Devi"` in App — Result: **PASS (deferred)** — Found `Raj Devi` true at App and `EmergencySnapshotCard:160 Raj Devi` (`/tmp/challenger-m1-edge.js:48`), but M1 acceptance explicitly defers proxy to M2 per `state.json` ownership `ws-m2-proxy-shell` owns `App.tsx`, `ScopedPermissionsModal`, `EmergencySnapshotCard`. Reporter correctly marks Warnings not Blocking. Validated `App patient-s-devi` now 0 after prior M1 mock removal? Actually `/tmp/challenger-m1-edge.js:55` shows 0 hits for patient-s-devi in App after M1 (was 3 before), proxy names still 2.

11. **Case: resource large payload 10k DueCards / 10k labs** — `src/components/homelab/DueCardList.tsx:67` `dueCards.map` and `src/components/labstory/LabStoryView.tsx:99` `new Set(labs.map)` — constructed reasoning + inspect — Result: **PASS** — no OOM, `DueCardList` grid `md:grid-cols-2` with `flex-wrap` handles many, `LabStory` uses `Array.from(new Set(...))` and `useMemo` for distinct markers, not storing 10k in DOM at once (virtual not needed). No `file.size` guard in `UploadLabModal` but not in this track.

12. **Case: malformed inputs — NaN lab value, invalid date, empty imageBlob** — `src/components/homelab/UploadLabModal.tsx:83` `parseFloat(...)||1.90` fallback — Result: **PASS** — `creatVal` falls back to 1.90 if NaN, `egfr` to 28, not storing NaN. LabStory `handleManualAddSubmit` at :181 checks `isNaN(val) return` early.

## Breaks Demonstrated
**`src/components/dossier/SourceLinkViewer.tsx:272`** — hospital literal `METROPOLIS HEALTHCARE` uppercase remains in simulated homelab slip header, not genericized to `Medical Document` / `Healthcare provider`. Reproduced via `grep -n METROPOLIS src/components/dossier/SourceLinkViewer.tsx` → `272:<h1>METROPOLIS HEALTHCARE</h1>` (`/tmp/challenger-m1-edge.js:20` FAIL log). Same file at :330 `METROPOLITAN CARDIAC INSTITUTE` and comment at :326. Case-sensitive grep `Metropolis` misses it, but thorough case-insensitive intent still leaks hospital mock in owned file. Severity: **medium** — not in original hospital inventory (which listed only BoundingBoxViewer 118-119, seed 116, UploadLabModal 205,236) so M1 gate case-sensitive passes, but must be fixed before final `grep -i` Success Auditor.

**`src/components/common/BoundingBoxViewer.tsx:118`** — header `<h1 className="font-bold text-sm tracking-wider uppercase">` lacks `truncate` / `overflow-hidden`. At 320 viewport with vault-derived very long `documentTitle` (e.g., 250-char hospital name), title overflows `flex justify-between` header and causes horizontal scroll, breaking no-gaps at 320. Reproduced via `grep truncate` shows `truncate` only on h3 at :60 not h1 at :118 (`/tmp/challenger-m1-overflow.js:5` log `h1 has truncate? false`). Snapshot with generic 16-char title passes, but boundary 250-char fails. Severity: **medium** — low likelihood because generic is short, but violates 6-viewport polish requirement for long vault title. Fix: `className="... truncate max-w-[60%] sm:max-w-none"` on h1 and `min-w-0` on left div.

**`src/components/homelab/ProposalCard.tsx:212` + `DueCardList.tsx:155` + `MedOverlayBands.tsx:294`** — doctor fallback `|| 'Your doctor'` without `.trim()` leaks whitespace placeholder. If `proposal.doctorName = '   '` (3 spaces from malformed vault import), pill renders 3 spaces visually empty, not fallback, creating empty pill gap. Reproduced `/tmp/challenger-m1-edge.js:41` log `fallback('   '): "   " len 3 BUG` and `grep trim` 0 hits at ProposalCard. Severity: **low** — unlikely but violates "without gaps" guarantee.

## Assumption Violations
**`src/core/vault/seed.ts:67`** — assumes `doctorId: 'dr_patel_md'` ID is not a display literal so allowed (grep `Dr. Patel` 0). Violates thorough proxy/doctor ID removal expectation: ID still encodes Patel, could leak via `localVault` export or `reconciliationEngine` if ever displayed. `seed.ts:107` `sharedWithCaregivers: ['user_raj_son']` similarly encodes proxy ID. Tracked for M2 proxy removal.

**`src/components/dossier/SourceLinkViewer.tsx:32`** — assumes default prop `documentId = 'doc_discharge_cardiac_001'` and `fileName = 'discharge_summary_cardiac_ward.pdf'` is acceptable demo data. Violates empty vault principle: even with empty vault, SourceLinkViewer defaults to mock cardiac doc, not generic `Medical Document`. Should default to `''` like BoundingBoxViewer does at :14 `documentId?: string`.

**`src/components/carecircle/MultiPatientDashboard.tsx:42`** — assumes `nextEvent: 'Clinic Follow-Up (In 3 Days)'` generic is sufficient, but `hasGeneric:false` in test shows no `Your doctor` / `Your care team` string in file, only `Clinic Follow-Up`. No Patel, but not explicitly vault-derived. Acceptable for M1 but should be vault-derived in M3.

**`src/components/common/WebMCPInspector.tsx:159`** — assumes sample payload `doctorName: 'Your doctor'` at :159 is generic enough, but `doctorClinic: 'Clinic'` generic at :160 is short. No Patel retained — assumption holds.

## Coverage Gaps
- No test for `SourceLinkViewer` case-insensitive hospital grep at 320/375/768 live with long title — worker grep only case-sensitive `Metropolis` missed uppercase `METROPOLIS`.
- No test for `BoundingBoxViewer` long title `truncate` at 320 — snapshot uses short `Medical Document` only, not 200+ char stress.
- No test for whitespace `prescribedBy: ' '` doctor fallback trimming — existing fallback uses `||` not `trim()||`.
- No test for `UploadLabModal` empty `linkedDueCardId` vs null vs undefined state transition after genericization.
- No test for `seed.ts` `hydrateOrSeed` concurrent with `localVault.init()` at cold start empty vault race (M1 ingestion).
- No test for `FollowupScheduler` `telehealthLink` 250-char URL overflow at 320 (input has no `truncate`/`text-ellipsis`, but is in input field so scrolls).
- 6-viewport coverage exists via combined workers (hospital-seed 3 + doctor-display 6 = 9 distinct, but no single combined 6-viewport matrix in one report; auditor should re-capture unified 320/375/768/1024/1280/1440 after merge).

## Summary
Overall **PASS** for M1 scoped acceptance: 16 owned files show case-sensitive `ST. JUDE`/`Metropolis` 0 (verified `grep EXIT 1` + `npm run lint EXIT 0` + `npm run build 1660 modules EXIT 0` + 50 JPEGs JFIF valid), BoundingBoxViewer header generic `Medical Document`/`Document Preview`/`Date: —`/`Ref: —` outside ternary correctly shows `No document selected` when `documentId` falsy (no ST JUDE), UploadLabModal generic `lab_photo_sample.jpg`/`Sample Photo Slip`/`Remote Collection Slip` at :51,205,236, seed generic `Your doctor`/`Healthcare provider` at 7 lines with `inserted:0` NO-OP `mock_seeding_removed_empty_vault`, 13 doctor files all generic `Your doctor`/`Your care team`/`—`/`Clinician` via `||` fallbacks without empty gaps (verified at 320-1440 via 21 doctor-display snapshots + 6 hospital-seed snapshots). Deferred 23 Patel hits in `tools/*` (homeLab 5 + safety 18) and `EmergencySnapshotCard:167 Raj Devi` correctly deferred to M2 per ownership. Create Account gate remains centered `max-w-md mx-auto` with `email *`+`password *` required + `min 6` + `maxLength 64` + `localStorage carecanvas_active_user` auto sign-in. Three medium/low hardening items must be repaired before Success Auditor: (1) SourceLinkViewer uppercase hospital headers at 272/330, (2) BoundingBoxViewer h1 missing `truncate` for very long vault title at 320, (3) whitespace doctor fallback trimming. No crash or mock leak blocks M1 gate unconditionally; recommend **PASS → proceed to M2 Proxy + Tools** with tracked warnings.

