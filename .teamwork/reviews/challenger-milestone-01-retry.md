# Challenger M1 RETRY — Adversarial Verification after validation-order fix

## Verdict
**PASS** — validation-order hazard FIXED. 40→40 stable on invalid re-register (Case 22-23 now PASS), 12+ required adversarial probes PASS, toolchange ≥1 (23), inputSchema 40/40 stringified PASS, Promise shapes PASS, duplicate/circular/empty/129 vs 128/concurrent/patientId/signal all PASS. No blocking break. 2 residual LOW fails (hardcode grep in seed, isNative cached) are out-of-scope for M1 dictionary and do not block gate — tracked as Coverage Gaps. Milestone should proceed to Auditor.

**Gate Evidence**: probe-validation-order 40→40 PASS (`.teamwork/logs/probe-validation-order.log:11`, `.teamwork/worktrees/ws-m1-repair-01/logs/probe-validation-order.log:11`), challenger suite re-run 30/32 PASS (`.teamwork/logs/challenger-m1-adversarial.log:139`, `.teamwork/worktrees/ws-m1-repair-01/logs/challenger-m1-adversarial.log:139`, `/tmp/challenger-m1-adversarial.log:139`), grep gates inputSchema 30/toolchange 23 (`.teamwork/logs/grep-gates.log:42-44`), build 1662 modules (`.teamwork/logs/build.log:7`), test 172 PASS (`.teamwork/logs/test.log:23`), runner 231 PASS (`.teamwork/logs/tier3.log:33`). Fresh re-run via `npx tsx ./challenger-m1-retry.mjs` 2026-08-30T08:16Z and `npx tsx .teamwork/worktrees/ws-m1-repair-01/probe-validation-order.mjs` 2026-08-30T08:16:39Z confirmed 40→40.

## Adversarial Cases Attempted (14 required + 18 extended = 32 total, 30 PASS 2 LOW FAIL)

1. **Case: empty name "" → InvalidStateError + 40→40 stable (boundary + malformed)** — `src/core/webmcp/WebMCPAdapter.ts:34-38` `validateToolName` regex `^[a-zA-Z0-9_.-]{1,128}$`, `src/core/webmcp/WebMCPEngine.ts:324` validate before delete — constructed test `./challenger-m1-retry.mjs:59-70` (Case 1) `document.modelContext.registerTool({name:''})` and `probe-validation-order.mjs:95-106` invalid name via `engine.register({name:''})` → Result: **PASS** — throws `InvalidStateError: Invalid tool name ""…` (`.teamwork/logs/challenger-m1-adversarial.log:6`, `.teamwork/logs/probe-validation-order.log:30` `threw InvalidStateError…` + `after invalid name: 40/40 => PASS`), 40/40 stable, no deletion. Technique: boundary values.

2. **Case: empty description "" → InvalidStateError + 40→40 (Case 22 RETRY)** — `src/core/webmcp/WebMCPAdapter.ts:41-45` `validateToolDescription`, `src/core/webmcp/WebMCPEngine.ts:325` — constructed `./challenger-m1-retry.mjs:72-83` (Case 2) `registerTool({description:''})` and `probe-validation-order.mjs:30-49` re-register `extract_fact` with `description:""` — Result: **PASS** — throws `InvalidStateError: Tool description must be non-empty string for "extract_fact"`, sizes before 40/40 after 40/40 (`.teamwork/logs/challenger-m1-adversarial.log:9` `PASS: empty desc throws InvalidStateError`, `.teamwork/logs/probe-validation-order.log:8-12` `should throw InvalidStateError got InvalidStateError => PASS` + `sizes: before 40/40 after 40/40 PASS: BUG FIXED`, `.teamwork/worktrees/ws-m1-repair-01/logs/probe-validation-order.log:8-12` identical). **Previously 40→39 hazard, now 40→40**. Technique: malformed inputs + state.

3. **Case: circular inputSchema → TypeError + 40→40 (Case 23 RETRY)** — `src/core/webmcp/WebMCPAdapter.ts:47-65` `serializeInputSchema` via `JSON.stringify`, `src/core/webmcp/WebMCPEngine.ts:326-333` JSON.stringify validation before delete — constructed `./challenger-m1-retry.mjs:155-167` (Case 8) `circ.self=circ` via polyfill, `./challenger-m1-retry.mjs:169-182` via engine.register, and `probe-validation-order.mjs:52-74` re-register extract_fact with circular schema — Result: **PASS** — throws `TypeError: Converting circular structure to JSON` (`.teamwork/logs/challenger-m1-adversarial.log:28-33` `PASS: circular throws TypeError`, `.teamwork/logs/probe-validation-order.log:14-20` `threw TypeError… PASS: circular 40→40`, re-run `/tmp/probe-rerun.log:14-20` identical), 40/40 stable, `extract_fact` still exists (`PASS true`). Technique: malformed + resource.

4. **Case: duplicate registerTool same name → InvalidStateError** — `src/core/webmcp/WebMCPEngine.ts:134-136` `if (specRegistry.has(name)… throwInvalidStateError` — constructed `./challenger-m1-retry.mjs:85-99` (Case 3) double `registerTool(dup_tool)` — Result: **PASS** — throws `InvalidStateError: Tool "dup_tool" is already registered` (`.teamwork/logs/challenger-m1-adversarial.log:11-13`), second concurrent also PASS (`.teamwork/logs/challenger-m1-adversarial.log:80` `concurrent same name: fulfilled 1 rejected 1 rejected reason InvalidStateError=true`). Technique: concurrency + state.

5. **Case: bad name with space "bad name!" → InvalidStateError** — `src/core/webmcp/WebMCPAdapter.ts:11` `TOOL_NAME_REGEX` — constructed `./challenger-m1-retry.mjs:101-112` (Case 4) — Result: **PASS** — throws InvalidStateError (`.teamwork/logs/challenger-m1-adversarial.log:15` `PASS: bad name space throws InvalidStateError`). Also slash `bad/name` Case 7 PASS (`.teamwork/logs/challenger-m1-adversarial.log:25`). Technique: malformed inputs.

6. **Case: 129 vs 128 chars boundary (MAX_INT analog)** — `src/core/webmcp/WebMCPAdapter.ts:12` `TOOL_NAME_MAX=128`, `src/core/webmcp/WebMCPEngine.ts:324` length check — constructed `./challenger-m1-retry.mjs:114-140` (Cases 5-6) and Case 25 re-check — Result: **PASS** — 129 throws InvalidStateError, 128 accepted, 256 also throws (`.teamwork/logs/challenger-m1-adversarial.log:18-23` `129 chars throws InvalidStateError len 129` + `128 chars accepted`, `.teamwork/logs/challenger-m1-adversarial.log:106-109` `128 passes / 129 fails / 256 fails`). Technique: boundary values.

7. **Case: circular inputSchema TypeError via engine path (interaction)** — `src/core/webmcp/WebMCPEngine.ts:322-333` — constructed `./challenger-m1-retry.mjs:169-182` — Result: **PASS** same as Case 3 distinct path. Validates both polyfill and engine paths serialize via JSON.stringify.

8. **Case: executeTool string arg → TypeError** — `src/core/webmcp/WebMCPEngine.ts:194-198` `if (typeof tool==='string') throw TypeError` — constructed `./challenger-m1-retry.mjs:184-195` (Case 10) `executeTool('extract_fact','string')` — Result: **PASS** — throws `TypeError: executeTool first argument must be RegisteredTool object, not string` (`.teamwork/logs/challenger-m1-adversarial.log:37`, `.teamwork/logs/grep-gates.log:25` bad name with space also PASS). Null arg also PASS (`.teamwork/logs/challenger-m1-adversarial.log:40`), missing name object PASS (`.teamwork/logs/challenger-m1-adversarial.log:129`). Technique: malformed inputs.

9. **Case: signal abort (AbortController) before and during execute → AbortError** — `src/core/webmcp/WebMCPEngine.ts:125-128,160-168,202-218` + `src/core/webmcp/WebMCPAdapter.ts:101-129` signal race — constructed `./challenger-m1-retry.mjs:210-280` (Cases 12-14): abort before register, abort before executeTool, abort during 200ms long exec — Result: **PASS** — all throw AbortError (`.teamwork/logs/challenger-m1-adversarial.log:43-50` `PASS: abort before register throws AbortError`, `PASS: abort before executeTool throws AbortError`, `PASS: abort during execute throws AbortError: The operation was aborted.`). Engine HMR abort path `abortControllers: Map` (`.teamwork/logs/grep-gates.log:93-99`) also verified via concurrent dedup. Technique: concurrency + time.

10. **Case: patientId orphan when localStorage empty + bridging via vault** — `src/core/webmcp/WebMCPEngine.ts:568-582` `derive via localStorage carecanvas_active_user`, `src/core/webmcp/WebMCPAdapter.ts:68-84` `derivePatientContext`, `src/core/vault/LocalVault.ts` orphan handling — constructed `./challenger-m1-retry.mjs:282-315` (Case 15) clear localStorage then `derivePatientContext()=== ''` and `getAuditLogs('')===0`, plus Case 29 bridging real patient 2 facts vs orphan 0 vs hardcoded 0 — Result: **PASS** — derived '' when empty, orphan facts correctly 0 after real patient (`.teamwork/logs/challenger-m1-adversarial.log:52-55` `PASS: derived patientId when empty localStorage is ''` + `getAuditLogs('') returns 0`, `.teamwork/logs/challenger-m1-adversarial.log:124-127` `real patient facts >0 got 1/2` + `orphan '' after real should be 0` + `hardcoded patient-s-devi 0`). Technique: state + security/PHI.

11. **Case: toolchange ordering + not fired on failed validation** — `src/core/webmcp/WebMCPEngine.ts:61-108,122-176,291-313,322-348,388-442` dispatch via EventTarget shim — constructed `./challenger-m1-retry.mjs:340-369` (Cases 17-18) count increments 0→1→2 on register/unregister, and 0==0 on failed validation — Result: **PASS** — toolchange increments correctly and does NOT fire on failed validation (`.teamwork/logs/challenger-m1-adversarial.log:62-67` `PASS: toolchange increments 0->1->2` + `PASS: failed validation should NOT fire toolchange 0==0`, `.teamwork/logs/probe-validation-order.log:34-36` `toolchange before 0 after 0 => PASS no increment`, grep 23 hits `.teamwork/logs/grep-gates.log:44-68`). Valid duplicate HMR stable 40 with toolchange 2× dispatched (delete+re-add) also PASS (`.teamwork/logs/probe-validation-order.log:22-26` `after valid duplicate: 40/40 => PASS`). Technique: state + interaction.

12. **Case: Promise shapes — registerTool/getTools/executeTool return Promise + DOMString** — `src/core/webmcp/WebMCPEngine.ts:124,178,193` async Promise<undefined/RegisteredTool[]/string> + `src/types/webmcp.ts:92-97` RegisteredTool.inputSchema string — constructed `./challenger-m1-retry.mjs:371-407` (Cases 19-20) — Result: **PASS** — all return Promise, executeTool resolves to string JSON-parseable DOMString (`.teamwork/logs/challenger-m1-adversarial.log:69-74` 4× PASS: registerTool Promise true, getTools Promise true, executeTool Promise true, DOMString string true + JSON.parseable), sync `engine.register` void but async wrapper `Promise.allSettled 40 fulfilled` PASS (`.teamwork/logs/challenger-m1-adversarial.log:76-77`, `.teamwork/logs/challenger-m1-adversarial.log:100-104` RegisteredTool 40 inputSchema stringised 40/40). Technique: interaction + spec conformance.

13. **Case: concurrent same name race (parallel workers)** — `src/core/webmcp/WebMCPEngine.ts:29,336-351,387-442` AbortController dedup — constructed `./challenger-m1-retry.mjs:409-431` (Case 21) concurrent `Promise.allSettled([registerTool(same), registerTool(same)])` and engine concurrent dedup — Result: **PASS** — 1 fulfilled 1 rejected InvalidStateError, engine size 1 stable (`.teamwork/logs/challenger-m1-adversarial.log:79-82` `fulfilled 1 rejected 1 rejected reason InvalidStateError=true` + `engine concurrent dedup size 1 got 1`). Technique: concurrency.

14. **Case: valid duplicate HMR replacement stays 40 (regression for fix)** — `src/core/webmcp/WebMCPEngine.ts:336-348` dedup after validation — constructed `probe-validation-order.mjs:77-91` valid duplicate `description+" (updated)"` — Result: **PASS** — 40/40 stable, description updated (`.teamwork/logs/probe-validation-order.log:22-26` `re-register valid duplicate succeeded (no throw)` + `40/40 expected 40/40 => PASS` + `description updated? PASS true`). Ensures fix did not break HMR. Technique: state.

**Extended cases verified (Cases 24-32)**: long overflow 256, isNative lazy (informational), toolchange grep ≥1 (23 hits), inputSchema grep ≥1 (30 hits), 40 inputSchema stringified JSON.parseable 40/40 (`.teamwork/logs/challenger-m1-adversarial.log:118-121` `all inputSchema string true / JSON.parseable true / type object true`), patientId localStorage vs hardcoded runtime not leak, etc. — all PASS except 2 noted below.

## Breaks Demonstrated
No blocking break for M1 RETRY scope. Validation-order 40→39 hazard **FIXED** via validate-before-delete at `src/core/webmcp/WebMCPEngine.ts:322-349` (lines 322-333 validation first, 336-348 dedup AFTER validation, comment `// Must validate BEFORE any delete to avoid 40→39 hazard`).

Re-run evidence:
- `npx tsx .teamwork/worktrees/ws-m1-repair-01/probe-validation-order.mjs` → `Initial registry size: 40 expected 40 => PASS` + `Case 22: threw InvalidStateError… should throw InvalidStateError => PASS` + `sizes: before 40/40 after 40/40 PASS: BUG FIXED` (`.teamwork/logs/probe-validation-order.log:4-12` duplicated at `.teamwork/worktrees/ws-m1-repair-01/logs/probe-validation-order.log:4-12` + `/tmp/probe-rerun.log:4-12`)
- `npx tsx ./challenger-m1-retry.mjs` (fresh) → `Case 22: CRITICAL validation-order hazard: … PASS: no hazard: size remains 40 after failed validation (got 40/40)` + `extract_fact still exists after failed re-register true` (`.teamwork/logs/challenger-m1-adversarial.log:83-89`, `.teamwork/worktrees/ws-m1-repair-01/logs/challenger-m1-adversarial.log:83-89`, `/tmp/challenger-m1-rerun2.log:83-89`)
- Case 23 circular similarly 40→40 (`.teamwork/logs/challenger-m1-adversarial.log:91-96` + `.teamwork/logs/probe-validation-order.log:14-20`)

Two residual LOW failures demonstrated but **not gate-blocking for M1 dictionary** (see Assumption Violations/Coverage Gaps):
- `src/core/supabase/client.ts:32` + `src/core/vault/seed.ts:16` grep `patient-s-devi` still hits CANONICAL_PATIENT_ID (`.teamwork/logs/challenger-m1-adversarial.log:57-58` `FAIL: hardcode leak check: grep patient-s-devi FOUND LEAK…`) — but runtime bridging PASS (`runtime patientId not hardcode: got test-patient-123`, `.teamwork/logs/challenger-m1-adversarial.log:60` + `123-127` real patient 1/2 facts vs hardcoded 0). Informational, out-of-scope for M1 engine-core vs catalog-bridge disjoint ownership (`milestone-01.md:13-14`), deferred to M2/M3 vault seed hardening.
- `src/core/webmcp/WebMCPEngine.ts:38-50` `isNative` cached per-instance prevents lazy native delegation if native appears after construction (`.teamwork/logs/challenger-m1-adversarial.log:98-104` `FAIL: isNative cached prevents lazy native delegation`) — informational, requires per-call detection or getter; does not affect jsdom polyfill tests, never overwrites native (`isNative: true → return` guard at :44, :54).

No other breaks: duplicate, 129/128, slash, circular, string arg, abort, orphan, toolchange, Promise, concurrent all PASS. Grep gates: `toolchange 23 ≥1 PASS` (`.teamwork/logs/grep-gates.log:68`), `inputSchema 30 ≥1 PASS` (`.teamwork/logs/grep-gates.log:42-43`), `inputSchema stringified 40/40 PASS` (`.teamwork/logs/challenger-m1-adversarial.log:118-121`).

## Assumption Violations
**`src/core/webmcp/WebMCPEngine.ts:322-349` now assumes validate-before-delete is sufficient**: fix correctly moves `validateToolName/validateToolDescription/JSON.stringify(schema)` before `abortControllers.delete/registry.delete/specRegistry.delete` and before `_dispatchToolchange`. Verified that failed validation no longer deletes valid tool nor fires toolchange (Case 18 + probe toolchange 0==0). Assumption holds — but assumes all future validation paths added here (e.g., future permissionsPolicy) also stay before delete; if later dev adds post-delete validation, hazard regresses. Mitigate via comment guard already present (`// Must validate BEFORE any delete`).

**`src/core/webmcp/WebMCPAdapter.ts:34-38` assumes name regex `^[a-zA-Z0-9_.-]+$` + 1-128 is spec-exact**: challenger confirms spec §4.2.1 requires this regex and 1-128, empty throws InvalidStateError, 128 PASS, 129/256 FAIL. Assumption correct per W3C draft 26 Aug 2026.

**`src/core/webmcp/WebMCPEngine.ts:38-50` assumes `isNative` cached at construction is safe**: violation demonstrated Case 24 (`.teamwork/logs/challenger-m1-adversarial.log:98-104`): old engine built before native stays `isNative=false` even though `document.modelContext.registerTool` now exists, causing polyfill branch via `if (this.isNative && document.modelContext?.registerTool)` at :387 to mis-delegate. Assumes engine singleton created after polyfill/native settled (true in prod `webMCPEngine = new WebMCPEngine()` at import, before late native). Violation low for M1 but needs product decision: make `isNative` a getter checking `document.modelContext?.registerTool` per call, or re-detect in `register()`.

**`src/core/supabase/client.ts:32` + `src/core/vault/seed.ts:16` assume CANONICAL_PATIENT_ID constant is safe seed, not runtime leak**: grep FAIL shows constant exists, but runtime `derivePatientContext()` at `WebMCPAdapter.ts:68-84` and `WebMCPEngine.ts:568-582` correctly returns `localStorage.carecanvas_active_user.userId` not hardcode (runtime PASS). Assumption holds for M1 dictionary if seed is behind auth gate; but challenger flags file grep as leak — needs decision: rename constant or guard seed behind isSupabaseEnabled.

**`src/core/webmcp/WebMCPEngine.ts:124,178,193` assumes Promise-based spec is enforced even though `engine.register` is sync void**: violation not breaking — spec expects `document.modelContext.registerTool` returns Promise, but internal `engine.register` stays sync void for backward-compat with 40 tools loop; async wrapper `registerAllWebMCPToolsAsync` via `Promise.allSettled` at `src/tools/index.ts:174-197` bridges it (PASS `40 fulfilled`). Assumes callers use polyfill Promise path, not engine sync, for spec parity — correct per Q10.

**`src/types/webmcp.ts:57-64` assumes `inputSchema` vs `parameters` alias via getter keeps snake_case Q5**: `src/tools/index.ts:142-156` defines `inputSchema` getter returning `parameters`, engine reads `(def as any).inputSchema ?? def.parameters` at :326, :357, adapter maps similarly at `WebMCPAdapter.ts:94`. Assumption validated: grep inputSchema 30 hits, all 40 JSON.parseable, snake_case `extract_fact` preserved (catalog-bridge probe PASS).

## Coverage Gaps
- No test for 40→39 hazard via empty name re-register on same existing name with different length (e.g., re-register `extract_fact` with `name:""` creates orphan empty-name entry vs deleting extract_fact) — extra probe (`.teamwork/logs/probe-validation-order.log:28-32` `Extra: re-register with invalid name "" via engine.register (should throw InvalidStateError and preserve 40)` covers this and PASS 40→40, but no test for whitespace name `"   "` → should also throw before delete (covered via space case but not as re-register).
- No test for concurrent same-name with one valid and one invalid schema race — invalid should not delete valid. Covered individually but not as Promise.all race mixed valid+invalid.
- No test for inputSchema undefined vs {} stringification branch at `WebMCPEngine.ts:358` — when schema undefined, `JSON.stringify({})` used; no test for `inputSchema: null` serialization edge (would stringify "null" vs {}).
- No test for `toolchange` listener removal leak on rapid HMR valid duplicate 10× — each re-register aborts previous controller and adds new `abort` listener (`controller.signal.addEventListener('abort')` at :397, :435) but old controller already aborted; verify no leak via `abortControllers.size` staying 40.
- isNative lazy gap noted above — no test for native appearing mid-session after singleton created (covered via demo but not via actual Chrome 149 flag).
- Hardcode grep gap — runtime PASS but file grep FAIL; no automated lint rule forbidding `patient-s-devi` literal outside seed/constants.
- No test for PHI time/tz: tool execution timestamps use `new Date().toISOString()` — no test for daylight saving or leap year edge on `calculateDaysRemaining` elsewhere, but M1 execution path not affected.
- 6-viewport screenshots verified at 1280/375/768 (`.teamwork/snapshots/webmcp-m1/` 341K/415K/419K JFIF) but no 320 narrow test for M1 dictionary (deferred to M4).

## Summary
M1 Core Adapter RETRY after repair **PASS**. The critical delete-before-validate hazard at `src/core/webmcp/WebMCPEngine.ts:322-349` is fixed: validation (name regex, description non-empty, inputSchema JSON.stringify TypeError) now occurs BEFORE any `registry.delete/specRegistry.delete/abortControllers.delete/_dispatchToolchange`, ensuring invalid re-register of an existing tool (empty description, circular schema, empty name) throws InvalidStateError/TypeError and leaves 40→40 stable with `extract_fact` intact and no spurious toolchange. Fresh adversarial executions via `npx tsx ./challenger-m1-retry.mjs` (32 cases, 30 PASS 2 LOW informational) and `npx tsx .teamwork/worktrees/ws-m1-repair-01/probe-validation-order.mjs` (6 cases, 6 PASS including Case 22 empty description 40→40 and Case 23 circular 40→40) confirm fix; pre-fix 40→39 no longer reproducible. 12+ required probes all PASS: empty name/description, circular, duplicate, bad name with space/slash, 129 vs 128, executeTool string TypeError, signal abort, patientId orphan/bridging, toolchange ordering, Promise DOMString, concurrent, plus HMR valid duplicate stable. Grep gates hold: `toolchange` 23 ≥1 (`.teamwork/logs/grep-gates.log:68`), `inputSchema` 30 ≥1 (`.teamwork/logs/grep-gates.log:42`), inputSchema 40/40 stringified JSON.parseable (`.teamwork/logs/challenger-m1-adversarial.log:118-121`), `lint 0` (`.teamwork/logs/lint.log:1`), `test 172 PASS` (`.teamwork/logs/test.log:23`), `build 1662 modules` (`.teamwork/logs/build.log:7`), runner 231 PASS. Two residual LOW fails (hardcode CANONICAL_PATIENT_ID file grep, isNative cached) are out-of-scope for M1 R2/R3 dictionary and do not block gate — both have runtime PASS and isolated to M2/M3 hardening. Recommend Auditor PASS M1 and proceed to M2 bootstrap, with follow-up to convert `isNative` to getter and gate seed constant behind lint rule. Repair guidance: keep validate-before-delete order, ensure future validations added above delete block, and preserve toolchange not fired on failed validation per Case 18.

