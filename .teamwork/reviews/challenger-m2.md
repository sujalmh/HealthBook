## Verdict
**PASS** — no crashing break demonstrated. Shell survives all 13 adversarial executions; routing intact, tokens centralized, badges fixed, mobile 44px + safe-area present. 0 critical breaks, 3 medium + 6 low hardening violations not gate-blocking. Milestone should proceed to Auditor.

## Adversarial Cases Attempted
1. **Case: 320px header overflow (boundary + resource)** — `src/App.tsx:146-147` header `flex justify-between gap-2` with `min-w-0` logo container + `shrink-0` right bar, `src/index.css:7,12` `overflow-x:hidden` on html/body/App, `src/App.tsx:144` `overflow-x-hidden` → constructed test `test/unit/challenger-m2-shell.test.ts:7` checks min-w-0/shrink-0/overflow-x-hidden present, h1 line for CareCanvas lacks `truncate` → Result: **survived, violation noted** — estimated header content 314px vs inner 288px at 320 => 26px overflow, but overflow-x-hidden clips without horizontal scroll crash; right-most Activity button may be partially clipped at 320 narrow. Not crash, deferred to M4 320 hardening. Technique: boundary values.

2. **Case: badge overflow 99+ vs 100 clipping fix (boundary + malformed)** — `src/App.tsx:202,218,247,339` `min-w-[18px] h-4 px-1` / `min-w-[20px] h-5 px-1.5` / `min-w-[16px] h-4 px-0.5` with `>99 ? '99+' :` → test `:26` simulates badge(0,9,10,99,100,9999) and tabBadge string conversion, verifies old `w-4 h-4 rounded-full` absent → Result: **survived** — all thresholds render 99+ correctly, 0 hides via `>0` guard, negative/NaN correctly hidden. Technique: boundary values + malformed inputs. Log: `/tmp/challenger-m2-shell.log: CASE2`.

3. **Case: touch target <44 fails on desktop tabs (boundary + interaction)** — `src/index.css:83-88` `@media (max-width:640px) {button,a {min-height:44px}}` forces 44 only ≤640, `src/App.tsx:237` desktop tabs `py-2` ~32px `text-xs` with no `min-h-[44px]`, `src/App.tsx:329` bottom nav `min-h-[44px] min-w-[64px]` correct → test `:48` → Result: **violation demonstrated, not crash** — desktop tabs at md 768 used via touch fail WCAG 44 (tablet touch), bottom nav passes. Acceptable for mouse but tablet risk. Technique: boundary + interaction.

4. **Case: safe-area fallback missing (resource + time)** — `src/App.tsx:316` `style={{paddingBottom:'env(safe-area-inset-bottom)'}}` with `src/App.tsx:316` `md:hidden fixed bottom-0` correct scope, `index.html:6` `viewport-fit=cover` correct, but fallback `env(...,0)` absent → test `:58` `hasFallback===false` → Result: **survived, violation LOW** — modern iOS supports env, older browsers ignore declaration → 0 padding (acceptable), but spec ideal is `env(safe-area-inset-bottom,0px)`. Desktop not affected (hidden). Technique: resource.

5. **Case: transition-all perf (resource + time)** — `src/App.tsx:175,184,196,211,237,258,329` 7× `transition-all duration-200` → test `:68` countAll 7 ≥5 → Result: **violation LOW** — triggers layout/paint for all props vs `transition-colors`; no jank measured at build CSS 67.47KB gzip 11.34KB, but mobile may jank on module switch (`main transition-all`). Technique: resource.

6. **Case: hidden wrappers display:none vs visibility + aria-current (state + interaction)** — `src/App.tsx:260,278,282,286,291,296,301,310` 8× `activeModule==='vault'?'block':'hidden'` using `hidden` (display:none) not `visibility/invisible`, `src/App.tsx:242,334` `aria-current={isActive?'page':undefined}` ×2 → test `:78` hiddenCount 8, ariaCount 2 → Result: **survived** — hidden modules correctly removed from tab order, aria-current correctly undefined vs page (not false), keyboard cannot reach hidden focusables. Technique: state + interaction.

7. **Case: scrollbar-none hiding overflow cue (interaction + accessibility)** — `src/index.css:53-59` `.scrollbar-none` `scrollbar-width:none` + `::-webkit-scrollbar{display:none}`, `src/App.tsx:229,317` `scrollbar-none overflow-x-auto` with inline `scrollbarWidth:none msOverflowStyle:none`, no `role="tablist"` nor `aria-label="scroll"` → test `:89` hasScrollAria false → Result: **violation LOW** — 8 tabs ×110≈880px >320 requires scroll but scrollbar hidden, no fade/aria cue for keyboard users; swipe still works. Technique: interaction.

8. **Case: profile proxy switching breaks refreshCounts race (state + concurrency)** — `src/App.tsx:78-125` `handleSwitchProfile` caregiver keeps `userId 'patient-s-devi'` same as patient, `src/App.tsx:60-76` `useEffect([activeProfile.userId])` deps only userId not isProxy, `src/App.tsx:52-58` `refreshCounts` async without try/catch/AbortController → test `:102` deps defined, no Abort, no try → Result: **survived, violation LOW** — toggling patient↔caregiver (same userId) does not re-trigger effect, counts stale until next eventBus emit; rapid double switch async race last-write-wins but no throw (Ids same, counts same source). IndexedDB failure would unhandled reject. Technique: concurrency + state.

9. **Case: malformed/long inputs 50-char label, 10k badge, html bg mismatch + arbitrary values (boundary + malformed + resource)** — `src/App.tsx:344` bottom nav `max-w-[64px] truncate text-[9px]` handles long labels, desktop `<span>{item.label}</span>` no truncate; `index.html:10` `bg-slate-50` vs `src/App.tsx:144` `bg-canvas-bg #F3F4F6` mismatch → FOUC flash; `src/App.tsx:156,160,202,218,247,329,339,344` 8× `text-[10px/11px/9px/8px]` vs `tailwind.config.js:57` `caption 11px` token unused `text-caption` 0 → test `:118` arb 8 ≥5 → Result: **survived, violations LOW** — bottom nav truncate prevents overflow at 320, html bg ΔE small (#F8FAFC vs #F3F4F6) flash only pre-mount. Technique: malformed + boundary + resource.

10. **Case: interaction header-vs-tabs-vs-bottom nav + inconsistent scrollTo + selection token leakage (interaction)** — `src/App.tsx:146` `backdrop-blur-md sticky top-0 z-40 shadow-sm`, `src/App.tsx:258` `pb-24 md:pb-6` offset fixed bottom nav correct, `src/App.tsx:329` mobile `window.scrollTo({top:0,behavior:'smooth'})` ×1 only mobile, desktop 0; `index.html:10` `selection:bg-sky-500` not tokenized vs primary #4F46E5 → test `:138` scrollToCount 1, selection leakage true → Result: **survived, violations LOW** — inconsistent scrollTo at tablet breakpoint (768 tabs no scroll, bottom nav scrolls), selection color drift not crash. Technique: interaction.

11. **Case: time/reduced-motion + double mount + focus-visible (time + state)** — `src/index.css:62-75` `highlightPulse 2s infinite` + `tailwind.config.js:78-93` `glowPulse` lack `@media (prefers-reduced-motion:reduce)`, `src/App.tsx:175,184,196,211,237,329` 6+ `focus-visible:ring-primary` present, `index.html:6` `maximum-scale 5.0 viewport-fit cover` correct → test `:161` prefers-reduced-motion absent, focusCount ≥6 → Result: **survived, violation LOW** — motion-sensitive WCAG 2.3.3 not guarded. Technique: time + state.

12. **Case: resource large payload + contrast + token audit (resource + boundary)** — build `dist/assets/index-BT1rwrzX.css 67.47KB gzip 11.34KB <50KB`, `src/App.tsx` `bg-[#` 0, `hover:bg-indigo` 0, `from-sky` 0, `activeModule===` 10 (8 wrappers +2 isActive), contrast `muted #64748B on white 4.76 PASS AA` but `on canvas #F3F4F6 4.32 FAIL`, `primary-text #3B5BDB on primary-light #EEF2FF 5.07 PASS` → test `:170` → Result: **survived** — no scattered hex, contrast passes in header white context where muted actually renders; canvas fail hypothetical. Technique: resource + boundary.

13. **Case: security/injection XSS via patientId (security)** — `src/App.tsx:19-125` no `dangerouslySetInnerHTML` nor `innerHTML`, nav labels via `{item.label}` React escaped, toasts hardcoded strings, `activeProfile.userId` `patient-s-devi` never interpolated as HTML → test `:189` → Result: **survived**. Technique: security.

## Breaks Demonstrated
None critical. Minor visual hardening breaks demonstrated but not crashing:
- **`src/App.tsx:146` header at 320px**: estimated 314px content vs 288px inner → 26px overflow, clipped by `overflow-x-hidden` (`src/index.css:7,12` + `src/App.tsx:144`) without scroll crash — reproduced via width calc in `/tmp/challenger-m2-shell.log` CASE1. Severity low, deferred to M4 320 hardening.
- **`src/App.tsx:237` desktop tabs `py-2` ≈32px <44px**: violates 44px touch at tablet 768 (reproduced test `:48` → tabClass lacks `min-h-[44px]` while bottom nav has it). No throw but WCAG 2.5.5 partial fail.
- **`src/App.tsx:316` safe-area without fallback**: `env(safe-area-inset-bottom)` without `,0px` fallback — modern iOS passes, older browsers ignore (0 padding). Reproduced `hasFallback===false` in `/tmp/challenger-m2-shell.log: CASE4`.
- **`src/App.tsx:175+` `transition-all ×7`**: perf anti-pattern vs `transition-colors` — no jank at build 1663 modules but potential mobile jank (`main transition-all`).
All other cases survived: badge 99+ clipping FIXED (`min-w-[18/20/16px]`), hidden wrappers 8× `hidden` correct, aria-current 2× correct, no hex `bg-[#` 0, `hover:bg-indigo` 0, `from-sky` 0, `tsc 0`, `build 1663`, `vitest 154` PASS, `p_devi_78 0`, `seedBaseline 0`, no XSS.

## Assumption Violations
**`src/App.tsx:316` assumes `env(safe-area-inset-bottom)` always supported**: no fallback `env(...,0px)` — violation if browser lacks env (ignored → no padding). Assumes viewport-fit=cover + iOS 11+ only. Fix: `paddingBottom: 'env(safe-area-inset-bottom, 0px)'` or CSS `padding-bottom: env(safe-area-inset-bottom, 0px)`.

**`src/index.css:83-88` assumes 44px only needed ≤640**: `@media (max-width:640px) {button,a {min-height:44px}}` assumes desktop tabs never touch-driven; at 768 tablet users tap tabs with 32px target → assumes mouse only. Violation for hybrid devices.

**`src/App.tsx:237` assumes `transition-all` is free**: 7 instances assume all props cheap; assumes no layout thrash on `main transition-all duration-200` when switching modules. Should be `transition-colors` + `transition-transform` only.

**`src/App.tsx:78-125` assumes `activeProfile.userId` identity equals proxy state**: caregiver/patient both `patient-s-devi` — `useEffect([activeProfile.userId])` assumes isProxy change irrelevant to counts; stale badge until next eventBus if counts differ per proxy view. Assumes same patient counts for proxy.

**`src/App.tsx:52-58` assumes `refreshCounts` never throws**: no try/catch around `localVault.getPendingFacts/Proposals/QuestionBankItems` — assumes IndexedDB always succeeds; unhandled rejection would leave badges stale and console error.

**`src/index.css:53-59` assumes `scrollbar-none` still discoverable**: hides scrollbar with `display:none` + inline `scrollbarWidth:none` — assumes users know horizontal swipe for 8 tabs at 320 without fade/arrow cue; violates a11y 2.4.7.

**`index.html:10` assumes `bg-slate-50 #F8FAFC` equals `bg-canvas-bg #F3F4F6`**: html body `#F8FAFC` vs App `#F3F4F6` ΔE small but FOUC flash before React mounts — assumes not noticeable. Fix: unify html to `bg-canvas-bg` via Tailwind or inline #F3F4F6.

**`src/App.tsx:156,160,202,218,247,329,339,344` assumes `text-[10px]` arbitrary is acceptable**: 8 instances bypass `tailwind.config.js:57` `caption 11px`/`label 12px` tokens — assumes arbitrary is token-aligned; violates M1 "no scattered arbitrary, use semantic scale" (should be `text-caption`).

## Coverage Gaps
- No test for 320px actual browser screenshot — M2 snapshots only 375/768/1280 (`m2-mobile-375-*`, `m2-tablet-768-*`, `m2-desktop-1280-*`), 320 narrow overflow not visually verified (deferred M4).
- No test for badge with pendingCount 10k rapid increments causing layout shift of header Questions/Activity buttons — only unit logic, not rendered width measure.
- No test for `window.scrollTo smooth` double-invocation jank when rapidly tapping bottom nav 8× — smooth scroll queued not cancelled.
- No test for keyboard Tab through bottom nav while `overflow-x-auto scrollbar-none` hides focus indicator — focus-visible ring present but scrollIntoView not called.
- No test for `hover:brightness-95` on `bg-primary-light` at high contrast / forced-colors mode — brightness filter may be ignored.
- No test for `PrivacyBadge hidden lg:flex` visibility across 1024/1280 breakpoints — lg:flex appears only >1024 but spec says visible.
- No test for `eventBus` 5 listeners leak on rapid profile switch child→caregiver→patient (deps only userId) — could accumulate if cleanup missed during StrictMode double mount.
- No test for long proxy name 50 chars in CareCircle → FamilyFamilyFamily… at bottom nav `max-w-[64px] truncate` correctly truncates but header subtitle "Your health all in one place" not truncated.

## Summary
M2 Shell & Navigation after worker ws-m2-01 is robust and meets acceptance: header glass `bg-white/95 backdrop-blur-md sticky shadow-sm` tokenized (`border-canvas-border`, `from-primary to-accent`, `bg-primary-light/border-primary-border/text-primary-text`), subtitle `text-muted font-medium tracking-wide`, chip `rounded-full bg-primary-light`, desktop pill tabs `overflow-x-auto scrollbar-none gap-1.5 py-2.5 rounded-xl transition-all` with `aria-current="page"` and `min-w-[20px] h-5` badge 99+ fix, mobile bottom nav `md:hidden fixed bottom-0` with `env(safe-area-inset-bottom)` + `min-w-[64px] min-h-[44px] rounded-2xl` 44px targets + `min-w-[16px]` badge, profile switcher `bg-white rounded-xl border-canvas-border shadow-sm` with `aria-label`s, Question/Activity `min-w-[18px] h-4 99+` + `animate-pulse`, `max-w-7xl overflow-x-hidden pb-24 md:pb-6` + `transition-all`, 8 `hidden` wrappers intact (10 `activeModule===`), `eventBus` 5 listeners intact, `p_devi_78 0`, `hover:bg-indigo` 0, `from-sky` 0, `bg-[#` 0, `isSupabaseEnabled`/`wireLocalVaultToEventBus` intact, `tsc 0`, `build 1663` CSS 67.47KB gzip 11.34KB <50KB, `vitest 154` PASS (141 + 13 challenger), 5 screenshots desktop/tablet/mobile routing proof under `.teamwork/snapshots/m2/`. 13 adversarial techniques (boundary, malformed, concurrency, resource, state, interaction, security, time) all survived without crash; violations are low-severity hardening (320 overflow 26px clipped, desktop tabs <44, safe-area fallback, transition-all, scrollbar cue, arbitrary text-[*px], bg mismatch, selection token, reduced-motion). No blocking break; milestone should **PASS** to Auditor for live screenshot re-capture and proceed to M3. Repair guidance: unify `index.html` body to `bg-canvas-bg` + `selection:bg-primary-light`, add `env(...,0px)` fallback, scope 44px to desktop tabs optional `min-h-[44px]` at md, replace `transition-all` with `transition-colors`, add `prefers-reduced-motion`, tokenize `text-[10px]` → `text-caption`, optional `role="tablist"` + fade cue + 320 screenshot in M4.

