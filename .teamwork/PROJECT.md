# PROJECT.md — CareCanvas Intelligence Gap — Generic Configurable AI + WebMCP 40 Preserve (teamwork-1788097222690)

Synthesized: 2026-08-30T19:20Z from dispatcher-intelligence-gap-2026-08-30.md 129 lines + dispatcher-configurable-provider-delta-2026-08-30.md 71 lines + spec-miner-structure-engine 400 lines + spec-miner-tests-verification + spec-miner-patterns-config via glob/grep/read/webfetch live verification; webfetch https://opencode.ai/docs/go verified Go endpoints as generic examples + https://webmachinelearning.github.io/webmcp/ spec §4.1-4.5 + .env.example:14-51 13 VITE_AI_* keys as configurable examples
Integrity: demo (reproducible, verifiable, not mocked — Q7 confirmed demo) — protocol toolchange/isSecureContext/Permissions-Policy real not mocked, LLM path mock-network allowed in CI but schema/vision shape real, no hardcoded provider literals in src/ (Q13 hardcode ban default)
Working Dir: /Users/sujal/Projects/proj1/.teamwork (state.json metadata.artifactsDir=".teamwork", Q1 .teamwork current project confirmed)
Pattern: distributed-coding DAG worktrees gates — M0 Planning BLOCKING → M1 Intelligence Core → M2 Propagation → M3 Hardening & Verification → Success Auditor; spawn budget hint 16 total, dead-man 600s armed 19:20Z reset after each milestone PASS, at 15/16 proactive succession via BRIEFING dump + handoff/succession timestamp
Model: inherited-from-chat per Sentinel Q1-Q13 — all roles inherit opencode chat model + variant/reasoning effort (fallback opencode/muse-spark-1.2-contributor-free only if chat no selection); state.json request.modelOverrides none so omit model param per orchestration skill; documented in BRIEFING "model: inherited-from-chat"

## Repo Layout (ground truth, file:line citations — synthesized from spec-miner-structure-engine)

- Root: /Users/sujal/Projects/proj1 — Vite 6 + React 18 + Tailwind 3.x (package.json:1-44 type:module scripts dev/build/test/lint), vite.config.ts:1-36 with test.environment jsdom + setupFiles test/setup.ts + include 10 files test/unit/**/*.test.ts + test/integration + test/tier3-integration, build 1660 modules prior (success auditor vite build 793kB JS + 72kB CSS at 1664), tests 172 vitest + 231 runner PASS pre-gap at .teamwork/state.json M1-M3 PASS with repair validation-order 40→39 fix
- Engine singleton: `src/core/webmcp/WebMCPEngine.ts:24-820` class WebMCPEngine + export webMCPEngine = new WebMCPEngine():820 constructed at import side-effect; calls detectAndPolyfill():38-50 immediately. Current detect: docContext globalThis.document?.modelContext:44 + isNative registerTool present else polyfill:52-289. Polyfill EventTarget shim toolchangeTarget:62-93 dispatchToolchange():96-108 firing new Event('toolchange'):98 installs onto document:288. Implements Promise registerTool:130-145 validateToolName regex ^[a-zA-Z0-9_.-]+$:11 max128 TOOL_NAME_MAX:12 + validateToolDescription:132 non-empty + serializeInputSchema:142 via JSON.stringify TypeError on circular, dedup via abortControllers Map:29 + specRegistry Map:27. Branch native vs polyfill 387-442 delegating document.modelContext.registerTool(specTool,{signal}):400 Promise catch InvalidStateError already registered 402-409. Emits tool_registered:419 + toolchange:424. execute:547-728 validates validateSchema:593 validateParams:610 derives patientId/activeProfile from localStorage carecanvas_active_user:568-581 same as WebMCPAdapter:68-84 (no hardcoded Shanti 566), approvalGate:643-672, telemetry invocationHistory:30. Preserves WebMCP 40 per src/tools/index.ts:69-123
- Types: `src/types/webmcp.ts:7-198` WebMCPToolDefinition internal name, description, moduleOwner, requiresHumanApproval:41, parameters:43 with inputSchema? alias:48 optional, ModelContextTool:59-74 spec name 1-128 regex, RegisteredTool:93-107 inputSchema stringified, ModelContext:110-118 Promise registerTool/getTools/executeTool + ontoolchange, WebMCPExecutionContext:120-133 patientId/activeProfile/vault/eventBus
- Catalog: `src/tools/index.ts:69-123` allWebMCPTools 40 breakdown vault 3 (extract_fact, confirm_fact, compile_health_record) + labstory 2 (extract_labs, correlate_meds) + pillmap 8 (add_medication, check_interactions, check_diet_interactions, check_duplicate_ingredient, suggest_schedule, simulate_adherence, export_for_pharmacist, set_reminder) + rxbridge 5 + homelab 5 + safety 9 + carecircle/dossier 8, at 127-136 registerAllWebMCPTools(engine=webMCPEngine) loops engine.register(tool):134 sync void — async variant 181-198 Promise.allSettled per-tool with AbortController per src/main.tsx:136-192
- Bootstrap: `src/main.tsx:14` bootstrapAbortControllers Map per-tool dedup, 92-98 isSecureContext guard warn WebMCP requires SecureContext, 102-118 permissionsPolicy.allowsFeature('tools') guard try/catch NotAllowedError/SecurityError/InvalidStateError graceful fallback never crash, 136-192 Promise.allSettled per-tool with signal controller.signal per §4.2.3 native path document.modelContext.registerTool(specTool,{signal}):150 fallback to webMCPEngine.register:178 counts fulfilled/rejected logged, 194-223 toolchange shim void 'toolchange' verifier
- Vault: `src/core/vault/LocalVault.ts:25-718` 14 maps facts:26 documents:27 meds:28 labs:29 allergies:30 conditions:31 proposals:32 calendarEvents:33 careCircle:34 doctorGrants:35 auditLog:36 questionBank:37 dueCards:38 dangerReports:39, init():47 no-op, syncFireAndForget:69-111 Supabase isSupabaseEnabled non-blocking, typed helpers emitMedicationAdded:116 etc, logAudit:160-202 patientId inference 168-183, validateBoundingBox:212-220 pageIndex int >=0 x/y 0-1000, addFact:223-238 validates bbox 224-225 duplicate 227 emits fact_added:234, updateFactStatus:256-276 emits fact_status_changed:274, addMedication:302-310 emits medication_added:307 addLab:350-358 lab_added:355, wireLocalVaultToEventBus:715 singleton localVault:712 patient isolation via patientId scoping getters 244-637
- Knowledge: `src/core/knowledge/interactionEngine.ts:26-404` resolveGenericName:30-45 mockBrandGenericCatalog:7 checkDrugInteractions:50-88 mockDrugDrugInteractions:65 checkDiet:93-195 etc — hardcoded fixture gap to be replaced; `src/core/knowledge/reconciliationEngine.ts:23-560` reconcileThreeLists:27-155 determineStatusBadge:160-191 generatePlainLanguageExplanation:196-252 Lisinopril 210 Apixaban 222 generateDoctorQuestions:257-314 hardcoded 283-297 lisinopril branch
- Fixtures: `src/fixtures/drug_knowledge.ts` mockBrandGenericCatalog mockDrugDrugInteractions mockDrugDietInteractions etc, `src/fixtures/documents.ts:1-18` mock-removed exports only __fixtureClean_documents, longitudinal_labs etc retain as fallback when AI disabled (Q10)
- UI shells: `src/components/vault/DocumentDropzone.tsx:26-103` handleRealExtract doc_${Date.now()} 29 FileReader readAsText/DataURL 96-102 execute extract_fact 41 text-only no vision gap; `src/components/vault/FactApprovalCard.tsx:12-196` confirm_fact approve/edit/reject 20-61; `src/components/vault/FactStreamView.tsx:13-45` effectivePatientId 14-23 localStorage vault filter fact_extracted/fact_confirmed 39-40; `src/components/homelab/UploadLabModal.tsx:29-351` editForm creatinine1.90 egfr28 potassium4.8 42-46 mock_photo_slip_blob_base64 58 handleSimulateUpload preset 50-68 handleApproveAndCommit synthetic doc_homelab_slip_002 103; `src/components/pillmap/PillMapView.tsx:180-197` listens medication_added/updated/proposal_status_changed only relevant; `src/components/labstory/LabStoryView.tsx:74-97` listens lab_added/fact_confirmed/medication_updated; `src/components/rxbridge/RxBridgeView.tsx` etc; `src/components/safety/SafetyView.tsx` etc; `src/components/carecircle/CareCircleView.tsx` etc; `src/components/dossier/DossierView.tsx` view_timeline compile_health_record; each reads localVault and eventBus relevance matrix at src/core/events/eventBus.ts:54-299 typed EventName 64-98 alias groups 133-140 emit 169-195 highlightSourceDocument 266-272
- Settings green field (confirmed via glob/grep): `glob src/components/**Settings* →0` `glob src/**/*[Ss]ettings* →0` `glob src/components/settings/** →0` `grep VITE_AI|settings|Settings src/` only src/index.css:19 font-feature-settings + src/components/carecircle/CareCircleView.tsx:12 Settings icon import + src/components/dossier/DossierView.tsx:302 Share Settings label — no dedicated Settings page/component exists yet — green field for new `src/components/settings/SettingsView.tsx` + `src/core/settings/SettingsStore.ts` persisting VITE_AI_* to localStorage with Settings>env precedence per Q9; `grep localStorage|store|useStore|zustand src/` hits LocalVault carecanvas_active_user fhirExporter but no VITE_AI store — green field; `grep VITE_AI src/` 0 hits outside .env.example vs 81 total repo vs 14 in .env.example — proves no AI code yet
- Tests: `test/unit/WebMCPEngine.test.ts:6-44` 40 tools via engine direct + probes 146-230 native Promise 40 inputSchema origin window toolchange DOMString probe-patient-001, `vite.config.ts:34` include tier3-integration for M4 cohesion 28+8 runner 231 vs vitest 172 divergence, `test/test-runner.ts:39-132` 15 suites Tier1 40×5=200 Tier2 T2-01..12 Tier3 INT-01..12 Tier4 workloads E2E Flows A-E, `test/setup.ts:6-19` guarded polyfill + clearAll isolation
- Intelligence new (to be created, owned by plan DAG): `src/core/ai/**` generic client wrapper reading import.meta.env.VITE_AI_* OR SettingsStore with Settings>env precedence, handling chat → {base}/chat/completions vs responses → {base}/responses generically via VITE_AI_PROVIDER, image_url/input_image single multimodal request where model supports it, response_format json_object / text.format json_schema generically, Zod validation, fallback heuristic only when VITE_AI_ENABLED=false or key absent (Q10 rule for text never for images), vision+text payload shape real even when mock-network in CI; `src/core/settings/**` SettingsStore + `src/components/settings/**` SettingsView UI; `src/types/vault.ts:18-43` Fact bbox confidence plainExplanation etc supports structured outputs
- Config: `.env.example:14-51` VITE_AI_ENABLED false:14 PROVIDER chat|responses:22 BASE_URL https://opencode.ai/zen/go/v1:28 API_KEY placeholder:29 MODEL deepseek-v4-flash-vision-exp:33 VISION_MODEL:34 STRUCTURED true:41 ORG_ID etc 13 keys — examples only any baseURL/model allowed per feedback — never commit .env (.gitignore:4 .env 5 .env.* 12 worktrees/handoff 14 **/*.log); `opencode.json:35-68` still only commandcode provider baseURL https://api.commandcode.ai/provider/v1:41 models 3 zai-org/GLM-5.2 deepseek moonshotai Kimi-K2.7 code:43-47 — configurable Go wiring if team opts with configurable baseURL not forced literal
- Stack Vite 6 React 18 Tailwind 3.x, build 1660 modules prior (1664 at build.log), lint tsc --noEmit 0, WebMCP 40 intact via getTools, toolchange shim, SecureContext, Permissions-Policy, cross-origin same-origin default

## Intelligence Gap Addendum — Generic Configurable AI Mandate (Q1-Q13 resolutions)

- **Generic configurable provider — no hardcoding (Q13 hardcode ban default):** Prior draft hard-coded deepseek-v4-flash-vision-exp via POST https://opencode.ai/zen/go/v1/chat/completions and muse-spark-1.2-contributor via .../responses now removed as requirement. Any OpenAI-compatible provider, any model, any baseURL allowed. .env.example 13 keys are configurable examples only. Implementation must read runtime config generically — import.meta.env.VITE_AI_* (Vite prefix) and/or Settings store localStorage — never hardcoded literals for provider/model/baseURL/endpoint fixed in src/. Verifiable via grep gates after implementation: grep -R deepseek-v4-flash-vision-exp src/ 0 PASS log no-hardcode-provider.log; grep -R muse-spark-1.2-contributor src/ 0 PASS; grep -R opencode.ai/zen/go/v1 src/ 0 PASS; grep -R opencode.ai/zen src/ 0 PASS; grep -R import.meta.env.*VITE_AI|VITE_AI.*localStorage|settings.*VITE_AI|useSettings|SettingsStore src/ >=1 PASS log configurable-read.log; grep -R vision|image_url|input_image src/ >=1 PASS log vision-multimodal.log single request contains image data URL + text; grep -R json_schema|json_object|response_format|text.format|structured src/ >=1 PASS log structured-generic.log generic structured mode not tied to single provider field
- **Settings page vs .env precedence (Q9 Settings>env):** BaseURL/model/provider can be configured via either Settings page OR .env VITE_AI_* — proposed Settings store (localStorage/settings) overrides import.meta.env if present else env. Settings page lives at src/components/settings/SettingsView.tsx with VITE_AI_* fields persisting to localStorage/store plus src/core/settings/SettingsStore.ts reading import.meta.env fallback. After implementation glob src/components/**Settings* >=1 PASS + grep localStorage.*VITE_AI|settings.*store src/ >=1 PASS log settings-config.log. Until then .env.example VITE_AI_* is only documented source — green field confirmed.
- **Vision+text multimodal single response (Q10 whatever fallback):** User requires Use images along with text, most models allow it in single response, if thats required. Hardcoded separate OCR path (homeLabTools.ts:54 isBase64Image ? textToParse='' skipping vision) must be replaced by AI multimodal. All image-based extraction/OCR must use AI — never rule heuristics for images (no split rawText for data URLs, no placeholder Creat1.0 for base64 images). Text PDFs may be sent as document text or rendered to image for vision if provider supports image+text single response. Verified via grep -R isBase64Image.*textToParse.*'' src/tools/homeLabTools.ts 0 after + image_url/input_image >=1 + captured network probe single request image+text log vision-multimodal.log
- **Every hardcoded branch → AI intelligence (Q12 golden set chosen by planner):** All 7 gaps enumerated at research/dispatcher-intelligence-gap-2026-08-30.md:3 must be AI — not just vaultTools:
  - src/tools/vaultTools.ts:47 split lines + fixed bbox x:0.08 y:0.12 → AI grounded bbox + typed categories med/lab/allergy/condition via structured JSON file:line:47,64,80
  - src/tools/labStoryTools.ts:321 regex 5 markers → AI panel extraction drawDate/marker normalization file:line:321-346 + 508 template eGFR ${direction} → AI causal narrative
  - src/tools/homeLabTools.ts:54 base64 skip + 116 placeholder Creat1.0 eGFR75 → AI vision extraction file:line:54-92,116-149
  - src/components/homelab/UploadLabModal.tsx:42 editForm defaults 1.90/28/4.8 + 54 mock blob → AI-extracted values file:line:42-46,54-68 + synthetic commit removed
  - src/core/knowledge/interactionEngine.ts:6 fixture mockDrugDrugInteractions → AI reasoning interactions/diet/duplicate file:line:6-11
  - src/tools/rxBridgeTools.ts:283 4 if ctx.includes lisinopril branches → AI questions from discharge context file:line:283-297
  - src/tools/safetyTools.ts:37 tag/severity rule + photo not analyzed → AI triage vision+text file:line:37,45-51
  Fan-out after confirm_fact remains but intelligence is AI: vault addFact → medication_added/lab_added → pillmap AI interactions, labStory AI correlate_meds, rxBridge AI suggest_question, homeLab dueCards AI, safety triage AI, careCircle/dossier AI citations. LocalVault 11 stores + eventBus relevance matrix preserved, but upstream triggers intelligent cross-fill. Verified via no-hardcode-branches.log all counts 0 when AI enabled.
- **Cross-field propagation:** Uploaded document facts staged unconfirmed → after confirm_fact approve/edit per fact via human-in-the-loop (inline_fact/batch per Q5) then intelligently update all relevant downstream fields without manual re-entry: Vault approval gate per-fact, PillMap 7×4 canvas interactions via AI reasoning, LabStory normalized BIOMARKER_STANDARDS ±10% borderline with AI correlate_meds trajectory, RxBridge explain_med_change flag_interaction/suggest_question via AI, HomeLab dueCards/proposals linked via AI vision, Safety danger triage via AI vision/text, CareCircle/Dossier timeline citations with real bbox + questionBank enriched without duplicate spam via AI. Propagation respects patient isolation localStorage carecanvas_active_user only never '' nor patient-s-devi leak, approval semantics only confirmed propagate (unconfirmed staged, rejected never). Cross-field visible via eventBus subscriptions no reload, WebMCP tool results DOMString JSON.
- **Working directory Q1:** .teamwork in current project /Users/sujal/Projects/proj1/.teamwork canonical per state.json metadata.artifactsDir .teamwork NOT isolated ~/teamwork_projects/{PROJECT_NAME}/.teamwork — pattern distributed-coding explicit
- **Integrity demo Q7/Q11:** reproducible verifiable not mocked — protocol real not mocked, LLM mock-network allowed in CI but schema/vision shape real, client-exposed accept per Q11 (key in bundle exposable for demo, judges paste own key per .env.example:29 comment)
- **Configurable wiring Q9/Q13:** .env.example example keys, .gitignore .env, opencode.json configurable baseURL not forced, lint0 build1660 test172 runner231 getTools40 remain PASS gated behind VITE_AI_ENABLED/Settings equivalent, fallback heuristic only when disabled (rule for text never for images per Q10)
- **Planning blocked M0 Q4:** prompt_draft sections + file:line >0 + hardcode/configurable/VITE_AI counts >0, research deltas exist 3+ files, git diff only .teamwork in M0, stat mtime ordering plan before src edits — planner chooses golden set per Q12

## Native Spec Expectations (webfetch-verified 2026-08-30, URLs per prompt.md)

- Source URLs: https://github.com/webmachinelearning/webmcp (README) + https://webmachinelearning.github.io/webmcp/ (normative spec 26 Aug 2026 §4.1-4.5) + https://github.com/webmachinelearning/webmcp/blob/main/implementation-status.md (Chrome 149 Origin Trial live, Edge 150, Brave Leo) + https://developer.chrome.com/blog/ai-webmcp-origin-trial + https://opencode.ai/docs/go (as generic OpenAI-compatible example, not forced)
- §4.1 IDL partial interface Document { [SecureContext, SameObject] readonly attribute ModelContext modelContext; } canonical ONLY document.modelContext SecureContext required
- §4.2 ModelContext [Exposed=Window, SecureContext] interface ModelContext : EventTarget { Promise<undefined> registerTool(ModelContextTool tool, optional ModelContextRegisterToolOptions options={}); Promise<sequence<RegisteredTool>> getTools(optional ModelContextGetToolOptions options={}); Promise<DOMString> executeTool(RegisteredTool tool, optional object inputObject={}, optional ModelContextExecuteToolOptions options={}); attribute EventHandler ontoolchange; } all Promise-based
- §4.2.1 Tool dict name 1-128 chars ^[a-zA-Z0-9_.-]+$ else InvalidStateError description non-empty else InvalidStateError inputSchema object stringified via JSON.stringify else TypeError optional title annotations:{readOnlyHint,untrustedContentHint} execute:(inputObject,{signal})=>Promise<any>
- §4.2.3-4.2.6 Options signal exposedTo:string[] fromOrigins RegisteredTool {name,description,inputSchema(string),title,annotations,origin,window} first arg to executeTool is RegisteredTool object not string
- §4.4 Events toolchange EventTarget on ModelContext fired via notify documents of a tool change (respects allowed to use tools + exposedTo origin matching)
- §4.5 SecureContext+Permissions-Policy tools policy disables → NotAllowedError; origin-keyed → SecurityError; not fully active → InvalidStateError; not https/localhost → SecurityError
- Origin exposure same-origin + built-in default; cross-origin iframe requires registerTool({exposedTo:["https://trusted.example"]}) + iframe allow="tools" + getTools({fromOrigins:[location.origin]})
- Implementation-status Chrome 149 Origin Trial live (flag chrome://flags/#enable-webmcp-testing for localhost, Origin Trial for HTTPS) — reviewers testing on Chrome 149+ over localhost:5173 SecureContext will have native if flag/token present else polyfill required
- Go docs as generic example: opencode-go/<model-id> format, endpoints .../chat/completions vs .../responses are examples of OpenAI-compatible mapping — now generic any baseURL/model allowed

## Findings — 7 Gaps + 3 New Nuances (all file:line, re-verified via spec miners)

1. Register never calls native if not branching on isNative to document.modelContext.registerTool — prior M1-M3 fix now delegates via WebMCPEngine 387-442 guard — must preserve in intelligence layer
2. Wrong globals + polyfill shadowing fixed via guard if (document.modelContext?.registerTool) never overwrite — keep
3. Shape mismatched parameters vs inputSchema bridged via adapter toSpecTool stringified — keep
4. Discovery/execution shape mismatched sync vs Promise fixed via polyfill Promise shapes — keep
5. Missing exposure hook toolchange/SecureContext fixed via src/main.tsx — keep
6. Timing & singleton constructor detectAndPolyfill at import before main.tsx register + HMR dedup via AbortController — keep
7. Schema bridging additionalProperties fixed — keep
8. (Intelligence) Extraction heuristic hardcode — vaultTools 47 split+64 bbox 0.08, labStory 321 regex 5, homeLab 54 skip+116 placeholder, UploadLabModal 42 defaults+58 mock, interactionEngine 6 fixtures, rxBridge 283 templates, safety 37 rules — all 7 gaps must become AI intelligence via src/core/ai/* generic client (vision+text single response, structured generic) + fallback heuristic only when disabled (Q10)
9. (Intelligence) Propagation missing — LocalVault addFact only emits fact_added:234 no orchestrated fan-out to pillmap/labStory/rxBridge/homeLab/safety/careCircle/dossier/questionBank — will be augmented via AI orchestration respecting approval/patient isolation
10. (Intelligence) Config green field — no Settings component, no AI wiring in src/ (grep VITE_AI 0 in src/ vs 13 in .env.example 81 total, hardcode 100 configurable 94), opencode.json still commandcode only 1 provider 3 models — will add src/core/ai + Settings wiring generically without hardcoded literals

## Modules & Ownership Map (ground truth for workstreams, no overlap within parallel batch — verified via detectConflicts)

| Module | Files (glob) | Owner proposal | Risk |
|--------|--------------|----------------|------|
| engine core + types + adapter | src/core/webmcp/WebMCPEngine.ts, src/types/webmcp.ts, src/core/webmcp/WebMCPAdapter.ts | worker_engine_core (prior PASS — preserved, not re-owned) | Must keep Promise shapes, toolchange, SecureContext, patientId bridging — no regression |
| vault source of truth + events | src/core/vault/LocalVault.ts, src/core/events/eventBus.ts, src/core/supabase/** | worker_fanout (M2) | Must keep 14 stores patientId isolation, bbox validation, auditLog, typed helpers; add cross-field AI orchestration but keep relevance matrix; verify patient isolation ''0 / test-patient>0 / patient-s-devi 0 |
| knowledge reasoning | src/core/knowledge/interactionEngine.ts, src/core/knowledge/reconciliationEngine.ts, src/fixtures/drug_knowledge.ts | worker_knowledge (M2) | Replace fixture lookups with AI reasoning primary when AI enabled, fallback fixture when disabled per Q10 (rule for text never for images); never expose hardcoded mockBrandGenericCatalog as primary when enabled |
| catalog bridging 40 | src/tools/index.ts | worker_engine_core preserved | Keep 40 tools snake_case Q5 parameters→inputSchema alias intact; no rename |
| extraction tools + heuristics to AI | src/tools/vaultTools.ts, src/tools/labStoryTools.ts, src/tools/homeLabTools.ts | worker_extraction (M1) | Replace all heuristics with generic AI client vision+text single response + structured generic + grounded bbox not 0.08; fallback text heuristic only when disabled (image never heuristic); ensure configurable-read >=1 vision>=1 structured>=1 no-hardcode 0 |
| pillmap/rxbridge/safety tools to AI | src/tools/pillMapTools.ts, src/tools/rxBridgeTools.ts, src/tools/safetyTools.ts, src/tools/careCircleTools.ts | worker_knowledge (M2) | Replace templates/fixtures with AI reasoning via AI client; keep tool contracts Promise DOMString JSON with humanApprovalRequired |
| vault UI extraction | src/components/vault/DocumentDropzone.tsx, src/components/vault/FactApprovalCard.tsx, src/components/vault/FactStreamView.tsx, src/components/vault/BoundingBoxViewer.tsx | worker_extraction (M1) | Wire FileReader real file to AI vision multipart single call, not readAsText garbled for images; preserve approval per-fact human signature, boundingBox grounding |
| homelab UI vision | src/components/homelab/UploadLabModal.tsx, src/components/homelab/HomeLabView.tsx, src/components/homelab/DueCardList.tsx, src/components/homelab/ProposalCard.tsx, src/components/homelab/DoctorInbox.tsx | worker_extraction (M1 homelab part) + worker_fanout (M2 dueCards/proposals) | Replace mock 1.90/28/4.8 defaults with AI extracted values; dueCards/proposals AI inferred via fanout |
| pillmap/labstory/rxbridge/safety/carecircle/dossier UI | src/components/pillmap/** (9 files), src/components/labstory/** (4), src/components/rxbridge/** (5), src/components/safety/** (4), src/components/carecircle/** (5), src/components/dossier/** (6) | worker_fanout (M2) | Each recomputes via AI-enhanced reasoning over confirmed facts; relevant-only subscriptions remain; no full-page reload; citations include real bbox |
| AI core client | src/core/ai/** (new: client.ts, vision.ts, structured.ts, config.ts, fallback.ts) | worker_ai_client (M1) | Generic client must read import.meta.env.VITE_AI_* OR SettingsStore with Settings>env precedence, handle chat vs responses generically composing {baseURL}/chat/completions or /responses, image_url vs input_image generically, response_format json_object vs text.format json_schema generically, Zod validation, timeout 30000, never hardcoded literals; ensure grep hardcode 0 after |
| settings store + UI | src/core/settings/** (new: SettingsStore.ts), src/components/settings/** (new: SettingsView.tsx, SettingsForm.tsx) | worker_settings (M3) | Persist VITE_AI_* to localStorage/settings store, Settings>env precedence, validate baseURL/model not empty, not committing .env, .gitignore .env PASS; show 13 keys configurable |
| app shell | src/App.tsx, src/main.tsx | worker_settings (M3) or worker_fanout (M2) — single owner to avoid overlap — assigned to worker_settings in M3 | Must keep localStorage carecanvas_active_user restore, route Settings page, bootstrap SecureContext+PermissionsPolicy+allSettled+AbortController preserved |
| verification harness | test/unit/**, test/integration/**, test/tier3-integration/**, test/harness/**, test/fixtures/**, vite.config.ts, package.json, .teamwork/verification/**, .teamwork/snapshots/** | worker_verification (M3) | Keep 172 vitest PASS + 231 runner PASS + build 1660 + lint0 + 40 getTools probe + toolchange + patient isolation probes; add intelligence gates env-config, no-hardcode-provider, configurable-read, vision-multimodal, structured-generic, no-hardcode-bbox/ocr/branches, cross-field, settings-config, planning-gate; snapshot 6 viewports 1280/375/768 etc JFIF>5K |
| docs infra | .env.example:14-51, opencode.json:35-68, .gitignore:4-14 | worker_verification (M3) docs | Verify .env.example example keys >=4, .gitignore .env, opencode.json configurable baseURL not forced, never literal key in src/ |
| teamwork orchestration | .teamwork/** (plan.md, progress.md, state.json, BRIEFING.md, GATE_STATUS.md, research/*, handoff/*) | orchestrator dispatch-only | Maintain DAG, ownership, heartbeats, dead-man, succession |

**Overlap check:** M1 batch worker_ai_client (src/core/ai/**) vs worker_extraction (src/tools/vault+labStory+homeLab + src/components/vault/homelab + src/types) DISJOINT — PASS (detectConflicts 0). M2 batch worker_knowledge (src/core/knowledge + src/tools/pillMap+rxBridge+safety+careCircle) vs worker_fanout (src/core/vault+events + src/components/pillmap+labstory+rxbridge+safety+carecircle+dossier + src/fixtures) DISJOINT — PASS (src/core/knowledge vs src/core/vault+events distinct subdirs, src/tools vs src/components distinct). M3 batch worker_settings (src/components/settings + src/core/settings + src/App.tsx + src/main.tsx) vs worker_verification (test/* + vite.config.ts + verification/snapshots + .env.example/opencode.json) DISJOINT — PASS. All batches respect 16 spawn budget (3 miners + 3 gates M0=6 + M1 2 workers + M1 gate 3=11 + M2 2 workers + M2 gate 3=16 at limit → proactive succession before M3 or budget reset after handoff if needed). Verified via ownership.ts#detectConflict before each batch; if conflict serialize or repartition.

## Risks & Mitigations

- Hardcode literal leak — mitigate never write deepseek/muse/zen literals in src/; client must compose {baseURL}/chat/completions generically via VITE_AI_PROVIDER, read via import.meta.env OR SettingsStore; verify grep gates 0 after
- Vision shape divergence — mitigate handle both image_url (chat) and input_image (responses) generically based on provider, send image+text single response where model supports it, timeout 30000, Zod schema real
- Patient isolation regression — mitigate keep LocalVault patientId scoping in every getter, derive from localStorage carecanvas_active_user same as WebMCPAdapter:68-84, never '' nor patient-s-devi; verify via isolation probe ''0 >0 ''0
- Approval semantics — mitigate only confirmed propagate, unconfirmed staged, rejected never, via updateFactStatus→emit fact_status_changed→fanout guarded
- Build/tests regression — mitigate keep 40 tools intact snake_case, lint0 test172 runner231 build1660; gate behind VITE_AI_ENABLED/Settings equivalent fallback heuristic for text (never for images Q10)
- Cross-field spam — mitigate questionBank dedup via AI without duplicate, dueCards idempotent via linkedDueCardId, auditLog preserved
- HMR duplicate InvalidStateError — mitigate per-tool AbortController Map same as prior fix
- 6-viewport gaps — mitigate workers capture desktop1280 mobile375 tablet768 plus 320/1024/1440 via browser.capture, auditor re-captures, JFIF>5K wc -c
- Spawn budget 16 — mitigate at 15/16 proactive dump BRIEFING + handoff/succession timestamp + invoke successor team-orchestrator wait:false
- Dead-man 600s — mitigate arm at start + reset after each milestone PASS, heartbeat via progress/state/BRIEFING/GATE_STATUS mtime relay 8m/10m, poll isCancelled before each batch/gate

## Decomposition Guidance (for plan.md DAG)

- M0 Planning BLOCKING → 0 workers (orchestrator synthesis) → gate critic→challenger→auditor verifies R4 artifacts existence, file:line>0 hardcode/configurable/VITE_AI counts>0 git diff only .teamwork stat mtime ordering plan before src edits
- M1 Intelligence Core (R1 extraction, generic configurable) → 2 parallel workers disjoint ai_client vs extraction disjoint — DAG no dependsOn after M0, parallel batch
- M2 Propagation (R2 every branch replaced + cross-field) → 2 parallel workers disjoint knowledge vs fanout dependsOn M1 → batch 2 parallel then chain
- M3 Hardening & Verification (R1+R2+R3 + no regression + no hardcoding) → 2 parallel workers disjoint settings vs verification dependsOn M2 → batch 3 parallel then Success Auditor final independent lint/test/build/grep + live native probe + 6-viewport audit + no-hardcode gates + cross-field + patient isolation + approval before Done
- Gates: critic→challenger→auditor per milestone (adaptive N+M+1 batched per pattern demo: single parallel call with scoped prompts to stay <16 budget), re-capture 1280/375/768 each, Success Auditor final independent probe 40 + inputSchema 40 toolchange 44 patientId probe-patient-001 grep gates + snapshots before Done

## Counts Verification (mechanical)

- grep -R VITE_AI .env.example 13 keys >=4 PASS, src/ 0→>=1 after M1, total 81 hits in .teamwork docs
- grep -R hardcode .teamwork/prompt*.md >0 PASS (~100 matches includes hardcode term)
- grep -R configurable .teamwork/prompt*.md >0 PASS (~94 matches)
- grep -R VITE_AI .teamwork/prompt*.md >0 PASS (>0 per prompt_draft)
- grep -R file:line research/dispatcher-*.md >0 PASS (dispatcher-intelligence-gap 21k contains file:line citations 7 gaps)
- grep -R deepseek|muse-spark|opencode.ai/zen src/ 0 PASS after (today 0 in src already)
- grep -R import.meta.env.*VITE_AI src/ >=1 after confirms generic read (today 0 green field)
- grep -R image_url|input_image src/ >=1 after confirms vision multimodal
- grep -R boundingBox.*0\.08 src/tools/vaultTools.ts 2 today →0 after
- grep -R mock_photo src/ 1 today →0 after
- grep -R 1\.90.*28.*4\.8 src/components/homelab/UploadLabModal.tsx 4 today →0 after
- grep -R lisi.*nopr src/tools/rxBridgeTools.ts 1 today →0 after when AI enabled
- grep -R mockDrugDrugInteractions src/core/knowledge 2 today →AI path must exist >=1
