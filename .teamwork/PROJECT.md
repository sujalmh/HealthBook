# PROJECT.md — CareCanvas WebMCP Hackathon — Protocol-Correct per W3C Spec Draft 26 Aug 2026 (teamwork-1788075497934)

Synthesized: 2026-08-30T13:30Z from dispatcher-webmcp-engine-protocol.md 142 lines + dispatcher-webmcp-hackathon-verification-2026-08-30.md delta 24136 bytes (live webfetch re-verified spec §4.1-4.5) + direct read of WebMCPEngine.ts 433 lines, webmcp.ts 125 lines, tools/index.ts 144 lines 40 tools, main.tsx 92 lines, WebMCPInspector.tsx 654 lines, ConnectWebMCPModal.tsx 292 lines, vite.config.ts, test harness 231 runner
Integrity: demo (reproducible, verifiable, not mocked) — Q7 confirmed demo
Working Dir: /Users/sujal/Projects/proj1/.teamwork (state.json metadata.artifactsDir=".teamwork", Q1 .teamwork current)
Pattern: distributed-coding DAG worktrees gates
Spawn budget hint: 16 total

## Repo Layout (ground truth, file:line citations)

- Root: /Users/sujal/Projects/proj1 — Vite 6 + React 18 + Tailwind 3.x (package.json:1-44), vite.config.ts:1-36 with test.environment jsdom + setupFiles test/setup.ts + include 10 files test/unit/**/*.test.ts + test/integration + test/tier3-integration, build 1660 modules prior (success auditor vite build 793kB JS + 72kB CSS)
- Engine singleton: `src/core/webmcp/WebMCPEngine.ts:14` class WebMCPEngine 433 lines, export `webMCPEngine = new WebMCPEngine():433` constructed at import side-effect; calls detectAndPolyfill():22-25 immediately. Current detect: docContext globalThis.document?.modelContext:29 + navContext globalThis.navigator?.modelContext:30 → isNative true if registerTool present else polyfill:37-39. Polyfill mockContext:43-57 with legacy shapes: registerTool(toolDef)=>register:45, unregisterTool:48, getRegisteredTools:51 sync, executeTool(name:string):54 → this.execute. installPolyfill at :59-64 writes globalThis.modelContext:60 + __CareCanvas_WebMCP__:61 + document.modelContext:63 WITHOUT guard → shadows native if present (bug)
- Types: `src/types/webmcp.ts:34-45` WebMCPToolDefinition with internal shape `name, description, moduleOwner, category, requiresHumanApproval:39, approvalGateType, parameters: WebMCPToolParameterSchema:5-19, returns, execute, uiSideEffects` — NOT spec shape. Spec expects `inputSchema, title, annotations:{readOnlyHint}, execute(inputObject,{signal})`. `WebMCPExecutionContext:47-60` with vault/eventBus/patientId/activeProfile/approvalInterceptor — spec expects only {signal}
- Catalog: `src/tools/index.ts:69-123` allWebMCPTools 40 breakdown vault 3 (extract_fact, confirm_fact, compile_health_record) + labstory 2 (extract_labs, correlate_meds) + pillmap 8 (add_medication, check_interactions, check_diet_interactions, check_duplicate_ingredient, suggest_schedule, simulate_adherence, export_for_pharmacist, set_reminder) + rxbridge 5 + homelab 5 + safety 9 + carecircle/dossier 8, at :127-136 registerAllWebMCPTools(engine=webMCPEngine) loops engine.register(tool):134 sync void — never awaits document.modelContext.registerTool Promise
- Bootstrap: `src/main.tsx:9-67` bootstrap():9 → await localVault.init():11 → hydrateFromSupabase best-effort :26-63 → registerAllWebMCPTools():66 before ReactDOM.createRoot:72 → fallback same :83 on catch; NO SecureContext guard, no Permissions-Policy check, no toolchange, no AbortController dedup, no Promise.allSettled per-tool, no isSecureContext guard (Q9+Q10 violation)
- UI surfaces: `src/components/common/WebMCPInspector.tsx:17-46` reads engine directly webMCPEngine.getRegisteredTools():46 — NOT native; label at :265-267 shows isNative vs Polyfill but count still from engine; listeners at :76-83 only eventBus tool_registered etc, NO native toolchange. `src/components/common/ConnectWebMCPModal.tsx:67` lists 4 globals window.modelContext,navigator.modelContext,document.modelContext,window.__CareCanvas_WebMCP__ and example at :68-81 uses window.modelContext.getRegisteredTools() sync + executeTool('extract_fact', {...}) string-based — diverges from spec Promise getTools() + executeTool(RegisteredTool object)
- Tests: `test/unit/WebMCPEngine.test.ts:13-35` 4 tests polyfill-only 40 tools via engine direct; vite 172 PASS + runner 231 PASS all via polyfill path (isNative false in jsdom), none exercising native branch. `vite.config.ts:34` include tier3-integration for M4 cohesion+supabase (28+8) so runner 231 vs vitest 172 divergence
- Other: `src/core/vault/LocalVault.ts` 719 lines 14 stores getSeedCounts etc — vault is source of truth for patientId bridging; `src/core/events/eventBus.ts` tool_registered etc; `src/App.tsx` 627 lines shell not WebMCP but restore localStorage carecanvas_active_user; `src/components/vault/DocumentDropzone.tsx` FileReader drop → webMCPEngine.execute('extract_fact', {documentId, rawText}) with correct patientId derivation at WebMCPEngine.ts:187-200 from localStorage

## Native Spec Expectations (webfetch-verified 2026-08-30, URLs per prompt.md)

- Source URLs: https://github.com/webmachinelearning/webmcp (README imperative example) + https://webmachinelearning.github.io/webmcp/ (normative spec 26 Aug 2026, 136 commits, §4.1-4.5) + https://github.com/webmachinelearning/webmcp/blob/main/implementation-status.md (Chrome 149 Origin Trial live, Edge 150, Brave Leo experimental, Firefox pending, Safari pending) + https://developer.chrome.com/blog/ai-webmcp-origin-trial
- §4.1 IDL: `partial interface Document { [SecureContext, SameObject] readonly attribute ModelContext modelContext; }` — canonical surface ONLY document.modelContext SecureContext required. https://webmachinelearning.github.io/webmcp/#document-extension — verified. Current code globalThis.document?.modelContext:29 + navContext:30 + globalThis.modelContext:60 are non-spec globals must be removed except test adapter
- §4.2 ModelContext: `[Exposed=Window, SecureContext] interface ModelContext : EventTarget { Promise<undefined> registerTool(ModelContextTool tool, optional ModelContextRegisterToolOptions options={}); Promise<sequence<RegisteredTool>> getTools(optional ModelContextGetToolOptions options={}); Promise<DOMString> executeTool(RegisteredTool tool, optional object inputObject={}, optional ModelContextExecuteToolOptions options={}); attribute EventHandler ontoolchange; }` — all Promise-based not sync void. https://webmachinelearning.github.io/webmcp/#model-context-container
- §4.2.1 Tool dict: `name` 1-128 chars ^[a-zA-Z0-9_.-]+$ else InvalidStateError, `description` non-empty else InvalidStateError, `inputSchema` object stringified via JSON.stringify else TypeError, optional `title`, `annotations:{readOnlyHint, untrustedContentHint}`, `execute:(inputObject, {signal})=>Promise<any>` . Current parameters:41 will be ignored by native — native would see empty inputSchema "" default. https://webmachinelearning.github.io/webmcp/#dom-modelcontexttool-inputschema
- §4.2.3-4.2.6 Options: signal, exposedTo:string[], fromOrigins, RegisteredTool {name,description,inputSchema(string),title,annotations,origin,window}. First arg to executeTool is RegisteredTool object not string. https://webmachinelearning.github.io/webmcp/#registered-tool
- §4.4 Events: `toolchange` EventTarget on ModelContext, fired via notify documents of a tool change (parallel webmcp task source, respects allowed to use tools + exposedTo origin matching). https://webmachinelearning.github.io/webmcp/#events — current codebase grep toolchange 0 in src/ (gap)
- §4.5 SecureContext+Permissions-Policy: tools policy disables → NotAllowedError; is origin-keyed check → SecurityError; not fully active → InvalidStateError; not https/localhost → SecurityError. https://webmachinelearning.github.io/webmcp/#permissions-policy — current main.tsx:66 unguarded will reject 40 times in insecure
- Origin exposure: same-origin + built-in browser agent default; cross-origin iframe requires registerTool({exposedTo:["https://trusted.example"]}) + iframe allow="tools" + getTools({fromOrigins:[location.origin]}) — grep exposedTo 0 currently
- Implementation-status: Chrome 149 Origin Trial live (flag chrome://flags/#enable-webmcp-testing for localhost, Origin Trial for HTTPS), Edge 150, Brave Leo, others pending — hackathon reviewers testing on Chrome 149+ over localhost:5173 SecureContext will have native if flag/token present, else polyfill required

## Findings — 7 Gaps + 3 New Nuances (all file:line, re-verified via delta)

1. Register never calls native: WebMCPEngine.ts:68-75 only registry.set + eventBus.emit, no branch on isNative to document.modelContext.registerTool — native agent getTools() returns 0
2. Wrong globals + polyfill shadowing: WebMCPEngine.ts:29-30 legacy navigator + polyfill :60-64 writing globalThis.modelContext + __CareCanvas_WebMCP__ + overwriting document.modelContext without saving original → shadows native until reload; ConnectWebMCPModal.tsx:67 4 wrong globals + :74,78 sync string API
3. Shape mismatched: types parameters vs inputSchema, execute(params,context vault/patientId) vs execute(inputObject,{signal}) — even if delegation added, vault context missing → patientId='' orphan facts :199 fallback
4. Discovery/execution shape mismatched: polyfill sync getRegisteredTools:51 + executeTool(name:string):54 vs spec Promise getTools():RegisteredTool[] + executeTool(toolObject):Promise<DOMString> — Inspector :46 bypasses native masking 0
5. Missing exposure hook: 0 exposedTo/fromOrigins/toolchange/SecureContext checks → iframe agents 0, built-in default misunderstood
6. Timing & singleton: constructor detectAndPolyfill:22-25 at import :433 before main.tsx:66 register; no re-detection after hydrate, no HMR dedup via AbortController signal → duplicate InvalidStateError on HMR
7. Schema bridging: pillMapTools etc JSON Schema structurally compatible but field name wrong + additionalProperties inconsistent → native form empty
8. (NEW delta) SecureContext/origin-keyed will reject in file:// or insecure http — bootstrap must guard isSecureContext + permissionsPolicy.allowsFeature
9. (NEW delta) Promise registerTool enables AbortSignal dedup/HMR — current sync void will double-register; need per-tool AbortController storage
10. (NEW delta) Execution return bridging DOMString — native returns stringified JSON, our WebMCPToolResult is object → must serialize via JSON.stringify and parse on executeTool return

## Modules & Ownership Map (ground truth for workstreams, no overlap within batch)

| Module | Files (glob) | Owner proposal | Risk |
|--------|--------------|----------------|------|
| engine core + types + adapter | src/core/webmcp/WebMCPEngine.ts, src/types/webmcp.ts, src/core/webmcp/WebMCPAdapter.ts (new if created) | worker_engine_core | Must map parameters→inputSchema stringified, name/description validation InvalidStateError, duplicate AbortController, isNative guard only document.modelContext, polyfill guard if (document.modelContext?.registerTool) never overwrite, Promise shims for jsdom only, toolchange EventTarget shim, patientId bridging via localStorage carecanvas_active_user, DOMString serialization, origin/window fields |
| catalog bridging | src/tools/index.ts, src/tools/vaultTools.ts, src/tools/labStoryTools.ts, src/tools/pillMapTools.ts, src/tools/rxBridgeTools.ts, src/tools/homeLabTools.ts, src/tools/safetyTools.ts, src/tools/careCircleTools.ts | worker_catalog_bridge | 40 tools snake_case keep Q5, parameters stays but adapter converts to inputSchema stringified + annotations readOnlyHint from requiresHumanApproval, no rename, approval gates keep pendingApprovalId Q6, verify 40 intact |
| bootstrap platform | src/main.tsx | worker_bootstrap_platform | Must do SecureContext guard (isSecureContext) + Permissions-Policy allowsFeature('tools') try/catch NotAllowedError/SecurityError graceful fallback never crash, async after localVault.init before mount, Promise.allSettled per-tool not Promise.all Q10, AbortController dedup for HMR, toolchange not needed here but register timing |
| UI inspector surfaces | src/components/common/WebMCPInspector.tsx, src/components/common/ConnectWebMCPModal.tsx | worker_ui_inspector | Must replace window.modelContext/navigator/legacy with document.modelContext only, examples use await document.modelContext.getTools() + executeTool(toolObject) spec-correct, Inspector refreshData tries native getTools Promise first then fallback to engine, label Native vs Polyfill accurate based on actual document.modelContext presence not stale isNative, 6-viewport no gaps 320/375/768/1024/1280/1440 |
| regression verification | test/unit/WebMCPEngine.test.ts, test/setup.ts, vite.config.ts, test/test-runner.ts, test/tier*/* | worker_regression_verify | Must keep 172 vitest PASS + 231 runner PASS, add native probe shim for jsdom fallback parity, no mockShanti, ensure lint 0 build 1660, Flows A-E, 6-viewport screenshots ≥2 per milestone desktop1280+mobile375+tablet768 under .teamwork/snapshots/webmcp-* JFIF >5K |

**Overlap check:** M1 batch worker_engine_core vs worker_catalog_bridge DISJOINT (src/core+types vs src/tools) — parallel safe, no file overlap. M2 single worker_bootstrap_platform owns only src/main.tsx — no parallel conflict. M3 batch worker_ui_inspector (src/components/common) vs worker_regression_verify (test/* + vite.config.ts) DISJOINT — parallel safe. Verified via detectConflicts before batch.

## Risks & Mitigations

- Polyfill shadowing native is critical hackathon bug — mitigate with guard `if (typeof document !== 'undefined' && (document as any).modelContext?.registerTool) { isNative=true; return; }` before installPolyfill, never write globalThis.modelContext except jsdom shim guarded by same check. Q2 shim only jsdom/tests.
- HMR duplicate InvalidStateError — mitigate per-tool AbortController storage Map<string,AbortController>, abort previous before re-register, catch InvalidStateError duplicate graceful skip. Q3 deduplication.
- isSecureContext alone insufficient — mitigate also check `document.permissionsPolicy?.allowsFeature('tools')` and try/catch NotAllowedError/SecurityError per Q4/Q10, fallback to polyfill never crash.
- parameters vs inputSchema field rename must not break 40 tools tests — keep internal `parameters` but adapter creates `inputSchema: parameters` when registering natively (stringified). Tests that assert parameters still pass; new probe asserts JSON.parse(inputSchema).
- patientId bridging orphan risk — engine execute wrapper must inject vault/eventBus/patientId derived from localStorage same as polyfill defaultContext:187-200, but native execute receives only {signal} so wrapper must capture outer localVault singleton and localStorage profile at call time, not rely on caller to supply. Verify not '' nor patient-s-devi.
- Promise.all vs allSettled — all rejects one failure → 0 tools if one invalid; allSettled ensures 39 succeed even if 1 invalid, Q10 requires allSettled.
- 40 tools must stay valid snake_case per Q5 — do not rename, just validate regex ^[a-zA-Z0-9_.-]{1,128}$ via grep gate.
- Cross-origin requires explicit exposedTo+allow — default same-origin+built-in only Q4, do not expose to chat.openai.com without list.
- Approval gates Q6 keep staged pendingApprovalId application-level not native elicitation — engine wrapper should still return pending result with humanApprovalRequired:true stringified, not throw.
- Screenshots must be ≥2 per milestone desktop1280+mobile375+tablet768 JFIF >5K under .teamwork/snapshots/webmcp-* — workers must use browser.capture with open http://localhost:5173 after dev server.
- Build 1660 modules prior — string replacements + new adapter file may delta ±10 acceptable but dist must remain valid, lint tsc --noEmit 0, tests 172+231 PASS.
- Prior hard-code cleanup still 0 — ensure ST. JUDE etc remains 0, workers must not reintroduce.

## Decomposition Guidance (for plan.md DAG)

- M1 Core Protocol Adapter & Dictionary (R2,R3 foundation) — 2 parallel workers disjoint engine/types+adapter vs catalog-bridge → DAG no dependsOn, parallel batch 1
- M2 Bootstrap Secure & Platform Events (R4 + R3 patientId) — 1 worker bootstrap_platform owns main.tsx dependsOn M1 → batch 2 serialized
- M3 UI Surfaces & Regression Verification (R1,R5 + R3 invoke + R4 toolchange verification + 6-viewport) — 2 parallel workers disjoint UI vs regression_verify dependsOn M2 → batch 3 parallel then chain to gates
- Gates: critic→challenger→auditor per milestone (adaptive N+M+1 batched per M3: single parallel call with scoped prompts to stay <16 budget), re-capture 1280/375/768 each, Success Auditor final independent lint/test/build/grep + live native probe + 6-viewport audit at 3 viewports (must show 40, inputSchema pass, toolchange >=1, grep gates, JFIF >5K)
