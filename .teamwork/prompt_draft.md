# Prompt Draft — CareCanvas Intelligence Gap — Awaiting Feedback

## Objective

> **Verbatim user goal (preserved):**
> ```
> So now the problem is, the website and the webmcp is complete, but the entire functionality is just simple function or hardcoded. What i mean to say is, if a user uploads a document, it wont be intelligently handled by the website to update other fields, because it lacks intelligence. Plan what can be done, this is just planning
>
> I've added .env file with llm config. add it. create a plan before contuining
> ```

> **Verbatim new feedback (amendment 2026-08-30 — preserved verbatim):**
> ```
> Dont hardcode this: chat → deepseek-v4-flash-vision-exp /chat/completions, responses → muse-spark-1.2-contributor /responses, it can be any models, dont hardcode: baseURL https://opencode.ai/zen/go/v1 it can be any base url. These will be configured using settings page, or the .env file. dont hardcode it.
> Replace all the hardcoded logic to ai based intelligence in the website. Use images along with text, most models allow it in single response, if thats rqeuired.
> All image based extraction or OCR, use AI.
> ```

> **Clarified scope synthesis (what to achieve, not how):**

CareCanvas has a protocol-correct WebMCP layer (40 tools via `document.modelContext` at `src/tools/index.ts:69-123`, `src/core/webmcp/WebMCPEngine.ts` M1-M3 PASS) but the *product intelligence* is simple/hardcoded: uploading a document (PDF or image slip via `src/components/vault/DocumentDropzone.tsx:26` / `src/components/homelab/UploadLabModal.tsx:50`) does **not** intelligently update downstream fields — vault facts, pillmap, labStory, rxBridge, homeLab, safety triage, careCircle/dossier/question bank. Extraction today is a heuristic (`src/tools/vaultTools.ts:47-64` split lines → up to 3 medication facts with fixed `boundingBox {x:0.08,y:0.12}`) and propagation stops after a single `addFact`/`addMedication`/`addLab`. Planning must define what **intelligent handling** must achieve using **generic configurable AI** — **any OpenAI-compatible provider, any model, any baseURL** — configured via **Settings page OR `.env` `VITE_AI_*`** (not hardcoded strings in code), so that a single document upload can be **vision+text multimodal in a single response where the model supports it** (images along with text), **all image-based extraction/OCR via AI not heuristics**, structured-extracted with grounded bounding boxes, and cross-field propagated — while preserving WebMCP 40, build/tests, and patient isolation. **This is planning only (M0 blocking) — no `src/` edits until user approves this draft.** A follow-up Orchestrator phase (M1+ implementation) will execute the approved plan. Every hardcoded branch enumerated in `research/dispatcher-intelligence-gap-2026-08-30.md:3` (vaultTools fixed bbox, labStory regex 5 markers, homeLab placeholder values, UploadLabModal defaults, interactionEngine fixtures, rxBridge templates, safety rules) must be replaced by **AI intelligence** — not just vault extraction.

> **This is a pre-orchestration blocking artifact.** Orchestrator must NOT start Phase 2 implementation until user approves or requests changes. Planning → Implementation enforced via `distributed-coding` with M0 planning gate.

## Context

- **Repo layout discovered via `glob` (list_dir):**
  - Stack Vite 6 + React 18 + Tailwind 3.x (`package.json:1-44` `type:module` + scripts `dev/build/test`), `vite.config.ts` with `test.environment: jsdom` + `setupFiles: ./test/setup.ts`, build ~1660 modules (prior `vite build` 793kB JS at `.teamwork/verification/final.md`), 40 tools catalog at `src/tools/index.ts:69-123` breakdown vault 3 (`extract_fact, confirm_fact, compile_health_record`) + labstory 2 (`extract_labs, correlate_meds`) + pillmap 8 + rxbridge 5 + homelab 5 + safety 9 + carecircle/dossier 8 (verified via `glob src/tools/**` → 7 files, `glob src/components/**` → 44 files, `glob src/**` → 68+ files).
  - Engine `src/core/webmcp/WebMCPEngine.ts` 820 lines after M1-M3 fix — `detectAndPolyfill` guarded native, spec-shaped polyfill (Promise `registerTool/getTools/executeTool`, `toolchange` EventTarget, `AbortSignal`, `exposedTo`, SecureContext logging), adapter `src/core/webmcp/WebMCPAdapter.ts:1` 134 lines mapping `parameters→inputSchema` stringified via `JSON.stringify`, `title=name`, `annotations.readOnlyHint`. Prior gap closed: delegates to native when `isNative`.
  - Types `src/types/webmcp.ts` 198 lines — `WebMCPToolDefinition` still internal `parameters` with `inputSchema?` alias, spec types `ModelContextTool/RegisteredTool`. Catalog bridge keeps `parameters` for tests while `inputSchema` alias stays consistent (getter at `src/tools/index.ts:142-155`).
  - Bootstrap `src/main.tsx` ~320 lines — `isSecureContext` guard, `Permissions-Policy allowsFeature('tools')`, `Promise.allSettled` per-tool with `AbortController` dedup, HMR dispose. Correct per spec §4.5.
  - Vault `src/core/vault/LocalVault.ts:1-719` 11 stores (facts/documents/meds/labs/allergies/conditions/proposals/calendar/careCircle/doctorGrants/questionBank/dueCards/dangerReports) + auditLog + eventBus typed helpers (`emitMedicationAdded` etc. at `:116-157`), all scoped by `patientId` from `localStorage carecanvas_active_user` — source of truth, seed is NO-OP, empty until user upload.
  - UI shells `src/components/vault/DocumentDropzone.tsx:1-187`, `src/components/vault/FactApprovalCard.tsx`, `src/components/vault/FactStreamView.tsx`, `src/components/homelab/UploadLabModal.tsx:1-351`, `src/components/pillmap/PillMapView.tsx:1-758`, `src/components/labstory/LabStoryView.tsx:1-615`, `src/components/rxbridge/RxBridgeView.tsx`, `src/components/safety/SafetyView.tsx`, `src/components/carecircle/CareCircleView.tsx`, `src/components/dossier/DossierView.tsx` — each reads `localVault` and `eventBus` relevant-only subscriptions (M2 matrix verified).
  - Knowledge `src/core/knowledge/interactionEngine.ts:1-404` (drug-drug/diet/duplicate via fixtures `src/fixtures/drug_knowledge.ts`) + `reconciliationEngine.ts` — rule-based, fixture `mockBrandGenericCatalog` etc., not LLM.
  - Fixtures `src/fixtures/documents.ts:1-18` mock-removed (exports only `__fixtureClean_documents`), real path is vault-derived.
  - Tests `test/unit/WebMCPEngine.test.ts`, `test/test-runner.ts` 172 vitest + 231 runner PASS pre-gap, `test/tier3-integration` 28+8, Flows A-E 6-viewport — all green at `.teamwork/state.json:219-232` M1-M3 PASS with repair `validation-order 40->39` fix.

- **Current intelligence gap evidence with `file:line` (via `grep` + `read`):**
  - **Vault extraction simple heuristic:** `src/tools/vaultTools.ts:47` `// Simple heuristic: split rawText into lines ... slice(0,3)` at `:48-52`, categories hardcoded `medication` at `:58`, `name: line.slice(0,40)` at `:59`, `value:{rawSnippet}` at `:60`, `boundingBox:{pageIndex:1,x:0.08,y:0.12+idx*0.08,width:0.78,height:0.05}` at `:64,80` — fixed regardless of document layout/image. `src/tools/vaultTools.ts:36-90` no vision, no entity normalization (dosage/frequency/brand→generic, labs vs allergies vs conditions), no structured JSON schema.
  - **DocumentDropzone rawText only:** `src/components/vault/DocumentDropzone.tsx:80-102` `FileReader.readAsText` for PDFs (binary garbled) and `readAsDataURL` for images but passes result as `rawText` string at `:84-85,41-45` — never as `image_url` vision input; only calls `extract_fact` with `documentId,rawText,documentType` and stops — no cross-field fan-out.
  - **confirm_fact limited propagation:** `src/tools/vaultTools.ts:165-204` on `approve` adds single `addMedication` (stub dosage `Standard`, `timingSlots:['morning']`) or `addLab` with generic `referenceRange {low:0,high:100}` — no pillmap `check_interactions/diet/duplicate/suggest_schedule`, no labStory normalization (`labStoryTools.ts:13-159` BIOMARKER_STANDARDS), no rxBridge `explain_med_change/flag_interaction/suggest_question`, no homeLab dueCard, no safety triage, no careCircle/dossier citation refresh.
  - **LabStory regex heuristic:** `src/tools/labStoryTools.ts:321-346` `if params.rawText` regex for 5 markers `creatinine|eGFR|potassium|HbA1c|glucose` via `text.match(p.regex)` — no LLM panel extraction, no date association, no multi-value. `labStoryTools.ts:508-547` `correlate_meds` narrative is template `eGFR ${direction}${changeDesc}` + `vault.getMedications.slice(0,3)` — not causal reasoning.
  - **HomeLab vision gap:** `src/tools/homeLabTools.ts:54-92` `isBase64Image = startsWith('data:image')||length>5000` → `textToParse=''` so data URL skips parsing; fallback placeholder `extractedValues {Creat 1.0, eGFR75}` at `:116-149` hardcoded. `src/components/homelab/UploadLabModal.tsx:42-46` `editForm:{creatinine:'1.90',egfr:'28',potassium:'4.8'}` defaults; `handleSimulateUpload:54-68` uses `imageBlob:'mock_photo_slip_blob_base64'` mock; `handleApproveAndCommit:82-138` writes those three labs directly with `sourceDocId:'doc_homelab_slip_002'` — approves without edit leaks synthetic 1.90/28.
  - **PillMap rule-only:** `src/tools/pillMapTools.ts:94-112` `check_interactions` via `ClinicalInteractionEngine.checkDrugInteractions` at `src/core/knowledge/interactionEngine.ts:6-11` importing `mockDrugDrugInteractions` fixture — static lookup, not document-derived. PillMapView only recomputes via that engine, never from new doc facts without manual drag/drop.
  - **RxBridge templates:** `src/tools/rxBridgeTools.ts:248-297` `suggest_question_for_doctor` `if ctx.includes('lisinopril')...` 4 hardcoded branches at `:283-297` → not LLM-generated from actual discharge list.
  - **Safety rule-only:** `src/tools/safetyTools.ts:37` `isSevere = severity==='severe'||tags includes chest_pain/dyspnea` rule; `photoBlob` stored as `{fileName:'edema_feet.jpg'}` at `:45-51` but not vision-analyzed.
  - **Dossier static citation:** `src/tools/vaultTools.ts:296-306` citations include hardcoded bbox `x:0.08` from extraction — synthetic grounding.
  - **Cross-field missing synthesis:** `LocalVault.ts:223-238` `addFact` emits only `fact_added` at `:234`; `addMedication` at `:307` emits `medication_added` — no orchestrated fan-out that would let one upload intelligently refresh pillmap/labStory/rxBridge/homeLab/safety/dossier/questionBank. EventBus has relevant-only subscriptions (e.g., `PillMapView.tsx:180-197` listens `medication_added/updated/proposal_status_changed` only; `LabStoryView.tsx:74-97` listens `lab_added/fact_confirmed/medication_updated` only) but upstream never triggers intelligent cross-fill.
  - **All 7 hardcoded gaps enumerated in `research/dispatcher-intelligence-gap-2026-08-30.md:3`** must be replaced by AI intelligence — not just vaultTools. See that research file for file:line citations and delta addendum for generic provider note.

- **LLM config discovered via `read` (.env.example allowed, NOT .env) + Settings page discovery via `glob`/`grep` (list_dir/grep_search):**
  - `.env.example:14-50` — `VITE_AI_ENABLED=false` toggle, `VITE_AI_PROVIDER=chat|responses` selector, `VITE_AI_BASE_URL=https://opencode.ai/zen/go/v1` (shown as **example configurable value only — not a hardcoded requirement**, any baseURL allowed), `VITE_AI_API_KEY=your-opencode-zen-key-...` placeholder, `VITE_AI_MODEL=deepseek-v4-flash-vision-exp` (shown as example for `chat`) + alternative `muse-spark-1.2-contributor` for `responses` at `:36-37` (both **examples only — any model allowed**), `VITE_AI_STRUCTURED_OUTPUTS=true` (`json_object`/`json_schema` + Zod), optional `VITE_AI_TEMPERATURE`, `MAX_TOKENS`, `TIMEOUT_MS=30000`, `ORG_ID`, `EXTRA_HEADERS`. Comment at `:29` confirms judges must paste own key, demo never commits `.env`. These `VITE_AI_*` keys are **example defaults**; actual runtime config may come from **either `.env` `VITE_AI_*` OR Settings page persistence** (e.g., localStorage/settings store) — user feedback says either source is valid; client must read runtime config generically, not hardcoded literals.
  - `opencode.json:35-68` — still only `commandcode` provider with `baseURL https://api.commandcode.ai/provider/v1`; model `opencode-go/muse-spark-1.2-contributor:68` present but no `opencode-go` provider entry for `https://opencode.ai/zen/go/v1` yet — green field for plan to add generically if the team chooses to use OpenCode agent for eval (but baseURL must remain configurable, not hardcoded to that value).
  - `grep VITE_AI|opencode.*zen|go/v1|responses` in `src/` → 0 hits (outside `.env.example`) — confirms no AI code yet; plan must cover client wiring with fallback to rule engine when `VITE_AI_ENABLED=false` or key absent, and must read config via `import.meta.env.VITE_AI_*` **or** Settings store — not literal strings.
  - **Settings page candidate discovery (required by user feedback):**
    - `glob src/components/**Settings*` → 0 files; `glob src/**/*[Ss]ettings*` → 0 files; `glob src/**` → 68 files none named Settings.
    - `grep -R "VITE_AI|settings|Settings" src/` → only hits are `src/index.css:19 font-feature-settings` and `src/components/carecircle/CareCircleView.tsx:12 Settings` icon import and `src/components/dossier/DossierView.tsx:302 Share Settings` tab label — **no dedicated Settings page/component exists yet** (green field). `grep -R "localStorage|store|useStore|zustand" src/` → hits are `LocalVault`, `carecanvas_active_user`, `LocalVaultManager` but no `VITE_AI` settings store. See `research/dispatcher-intelligence-gap-2026-08-30.md` delta addendum + `research/dispatcher-configurable-provider-delta-2026-08-30.md` for logs.
    - Conclusion: Settings page persistence (e.g., `localStorage` + settings store) must be **new** in implementation; until then `.env.example` `VITE_AI_*` is the only documented source. Plan must specify that **both sources are valid and client must prefer runtime config (Settings store if present, else `import.meta.env`) without hardcoding provider/model/baseURL literals**.
  - `webfetch https://opencode.ai/docs/go` on 2026-08-30 — verified Go docs: `Muse Spark 1.2 Contributor` endpoint `https://opencode.ai/zen/go/v1/responses` (`@ai-sdk/openai`), `DeepSeek V4 Flash Vision Exp` endpoint `https://opencode.ai/zen/go/v1/chat/completions` (`@ai-sdk/openai-compatible`), model id format `opencode-go/<model-id>`. These were **examples** of OpenAI-compatible endpoints — plan now treats **any OpenAI-compatible baseURL/model** as configurable, not just those two. Earlier `webfetch https://opencode.ai/docs/zen` and `webfetch https://webmachinelearning.github.io/webmcp/` (reused from `.teamwork/research/dispatcher-webmcp-hackathon-verification-2026-08-30.md`) remain valid for WebMCP spec; docs now inform generic structured-mode mapping (`response_format:{type:"json_object"}` vs `text.format:{type:"json_schema"}`) without hardcoding specific model→endpoint table.

- **Constraints/integrityMode hint:** No isolation — `state.json:metadata.artifactsDir=".teamwork"` and `request.workingDirectory=".teamwork"` → `.teamwork` in current project `/Users/sujal/Projects/proj1/.teamwork` is canonical. Prior `integrityMode: demo` (reproducible, verifiable, not mocked) — M1-M3 used demo for hackathon; same recommended here (user did not ask for benchmark; do not choose benchmark unless user says). Demo means protocol `toolchange`, `isSecureContext`, `Permissions-Policy` remain real not mocked; LLM path may be mock-network with schema validation real when no key, but must be verifiable. No hardcoded provider details — any provider allowed.

- **Working Directory:** `.teamwork` in current project (`/Users/sujal/Projects/proj1/.teamwork`) — per `state.json:metadata.artifactsDir` — NOT isolated `~/teamwork_projects/{PROJECT_NAME}`.

## Requirements (blocks — only what user cares about, ask don't assume)

> Capability-based: describe **what** must hold for intelligent handling, not **how** to code it. No file-edit prescription. Each block lists non-goals to avoid scope creep. Orchestrator must NOT interpret these as implementation steps.

### R1 — Intelligent document extraction with vision+text multimodal + structured outputs via generic configurable provider, all image OCR via AI, no hardcoding

**Requirement:** When a user uploads a document (PDF or image slip, real `FileReader` result — not mock `mock_photo_slip_blob_base64`), the system must intelligently extract **typed clinical entities** (labs with value/unit/drawDate/marker normalization, medications with brand→generic/dose/frequency/slot/withFood, allergies, conditions, supplements, vitals, diet habits) with **grounded normalized bounding boxes** (`{pageIndex, x, y, width, height}` derived from source, not fixed `0.08/0.12`) and **confidence + plain narration** per fact, using **generic configurable AI** — **any OpenAI-compatible provider, any model, any baseURL** — configured via **Settings page OR `.env` `VITE_AI_*`** (client must read runtime config such as `import.meta.env.VITE_AI_*` or Settings store/localStorage — **not hardcoded literals** for model/baseURL/endpoint). When `VITE_AI_ENABLED=true` (or Settings equivalent) and key present, the client must call the **configurable baseURL + path** (e.g., `{baseURL}/chat/completions` or `{baseURL}/responses` depending on provider/model — generically) with the **configurable model** selected by the user. Images must be sent as **vision inputs** (e.g., data URL `image_url`/`input_image` depending on provider) **along with text in a single multimodal response where the model supports it** (user explicitly requires images+text together if required — not separate OCR then text calls); text PDFs must be sent as document text (or rendered to image for vision if the provider supports image+text single response). **All image-based extraction or OCR must use AI** — never rule heuristics for images (no `split rawText` for data URLs, no placeholder `Creat 1.0` for base64 images). Output must be **structured JSON** validated against a schema (e.g., `Fact[]` with `category/name/value/unit/bounding_box/confidence/plainExplanation`) via the provider's **generic structured mode** (`json_object`/`json_schema`/`response_format`/`text.format` generically — not tied to a single provider's field name) when enabled (`VITE_AI_STRUCTURED_OUTPUTS` equivalent). When LLM is disabled or fails/rate-limited, the existing rule heuristic may serve as **fallback** (still staged `unconfirmed`), but never as primary when enabled and never for image OCR. Secret key must never be committed; judges/users configure via `.env` OR Settings page, and the app reads runtime config.

**Non-goals:** No hardcoded provider/model/baseURL/endpoint literals in `src/` (any specific model or baseURL must be configurable, not fixed strings in code); no hardcoded `slice(0,3)` medication-only or fixed `0.08` bbox; no mock `mock_photo_slip_blob_base64` default; no exposing key in repo; no benchmark-stdlib-only restriction; no prescribing client file path or prompt template — plan will define those. Example values may appear in `.env.example` as **configurable examples only**, never as hardcoded requirement in code (see Context for examples).

### R2 — Replace every hardcoded logic branch with AI intelligence + intelligent cross-field propagation when a document is handled

**Requirement:** **Every hardcoded branch enumerated in research** must be replaced by **AI-based intelligence** (not just `vaultTools.ts`): `vaultTools.ts:47` split-lines+bbox, `labStoryTools.ts:321` regex 5 markers + `labStoryTools.ts:508` template `eGFR ${direction}`, `homeLabTools.ts:54` base64-skip + placeholder `Creat 1.0`, `UploadLabModal.tsx:42` defaults `1.90/28/4.8` + `mock_photo_slip_blob_base64`, `interactionEngine.ts` fixture lookups (`mockDrugDrugInteractions`), `rxBridgeTools.ts:283` `if ctx.includes('lisinopril')` 4 branches, `safetyTools.ts:37` tag/severity rule — all must become AI-driven where document/image context is available (e.g., pillmap interactions/diet/duplicate/schedule via AI reasoning over extracted meds, labStory `correlate_meds` causal narrative via AI, rxBridge `suggest_question_for_doctor` via AI from actual discharge list, safety triage via AI assessment of freeText/photo, homeLab vision extraction via AI). An uploaded document's facts, once staged (`unconfirmed`) and later **confirmed (approve/edit) per fact** via human-in-the-loop, must then intelligently update **all relevant downstream fields** without requiring the user to manually re-enter data in each canvas:
- **Vault facts** → approval gate remains per-fact (`confirm_fact` human signature, not auto-confirmed).
- **PillMap** — new/changed medications appear on 7×4 canvas with interaction artefacts recomputed via AI-enhanced reasoning (not just static fixture) so user sees warnings/moves without manual `Add Medication` drag.
- **LabStory** — new lab points appear normalized (BIOMARKER_STANDARDS reference/optimal ranges, ±10% borderline, critical flags) on timeline with AI `correlate_meds` trajectory/cohort narrative reflecting the new points and active meds.
- **RxBridge** — reconciliation `explain_med_change` items, `flag_interaction`/`flag_diet_interaction` badges, and `suggest_question_for_doctor` question bank entries reflect the newly extracted regimen vs prior via AI synthesis.
- **HomeLab** — due cards inferred or marked completed when slip matches panel; proposals (`propose_dosage_change` etc.) linked to new lab points where clinically relevant, extracted via AI vision not hardcoded values.
- **Safety** — danger signs/triage (`report_danger_sign`) inferred if document contains red-flag text/photo cues via AI vision/text (still requires clinician path, not silent).
- **CareCircle/Dossier** — timeline `view_timeline` citations with real bounding boxes, emergency snapshot/allergies/conditions, and `compile_health_record` FHIR dossier reflect new facts; question bank enriched without duplicate spam via AI.
Propagation must respect patient isolation (facts for `patientId` derived from `localStorage carecanvas_active_user` only, never `''` nor `patient-s-devi` leak) and approval semantics (only `confirmed` facts propagate; `unconfirmed` stay staged; `rejected` never propagate). Cross-field updates must be visible via the existing relevance-aware `eventBus` subscriptions (no full-page reload required) and reflected in WebMCP tool results (`document.modelContext.executeTool` returning DOMString JSON with structured data).

**Non-goals:** No auto-confirm without human tap; no silent overwrite of existing meds/labs without diff; no cross-patient leakage; no inventing new tool names — reuse existing 40 tools plus intelligent orchestration between them; no leaving any of the 7 hardcoded gaps as fallback primary when AI is enabled.

### R3 — Generic configurable LLM config via .env OR Settings page without regression and no hardcoding

**Requirement:** The `.env.example` already checked in (:14-50) documents **example** `VITE_AI_*` keys (enabled/provider/baseUrl/model/visionModel/structured/timeout) — these are **configurable examples, not forced values**; the actual runtime config may come from **either `.env` (ignored via `.gitignore`) OR Settings page persistence (e.g., localStorage/settings store)** — users/judges may configure via either surface (copy `.env.example` → `.env` and paste key, or set via Settings UI). The app must read **runtime config generically** — `import.meta.env.VITE_AI_*` (Vite prefix) and/or Settings store — **never hardcoded literals** for provider/model/baseURL/endpoint fixed in `src/` (any model or baseURL must be user-configurable). `opencode.json` provider wiring for Go (if the team chooses to use OpenCode agent for eval) must point to a **configurable baseURL** consistent with the user's chosen provider, but must not leak `.env` secrets and must not hardcode the example baseURL as the only allowed. Build remains reproducible (`vite build` 1660±delta), lint `tsc --noEmit` 0, and WebMCP 40 tools remain discoverable/executable via `document.modelContext` in Chrome 149 (SecureContext) and via polyfill in jsdom — `tsc`, `vite build`, `vitest`, `test-runner` must still PASS with new AI path added (gated behind `VITE_AI_ENABLED`/Settings equivalent). Config must support **any model / any baseURL**.

**Non-goals:** No reading of `.env` by Dispatcher/Orchestrator (deny); no committing `.env` or `*.pem`; no `~/.config/opencode/teamwork` writes; no rehash of WebMCP protocol fix — that layer is already PASS and must not regress (`grep -R getTools` still 40, `grep Shanti` still only comment hits, etc.); no hardcoding example provider details as requirements.

### R4 — Planning-only M0 blocking before any implementation

**Requirement:** Phase M0 is **planning only**: produce reviewable artifacts under `.teamwork/` (updated `PROJECT.md`, `TEST_INFRA.md`, `prompt_draft.md` (this file), `research/dispatcher-intelligence-gap-2026-08-30.md` already drafted + delta addendum `research/dispatcher-configurable-provider-delta-2026-08-30.md` with Settings grep, plus new `plan.md` DAG, feature-vs-competitor novelty note, LLM wiring spec, verification plan, demo script) and block via `RequestFeedback` until user approves or requests changes. No `src/` edits, no `provider`/`model` rewiring, no test additions in M0. Orchestrator must NOT spawn M1 worktrees until M0 approval. The plan must explicitly sequence **planning (M0) → intelligence implementation (M1+)** with `distributed-coding` pattern (or alternative if user prefers), file ownership without overlap, and gates (Critic→Challenger→Auditor) that verify both LLM intelligence and WebMCP preservation and that **no hardcoded provider/model/baseURL literals remain in `src/`**.

**Non-goals:** No code, no prompt template implementation, no architecture prescription beyond capability boundaries; no absorbing full worker histories — keep summarized citations with `file:line`.

## Independent Verification (per requirement — objective check)

> Each verification is **mechanical, reproducible**, runnable by reviewer without human judgment. Must work in both native (Chrome 149 `isSecureContext` true at `http://localhost:5173`) and fallback jsdom (`npx tsx`). Provide logs under `.teamwork/verification/` and screenshots under `.teamwork/snapshots/` per prior gate pattern. Dispatcher logs must show `glob`/`grep`/`read`/`webfetch` usage.

### R1 — Verification: generic configurable extraction with vision+text multimodal + structured outputs, no hardcoding, all OCR via AI

- **Grep gates (must ALL PASS with logs):**
  - `grep -R "VITE_AI_ENABLED|VITE_AI_PROVIDER|VITE_AI_BASE_URL|VITE_AI_MODEL" .env.example` count >=4 PASS, log `.teamwork/verification/env-config.log`
  - **No-hardcode provider gates (after implementation, count must be 0 in `src/`):** `grep -R "deepseek-v4-flash-vision-exp" src/` count 0 PASS; `grep -R "muse-spark-1\.2-contributor" src/` count 0 PASS; `grep -R "https://opencode\.ai/zen/go/v1" src/` count 0 PASS; `grep -R "opencode\.ai/zen" src/` count 0 PASS — proves no hardcoded model/baseURL literals in code (they may exist only in `.env.example` as configurable example). Log `.teamwork/verification/no-hardcode-provider.log`
  - **Configurable-read gate:** `grep -R "import\.meta\.env.*VITE_AI|VITE_AI.*localStorage|settings.*VITE_AI|useSettings|SettingsStore" src/` count >=1 PASS (verifies client reads runtime config from env OR Settings page, not literals). Log `.teamwork/verification/configurable-read.log`
  - **Hardcoded bbox removal:** `grep -R "boundingBox.*x: 0\.08.*y: 0\.12" src/tools/vaultTools.ts` count 0 after implementation (today still present as evidence — plan notes it will be replaced by AI-grounded values after M1); verifier for planning checks research cites it, for implementation checks 0. Log `.teamwork/verification/no-hardcode-bbox.log`
  - **Vision multimodal gate:** `grep -R "vision|image_url|input_image" src/` count >=1 after implementation (before, count 0 — proves green field → plan covers gap), and captured network probe shows a single request containing **both** image data URL and text when an image slip is uploaded (multimodal single response), not two separate calls. Log `.teamwork/verification/vision-multimodal.log`
  - **Structured generic gate:** `grep -R "json_schema|json_object|response_format|text\.format|structured" src/` count >=1 after implementation (before 0) — generic structured mode, not tied to single provider field. Log `.teamwork/verification/structured-generic.log`
  - **All OCR via AI gate:** `grep -R "extract.*text.*split.*rawText.*slice\(0,3\)|startsWith.*data:image.*textToParse.*''" src/tools/vaultTools.ts src/tools/homeLabTools.ts` count 0 after implementation (proves image OCR heuristic removed; today count >0 as evidence). Log `.teamwork/verification/no-hardcode-ocr.log`

- **Config probe (planning):** `cat .env.example` shows `VITE_AI_*` example keys; `cat` of candidate Settings component `glob src/components/**Settings*` shows 0 files (green field) and `grep Settings` shows only carecircle/dossier labels — screenshot and log `.teamwork/verification/env-config.log` PASS; `cat opencode.json` provider still `commandcode` (plan notes future configurable Go wiring, not hardcoded — PASS for planning).

- **Demo probe (after implementation, not in M0):** Upload a sample discharge PDF (text `Apixaban 5mg BID, Creatinine 1.9 mg/dL, eGFR 28`) via real `DocumentDropzone` drag-drop at `http://localhost:5173` → `modelContext.executeTool(find{extract_fact}, {documentId, rawText|imageBlob})` returns DOMString JSON parsed to `Fact[]` length >0 with at least one `category:lab` (Creatinine) and one `category:medication` (Apixaban) and each has `boundingBox {pageIndex,x,y,width,height}` not equal to `0.08/0.12` fixed and `confidence>0`; upload image slip (data URL) shows fetch body contains **image_url or input_image plus text** in **single request** via captured probe (mock network with schema validation) and **no hardcoded provider literal** in request URL (URL equals configurable `VITE_AI_BASE_URL` or Settings baseURL). Screenshot `vision-upload-1280.jpg` + `375.jpg` + `768.jpg` JFIF >5K under `.teamwork/snapshots/intelligence/`. Log `.teamwork/verification/structured-outputs.log`. **For M0 planning, this probe is documented as future — not executed today.**

### R2 — Verification: every hardcoded branch replaced by AI + cross-field propagation

- **Glob/grep evidence (planning):** `glob src/components/**` 44 files + `read src/tools/vaultTools.ts:165-204` confirm propagation only touches single store; `research/dispatcher-intelligence-gap-2026-08-30.md:3` enumerates 7 gaps with `file:line` (already PASS, `grep -c "file:line"` >0 in research). `grep -R "emitMedicationAdded|emitLabAdded|fact_status_changed|medication_added" src/core/vault/LocalVault.ts src/tools/vaultTools.ts` shows relevant-only fan-out exists but not intelligent orchestration — log `.teamwork/verification/propagation-grep.log`.
- **Hardcode branch elimination gates (after implementation, all counts 0 in `src/`):**
  - `grep -R "mock_photo_slip_blob_base64" src/` 0 PASS (no mock default)
  - `grep -R "Creat 1\.0.*eGFR.*75|extractedValues.*Creat" src/tools/homeLabTools.ts` 0 PASS (no placeholder)
  - `grep -R "creatinine.*1\.90.*egfr.*28" src/components/homelab/UploadLabModal.tsx` 0 PASS (no hardcoded editForm defaults — values must come from AI extraction)
  - `grep -R "if.*ctx\.includes.*lisinopril" src/tools/rxBridgeTools.ts` 0 PASS (no hardcoded 4-branch templates)
  - `grep -R "text\.match\(p\.regex\).*creatinine" src/tools/labStoryTools.ts` 0 PASS when AI enabled (regex fallback may remain as disabled-fallback but not primary)
  - `grep -R "mockDrugDrugInteractions|mockBrandGenericCatalog" src/core/knowledge/interactionEngine.ts` count may remain for fallback but AI path must exist and be primary when enabled — verify `grep -R "import\.meta\.env.*VITE_AI|AI.*interaction|AI.*correlate" src/` >=1
  - `grep -R "severity.*severe.*chest_pain|fileName.*edema" src/tools/safetyTools.ts` 0 when AI enabled (rule fallback not primary for photo vision)
  - Logs `.teamwork/verification/no-hardcode-branches.log`
- **Event matrix probe (after implementation):** After `extract_fact` doc `doc-intel-001` with `rawText` containing 2 meds + 1 lab, `confirm_fact` approve all → assert within 2s that `localVault.getMedications(patientId)` grew by ≥2, `localVault.getLabs(patientId)` grew by ≥1 with normalized `BIOMARKER_STANDARDS` (e.g., Creatinine `referenceRange {0.6,1.2}` not `{0,100}`), `getQuestionBankItems(patientId)` grew by ≥1 via AI `suggest_question`, `getDueCards/dangerReports` updated if applicable — all for same `patientId` as `localStorage carecanvas_active_user.userId`, not `''`. Verify via `npx tsx .teamwork/verification/cross-field-probe.ts` → `CROSS_FIELD PASS` log `.teamwork/verification/cross-field.log`. UI probe: navigate vault→pillmap→labStory→rxBridge→dossier without reload, each shows new data; screenshots per canvas at 1280/375.
- **Approval semantics probe:** Confirm `unconfirmed` facts never appear in `compile_health_record` `confirmedFacts` nor pillmap `getActiveMedications`, but after `confirm_fact approve` they do; `reject` removes from propagation set — verified via harness `test/tier3-integration/cross-module-integration.spec.ts` pattern.
- **Patient isolation probe:** Set `localStorage carecanvas_active_user` to `test-patient-intel-001`, upload doc, assert `vault.getFactsByPatient('')===0`, `getFactsByPatient('test-patient-intel-001').length>0`, and `getFactsByPatient('patient-s-devi')===0` — prevents orphan leak (same pattern as M1 `challenger-m1-retry.mjs:282-315`).

### R3 — Verification: generic configurable LLM config wiring without regression, no hardcoding

- **Grep gates:**
  - `grep -R "VITE_AI" src/` after implementation count >=1 PASS (client reads `import.meta.env.VITE_AI_*` or Settings store generically) — planning gate today count 0 documented as green field
  - `grep -R "\.env" .gitignore` count >=1 PASS (ensures `.env` not committed)
  - `grep -R "apiKey.*VITE_AI|import\.meta\.env" src/` count >=1 after implementation (never hardcoded key literal)
  - **No-hardcode secret/model/baseURL in src:** `grep -R "deepseek|muse-spark|opencode\.ai/zen" src/` 0 PASS (proves configurable); `grep -R "sk-" src/` 0 PASS (no literal key)
  - `cat opencode.json | grep -A2 opencode-go` shows Go provider after implementation only if team opts and with configurable baseURL (not forced) — planning gate logs current `commandcode` only and notes delta
  - `npm run lint` (`tsc --noEmit`) 0, `npm run build` valid 1660±delta, `npm test` 172 PASS, `npx tsx test/test-runner.ts` 231 PASS — logs `.teamwork/logs/*.log` + `.teamwork/verification/final.md` (after implementation); for M0, prior M1-M3 logs already PASS and must stay PASS (no src edits in M0, so automatically preserved)
- **Secret leak gate:** `git diff --stat` shows only `.teamwork/**` changed in M0 (no `src/`), `git ls-files | xargs grep -l "sk-"` 0, `grep -R "VITE_AI_API_KEY" src/` never contains literal value — documented in `research` file.
- **Settings page gate (after implementation):** `glob src/components/**Settings*` and `grep -R "Settings" src/` should show a Settings component exists and that it persists `VITE_AI_*` (baseURL/model/provider) to `localStorage`/store; `grep -R "localStorage.*VITE_AI|settings.*store" src/` >=1 PASS — log `.teamwork/verification/settings-config.log`

### R4 — Verification: planning before implementation (M0 gate)

- **Artifacts existence (must all exist before implementation — today M0 check):**
  - `.teamwork/prompt_draft.md` with `## Objective` (both verbatim blocks), `## Context` (repo layout + gap file:line + .env.example config + Settings grep discovery + webfetch citations), `## Requirements` (R1-R4 capability blocks with non-goals, generic configurable wording, no hardcoded model/baseURL), `## Independent Verification` (per requirement mechanical including no-hardcode gates), `## Acceptance Criteria` (checkboxes), `## Working Directory` (`.teamwork` in current project), `## Integrity Mode`, `## Execution Path` (distributed-coding two-phase blocking), `## Open Questions` (Q1..Qn), and `> Status: AWAITING_FEEDBACK` blocking line — `grep -c "## Objective" .teamwork/prompt_draft.md` >=1 PASS, `grep -c "file:line"` in research >0 PASS, `grep -c "hardcode" .teamwork/prompt_draft.md` >0 PASS, `grep -c "configurable" .teamwork/prompt_draft.md` >0 PASS, `grep -c "VITE_AI" .teamwork/prompt_draft.md` >0 PASS
  - `.teamwork/research/dispatcher-intelligence-gap-2026-08-30.md` with `file:line` citations + tool discipline log (this run) — `ls -lh .teamwork/research/dispatcher-*.md` 3+ files PASS (delta file added)
  - `.teamwork/research/dispatcher-configurable-provider-delta-2026-08-30.md` with Settings-page grep results and generic provider note (new delta file, see Verifiable Success) — `grep -c "VITE_AI" >0` PASS
  - `.teamwork/PROJECT.md` (exists from M1) + `.teamwork/TEST_INFRA.md` (exists) + new `plan.md` draft (to be produced by Orchestrator in M0 planning milestone, not by dispatcher today — placeholder note in Open Questions)
  - **M0 src no-edit gate:** `git diff --stat | grep -v ".teamwork/"` count 0 (only `.teamwork/` touched) — proves planning did not mutate `src/` — log `.teamwork/verification/planning-gate.log` with `stat .teamwork/prompt_draft.md` mtime vs `stat src/tools/vaultTools.ts` mtime (planning mtime < src mtime after implementation will show ordering)

- **Orchestrator gate (after approval):** `cat .teamwork/state.json` shows `plan.createdAt` before `progress.completedWorkstreams[0]` timestamp (enforced by Sentinel — orchestrator must NOT start Phase 2 until `AWAITING_FEEDBACK` resolved via `RequestFeedback`). For today, Sentinel surfaces `prompt_draft.md` as blocking artifact without spawning orchestrator.

## Acceptance Criteria (testable DONE)

- [ ] **R1 DONE — Generic configurable intelligent extraction:** Uploading a real PDF (text) or image slip (data URL) via `DocumentDropzone`/`UploadLabModal` with AI enabled (via `VITE_AI_ENABLED` or Settings equivalent) triggers **configurable** AI call (baseURL/model read from runtime config — **not hardcoded literals** for model/baseURL/endpoint in `src/`, any OpenAI-compatible provider allowed) using the provider's **generic structured mode** (`json_object`/`json_schema`/`response_format`/`text.format` generically) and **vision+text multimodal single response where model supports it** (images along with text in one call; all image OCR via AI), returns `Fact[]` with mixed categories (≥1 lab + ≥1 medication for a sample discharge), each with normalized `value/unit`, `confidence`, and grounded `boundingBox {pageIndex,x,y,width,height}` not equal to fixed `0.08/0.12` — verified via `structured-outputs.log` + `env-config.log` + `no-hardcode-provider.log` (grep 0 for hardcoded literals) + `configurable-read.log` + `vision-multimodal.log` + screenshots `vision-upload-{1280,375,768}.jpg` JFIF >5K + `grep` gates for `image_url|input_image` and `json_schema|response_format` as generic signals >=1.

- [ ] **R2 DONE — Every hardcoded branch replaced + cross-field propagation:** All 7 hardcoded gaps replaced by AI intelligence (vaultTools fixed bbox→AI grounded, labStory regex→AI panel extraction, homeLab placeholder→AI vision, UploadLabModal defaults→AI values, interactionEngine fixture→AI reasoning, rxBridge templates→AI questions, safety rules→AI triage vision) verified via `no-hardcode-branches.log` (all counts 0 for those literals when AI enabled). After `confirm_fact` approve of all staged facts from one document, downstream fields automatically reflect new data without manual re-entry: `localVault.getMedications(patientId)` +≥1 med with generic resolution, `getLabs(patientId)` +≥1 lab normalized per `BIOMARKER_STANDARDS`, pillmap shows interaction arcs recomputed via AI, labStory timeline/chart includes new points with AI `correlate_meds` delta, rxBridge `compilePatientSummary` includes new med list + AI `suggest_question_for_doctor` question bank +1, homeLab dueCards/proposals updated if relevant via AI, safety triage not spuriously triggered but photo red flags routed via AI vision, dossier `compile_health_record` citations include new doc's bounding boxes + timeline items — verified via `cross-field.log` harness `CROSS_FIELD PASS` + 6-viewport canvas screenshots + patient isolation probe (`''` 0, `test-patient-intel-001` >0, `patient-s-devi` 0).

- [ ] **R3 DONE — Generic configurable wiring no regression, no hardcoding:** `cat .env.example` documents **example** `VITE_AI_*` (enabled/provider/baseUrl/model/visionModel/structured/timeout) and Settings page persists same keys; runtime reads generic config (either `import.meta.env.VITE_AI_*` or Settings store, never literal key/model/baseURL), fallback heuristic when disabled/failing returns staged `unconfirmed` with message, no secret committed (`git ls-files` grep `sk-` 0, `.gitignore` has `.env`), and **no hardcoded provider/model/baseURL literals in `src/`** (generic grep for specific model/baseURL literals in `src/` 0), and `npm run lint` 0 + `npm run build` 1660±delta + `npm test` 172 PASS + `npx tsx test/test-runner.ts` 231 PASS + `await document.modelContext.getTools()` 40 PASS in both Chrome 149 and jsdom — logs `.teamwork/verification/final.md` + `configurable-read.log` + `no-hardcode-provider.log` + `env-config.log`.

- [ ] **R4 DONE — Planning blocked implementation:** `.teamwork/prompt_draft.md` (this file) exists with all required sections + both verbatim blocks + `> Status: AWAITING_FEEDBACK` + tool discipline evidence (`glob`/`grep`/`read`/`webfetch` logs in research), `research/dispatcher-intelligence-gap-2026-08-30.md` exists with `file:line` >0 + delta addendum, `research/dispatcher-configurable-provider-delta-2026-08-30.md` exists with Settings grep + generic provider note, `git diff --stat` shows only `.teamwork/**` modified in M0 (no `src/`), and Sentinel/Orchestrator has NOT started Phase 2 worktrees until user resolves feedback via `RequestFeedback` — verified via `stat` mtime ordering `planning-gate.log` and `state.json` timestamps (plan before workstreams). `grep -c "hardcode"` >0, `grep -c "configurable"` >0, `grep -c "VITE_AI"` >0 in prompt_draft.md PASS.

## Working Directory

**`.teamwork` in current project (`/Users/sujal/Projects/proj1/.teamwork`)** — confirmed from `state.json:metadata.artifactsDir=".teamwork"` and `request.workingDirectory=".teamwork"` — NOT isolated `~/teamwork_projects/{PROJECT_NAME}/.teamwork`. Per dispatcher constraint, no isolation was requested and this draft states it explicitly. All artifacts must live under `.teamwork/` ( `research/`, `prompt_draft.md`, `verification/`, `snapshots/`, `worktrees/` ) — never under `~/.config/opencode/teamwork` (that tree stays clean, only `.ts`/`.json`/pattern there). If user later wants isolation, alternative would be `~/teamwork_projects/proj1/.teamwork` — confirm default "`.teamwork` in current project" is intended in feedback Q1.

## Integrity Mode

- **development** (rapid, heuristic mocks tolerated, quick polyfill overwrite allowed — not sufficient for hackathon submission trust)
- **demo** (**recommended for this hackathon intelligence milestone** — reproducible, verifiable, not mocked — judges clone `npm install && npm run build && npm test && npm run dev` → `http://localhost:5173` SecureContext → `await document.modelContext.getTools()` → 40 remains PASS, upload real PDF/image → see intelligent multi-field updates with real vault `patientId` isolation, no `Shanti` leak, LLM path verifiable via schema logs + screenshots; network to configurable AI baseURL may be mocked in CI but schema validation and vision multimodal payload shape must be real)
- **benchmark** (from-scratch stdlib only — not asked, would forbid `ClinicalInteractionEngine` fixtures and is unnecessary)

**Recommendation: `demo`** — same as prior M1-M3. Protocol `toolchange`/`SecureContext` must be real not mocked; LLM extraction must be real schema-shaped (mock network allowed in offline CI but vision `input_image` vs `image_url` shape and `json_schema` validation must be real, and no hardcoded provider literals). Please confirm via feedback (see Q7).

## Execution Path

**`distributed-coding` with explicit two-phase gating (M0 planning BLOCKING → M1+ implementation)** — per `state.json:metadata.pattern="distributed-coding"` and `teamwork/patterns/distributed-coding/pattern.json`. Thorough planning then implementation is **enforced as milestones M0 → M1 → M2 → M3** with parallel worktrees and per-milestone gates, not optional single-file edits:

- **Phase 1 — M0 Planning (BLOCKING, no src edits) — THIS DRAFT:**
  - Dispatcher (this role) → `research/dispatcher-intelligence-gap-2026-08-30.md` (done) + `research/dispatcher-configurable-provider-delta-2026-08-30.md` (new delta) + `prompt_draft.md` (this file) with blocking `AWAITING_FEEDBACK`.
  - Orchestrator (after user approval) synthesizes `.teamwork/PROJECT.md` delta (intelligence gap addendum noting generic configurable provider, Settings page OR .env, multimodal vision+text, all OCR via AI, 7 gaps replaced), `.teamwork/TEST_INFRA.md` delta (vision multimodal/structured generic/cross-field/no-hardcode probes), `.teamwork/plan.md` DAG with M0→M1→M2→M3 dependencies, ownership without overlap, verification plan, demo script, plus `research/` supplements if parallel dispatchers exist. Output written under `.teamwork/` only. **Gate: Sentinel must get user `AWAITING_FEEDBACK` approval before spawning workers for `src/`.**

- **Phase 2 — Implementation (parallel workers, isolated `.teamwork/worktrees/`, gates) — AFTER APPROVAL:**
  - **M1 Intelligence Core (R1 extraction, generic configurable):** Generic AI client wrapper that reads `import.meta.env.VITE_AI_*` **or** Settings store (not hardcoded literals), handles configurable provider/model/baseURL branching generically, vision+text multimodal single response where model supports it, structured JSON generically, fallback heuristic only when disabled, **all image OCR via AI** — ownership likely `src/core/ai/*` (new) + `src/tools/{vault,labStory,homeLab}Tools.ts` disjoint from catalog bridge, preserves `parameters→inputSchema` alias, ensures generic grep for hardcoded provider literals in `src/` stays 0.
  - **M2 Cross-Field Intelligence Propagation (R2):** Replace every hardcoded branch with AI intelligence (pillmap/labStory/rxBridge/homeLab/safety/careCircle) and orchestrate `confirm_fact` fan-out to pillmap/labStory/rxBridge/homeLab/safety/careCircle/dossier/questionBank via `LocalVault` helpers + `eventBus` relevance matrix, plus dueCard/danger inference via AI — ownership `src/core/vault/*` + `src/components/{vault,homelab}*` + `src/components/{pillmap,labstory,rxbridge,safety,carecircle,dossier}*` partitioned per workstream to avoid overlap, async `Promise.allSettled` per-fact, no hardcoded templates.
  - **M3 Hardening & Verification (R1+R2+R3 + no regression + no hardcoding):** Env/Settings wiring validation (configurable `VITE_AI_*` via either source, no literals), WebMCP 40 preservation, patient isolation probes, lint/build/test/flow/6-viewport screenshots (`vision-upload-*`, `cross-field-*`), final `Success Auditor` with `grep` no-hardcode gates + native probe + `structured-outputs.log` + `no-hardcode-provider.log`.
  - Gates per milestone: `Critic → Challenger → Auditor → Success Auditor` with Ralph Loop back to Orchestrator on FAIL; final Success Auditor requires all R1-R4 PASS, no secret leak, no hardcoding, and `git diff --stat` shows disciplined ownership.

Confirm `distributed-coding` two-phase or propose `iterative-coding` alternative in feedback Q8. Do NOT start orchestrator until `RequestFeedback` resolved.

## Open Questions for User

- **Q1 — Working directory confirmation:** Default `.teamwork` in current project `/Users/sujal/Projects/proj1/.teamwork` (per `state.json:metadata.artifactsDir`) vs isolated `~/teamwork_projects/{PROJECT_NAME}/.teamwork`. No isolation requested — confirm "`.teamwork` in current project" is intended.
- **Q2 — Extraction trigger with generic provider:** Should intelligent extraction run **inside WebMCP tool `extract_fact` when agent calls it** (tool itself calls generic AI client and returns `Fact[]`) vs **in UI `DocumentDropzone.handleRealExtract` before calling vault `addFact`** (component calls AI then writes via tool)? Impacts `plainLanguageSummary` vs `data` contract and demo traceability via `document.modelContext.executeTool`; either way must use configurable provider not hardcoded.
- **Q3 — Vision multimodal for PDFs vs images (generic):** PDFs today are textual; should vision be used **only for images** (`lab_slip_photo` data URL → `image_url`/`input_image` generically) while PDFs use text extraction, or should PDFs also be rendered to image for vision **single response with text+images** when provider supports multimodal? User requires images+text in single response if required — how to apply to PDF image rendering?
- **Q4 — Structured output scope (generic):** Which tool should showcase structured outputs per generic structured mode? `extract_fact` `Fact[]` with `bounding_box` vs `extract_labs` `LabRecord[]` vs `upload_lab_image` vs all three? Need JSON Schema shape to codify generically (`response_format` vs `text.format.json_schema` depending on provider).
- **Q5 — Propagation approval level:** When a document yields 5 facts (2 meds + 2 labs + 1 allergy), should confirming **one fact** auto-update only that downstream field (e.g., med → pillmap only via AI) or should **batch-confirm all** fan out to every affected canvas? Affects `approvalGateType` (`inline_fact` vs `batch`) and question bank spam control.
- **Q6 — HomeLab vs DocumentDropzone split (all OCR via AI):** Should `UploadLabModal`'s hardcoded `editForm 1.90/28/4.8` be replaced by AI vision-extracted values (so approve uses AI values, all image OCR via AI) while `DocumentDropzone` handles generic documents, or should both share same generic AI extraction path? Impacts `linkedDueCardId` handling.
- **Q7 — Integrity mode confirmation:** Confirm `demo` (reproducible, verifiable, not mocked — recommended) vs `development` vs `benchmark`. For LLM, should network be **real** (requires configurable `VITE_AI_API_KEY` valid, `npm run dev` hits configurable baseURL) or **mocked with schema validation real** in CI?
- **Q8 — Execution path confirmation:** Confirm `distributed-coding` two-phase M0→M1→M2→M3 blocking vs `iterative-coding` (single worker loop) vs `document-review`. For intelligence gap we recommend `distributed-coding` with M0 blocking — confirm.
- **Q9 — Settings page vs .env precedence:** Since baseURL/model/provider can be configured via **either Settings page OR `.env`**, which should take precedence at runtime? Proposed: Settings store (localStorage/settings) overrides `import.meta.env` if present, else env — confirm. Also where should Settings page live (e.g., `src/components/settings/SettingsView.tsx` with `VITE_AI_*` fields)?
- **Q10 — Fallback when AI disabled/fails:** When AI disabled or key absent/rate-limited, should fallback be **rule heuristic** (`split rawText` + regex — but never for images) with notice toast, or **return empty with "enable AI" message** and not stage facts? User says all image OCR must be AI — so image fallback must be "enable AI" not placeholder.
- **Q11 — Secret handling with Settings page:** `import.meta.env.VITE_AI_API_KEY` and Settings page key both ship in bundle (client-exposed) — acceptable for `demo` hackathon (judges paste own key via `.env` OR Settings page, comment at `.env.example:29`), or should plan propose a local proxy/serverless stub to hide key? Tradeoff complexity vs transparency.
- **Q12 — Evaluation set for planning:** Should the plan include a small golden set (e.g., 3 de-identified docs: discharge list with med changes, lab panel with Creatinine/eGFR/Potassium, allergy/condition note) to verify extraction quality in `verification/` logs without PHI? Needed for reproducible `structured-outputs.log`.
- **Q13 — Any model / any baseURL generic confirmation:** Plan now treats provider/model/baseURL as **fully configurable** (any OpenAI-compatible). Confirm that **no specific model or baseURL is required** — examples like `deepseek-v4-flash-vision-exp` or `https://opencode.ai/zen/go/v1` are only documentation examples, not constraints. Should the plan explicitly list "hardcode ban" verification (`grep 0` for those literals in `src/`)?

> **Blocking:** Reply with `RequestFeedback` (or comment on this artifact) to approve or request changes. Orchestrator will NOT start until you resolve feedback. Planning gate M0 must be approved before code.

> Status: AWAITING_FEEDBACK
