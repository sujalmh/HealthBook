# Prompt Draft — CareCanvas WebMCP Hackathon Submission — Awaiting Feedback

## Objective

> **Verbatim user goal (preserved):**
> ```
> the app’s project submission for webmcp hackathon. so make sure the WebMCP integration is protocol-correct. do thorough online research about webmcp.
> ```
> **Clarified scope synthesis (what to achieve, not how):**

CareCanvas must be **protocol-correct per W3C WebMCP spec (Draft 26 Aug 2026)** for hackathon judging: all 40 existing WebMCP tools across 7 modules (vault 3 + labstory 2 + pillmap 8 + rxbridge 5 + homelab 5 + safety 9 + carecircle/dossier 8 at `src/tools/index.ts:69-123`) are discoverable and executable via the **canonical browser interface `document.modelContext`** — not via custom globals — with spec-compliant dictionaries, Promise-based lifecycle, SecureContext/Permissions-Policy handling, and cross-origin exposure, while preserving existing product invariants (build 1660 modules, tests 172 vitest / 231 runner PASS prior, empty vault until Create Account, real FileReader upload, approval gates, 6-viewport responsive). Existing research at `.teamwork/research/dispatcher-webmcp-engine-protocol.md:1` (142 lines, 7 findings) was reused and **independently re-verified via live webfetch** this run; 3 new nuances added.

> **This is a pre-orchestration blocking artifact.** Orchestrator must NOT start Phase 2 until user approves or requests changes.

## Context

- **Repo layout discovered:**
  - Stack Vite 6 + React 18 + Tailwind 3.x (`package.json:1-44`, `vite.config.ts:1-36` with `test.environment: jsdom` + `setupFiles: ./test/setup.ts`), build 1660 modules (prior success auditor `vite build` 793kB JS at `.teamwork/verification/final.md`), 40 tools catalog at `src/tools/index.ts:69-123` breakdown 3+2+8+5+5+9+8 verified via `glob src/tools/**` → 8 files.
  - Engine singleton `src/core/webmcp/WebMCPEngine.ts:14` class `WebMCPEngine` 433 lines, export `webMCPEngine = new WebMCPEngine():433` constructed at import side-effect; calls `detectAndPolyfill():22-25` immediately.
  - Types `src/types/webmcp.ts:34-45` `WebMCPToolDefinition` with internal shape `name, description, moduleOwner, category, requiresHumanApproval, approvalGateType, parameters: WebMCPToolParameterSchema:5-19, returns, execute, uiSideEffects` — **NOT spec shape** (spec expects `inputSchema`, `title`, `annotations`, `execute(inputObject, options={signal})`).
  - Registration entry `src/tools/index.ts:127-136` `registerAllWebMCPTools(engine = webMCPEngine):127` loops `engine.register(tool):134` sync void — never awaits `document.modelContext.registerTool`.
  - Bootstrap timing `src/main.tsx:9-67` `bootstrap():9` → `await localVault.init():11` → `hydrateFromSupabase` best-effort `:26-63` → `registerAllWebMCPTools():66` before `ReactDOM.createRoot:72` → fallback same `:83` on `bootstrap.catch`; no `SecureContext` guard, no `toolchange` handling, no HMR dedup.
  - UI surfaces `src/components/common/WebMCPInspector.tsx:17-46` reads engine directly `webMCPEngine.getRegisteredTools():46` — NOT native; `src/components/common/ConnectWebMCPModal.tsx:67` lists 4 globals `window.modelContext, navigator.modelContext, document.modelContext, window.__CareCanvas_WebMCP__` and example at `:74,78` uses `window.modelContext.getRegisteredTools()` sync + `executeTool('extract_fact', {...})` string-based — diverges from spec's `document.modelContext.getTools()` Promise + object-based.
  - Tests `test/unit/WebMCPEngine.test.ts:13-35` asserts 40 tools via engine direct; prior overall 172 vitest PASS + 231 runner PASS all via polyfill path (`isNative false` in jsdom), **none exercising native branch**.

- **Online spec discovered (webfetch-verified 2026-08-30 — not invented):**
  - **Source URLs:** `https://github.com/webmachinelearning/webmcp` (repo README + imperative examples) and `https://webmachinelearning.github.io/webmcp/` (normative spec 26 Aug 2026, 136 commits) and `https://github.com/webmachinelearning/webmcp/blob/main/implementation-status.md` (browser support).
  - **IDL (spec §4.1):** `partial interface Document { [SecureContext, SameObject] readonly attribute ModelContext modelContext; }` — canonical surface is **only `document.modelContext`** (SecureContext required). Not `window.modelContext`, not `navigator.modelContext`, not `globalThis.modelContext`. See https://webmachinelearning.github.io/webmcp/#document-extension — confirmed from webfetch (spec IDL index). Current code at `WebMCPEngine.ts:29` `globalThis.document?.modelContext` + `:30` `globalThis.navigator?.modelContext` and polyfill at `:60-64` writing `globalThis.modelContext` + `__CareCanvas_WebMCP__` + `document.modelContext` are non-spec globals.
  - **Methods (spec §4.2):** `[Exposed=Window, SecureContext] interface ModelContext : EventTarget { Promise<undefined> registerTool(ModelContextTool tool, optional ModelContextRegisterToolOptions options={}); Promise<sequence<RegisteredTool>> getTools(optional ModelContextGetToolOptions options={}); Promise<DOMString> executeTool(RegisteredTool tool, optional object inputObject={}, optional ModelContextExecuteToolOptions options={}); attribute EventHandler ontoolchange; }` — all Promise-based, not sync void. See https://webmachinelearning.github.io/webmcp/#model-context-container
  - **Tool dictionary (spec §4.2.1 `ModelContextTool`):** `name` (1-128 chars, `^[a-zA-Z0-9_.-]+$`, else InvalidStateError), `description` non-empty else InvalidStateError, `inputSchema` (JSON Schema object, stringified internally via `JSON.stringify`, else TypeError on circular), optional `title`, optional `annotations: {readOnlyHint, untrustedContentHint}`, `execute: (inputObject, options={signal}) => Promise<any>` returning serialized content. Current `parameters` field at `WebMCPToolDefinition:41` will be **ignored** by native — native would see empty `inputSchema` (`""` default) and lose schema. See https://webmachinelearning.github.io/webmcp/#dom-modelcontexttool-inputschema — verified via webfetch full `registerTool` steps 7-13.
  - **Options:** `ModelContextRegisterToolOptions: signal, exposedTo: string[]` trusted origins (§4.2.3), `ModelContextGetToolOptions: fromOrigins: string[]` (§4.2.4), `ModelContextExecuteToolOptions: signal` (§4.2.5), `RegisteredTool: name, description, inputSchema (string), title, annotations, origin, window` (§4.2.6). First arg to `executeTool` is **RegisteredTool object** not string name. Current `WebMCPEngine.ts:54` `executeTool(name:string, params)` mismatched.
  - **Events (spec §4.4):** `toolchange` EventTarget on `ModelContext`, fired via `notify documents of a tool change` algorithm (in parallel, queues task on `webmcp` task source, respects `allowed to use tools` + `exposedTo` origin matching via `toolIsExposedToOrigin`). Agents refresh via `document.modelContext.addEventListener("toolchange", async ()=>{await getTools()})`. Current code has 0 `toolchange` listeners (grep `toolchange` 0 in `src/` this run). See https://webmachinelearning.github.io/webmcp/#events
  - **SecureContext + Permissions-Policy (spec §4.5):** `tools` policy disables all methods → `NotAllowedError`; `is origin-keyed` check → `SecurityError`; not fully active → `InvalidStateError`; not https/localhost → `SecurityError`. See https://webmachinelearning.github.io/webmcp/#permissions-policy — current `src/main.tsx:66` unguarded register will reject 40 times in insecure contexts.
  - **Origin exposure (spec §4 + explainer):** Tools exposed to same-origin + built-in browser agent by default; cross-origin iframe requires `registerTool(tool, {exposedTo:["https://trusted.example"]})` + iframe `allow="tools"` + callee `getTools({fromOrigins:["https://trusted.example"]})`. See README `cross-origin iframe exposure: registerTool() and exposedTo` + spec `exposedTo` steps 15-16. Current code has 0 `exposedTo`/`fromOrigins` references (grep 0).
  - **Implementation status (webfetch 2026-08-30 at https://github.com/webmachinelearning/webmcp/blob/main/implementation-status.md):** Chrome **149 Origin Trial live** (blog https://developer.chrome.com/blog/ai-webmcp-origin-trial, early preview, intent to experiment, chromestatus 5117755740913664), Edge **150 Origin Trial live**, Brave experimental Leo AI (issue 55232), ChatGPT Desktop supported, Firefox position pending (mozilla/standards-positions#1412, Bugzilla 2018306), Safari pending (WebKit/standards-positions#670). Therefore hackathon reviewers testing on Chrome 149+ (https or localhost) will have native `document.modelContext` if Origin Trial token present or flag `chrome://flags#web-mcp` enabled; elsewhere polyfill required.

- **Current gaps why 40 tools will NOT be exposed in native browser (all cite file:line, verified this run):**
  1. **`register` never calls native:** `WebMCPEngine.ts:68-75` does only `registry.set + eventBus.emit`, no branch on `isNative` to `document.modelContext.registerTool`. Prior auditor at `.teamwork/workstreams/ws-02-result.md:48` noted same — opposite of intended. Native agent `getTools()` will return 0.
  2. **Wrong globals + polyfill shadowing:** `WebMCPEngine.ts:29-30` legacy `navigator.modelContext` + polyfill `:60` `globalThis.modelContext` + `:61` `__CareCanvas_WebMCP__` + `:63` overwriting `document.modelContext` without saving original → if flag present, polyfill permanently shadows native until reload; `ConnectWebMCPModal.tsx:67` lists 4 wrong globals and `:74,78` sync string API.
  3. **Shape mismatched:** `src/types/webmcp.ts:34-45` `parameters` vs spec `inputSchema`, `execute(params, context:WebMCPExecutionContext:47-60 with vault/eventBus/patientId)` vs spec `execute(inputObject, {signal})`. Even if delegation added, vault context missing → `patientId=''` orphan facts (`WebMCPEngine.ts:199` fallback).
  4. **Discovery/execution shape mismatched:** polyfill sync `getRegisteredTools():51` + `executeTool(name:string):54` vs spec Promise `getTools(): Promise<RegisteredTool[]>` + `executeTool(tool:RegisteredTool, inputObject): Promise<DOMString>`. Inspector `WebMCPInspector.tsx:46` bypasses native, masking 0.
  5. **Missing exposure hook:** 0 `exposedTo`/`fromOrigins`/`toolchange`/`SecureContext` checks → iframe agents see 0, built-in agent default misunderstood.
  6. **Timing & singleton:** constructor `detectAndPolyfill():22-25` at import `:433` before `main.tsx:66` register; no re-detection after hydrate, no HMR dedup via `AbortController` signal → second register would be `InvalidStateError: duplicate`.
  7. **Schema bridging:** `pillMapTools.ts:17-28` etc JSON Schema structurally compatible but field name wrong + `additionalProperties` inconsistent → native agent's auto-form empty.

- **Test infra discovered:**
  - `test/unit/WebMCPEngine.test.ts:13-35` 4 tests polyfill-only; `vitest` include 10 files (`vite.config.ts:34`), `test/test-runner.ts` exhaustive 231 (Tier1 40 tools 200 tests + Tier2 T2-01..12 + Tier3 INT-01..12 + Tier4 workloads + E2E Flows A-E). Prior PASS 172 vitest / 231 runner at `.teamwork/state.json:progress` with lint 0 `tsc --noEmit`. Build `vite build` 1660 modules prior at `.teamwork/verification/final.md` (1673 vs 1659 delta documented at auditor).

- **Constraints/integrityMode hint:** No isolation requested — default `state.json:metadata.artifactsDir=".teamwork"` working directory is `.teamwork` in current project (`/Users/sujal/Projects/proj1/.teamwork`). Prior integrityMode `development` at `state.json:request.integrityMode`, but for hackathon submission **demo (reproducible, verifiable, not mocked)** is recommended (see Integrity Mode section — requires user confirmation).

- **Working Directory:** `.teamwork` (or `~/teamwork_projects/{PROJECT_NAME}/.teamwork` if isolated) — confirmed default `.teamwork` in current project is intended since no isolation requested per state.json.

## Requirements (blocks — only what user cares about, ask don't assume)

> Capability-based: describe **what** must hold for hackathon judging, not **how** to code it. No file-edit prescription.

### R1 — Native-compliant exposure: 40 tools visible via `document.modelContext` in supporting browsers

**Requirement:** When a browser provides native `document.modelContext.registerTool` (e.g., Chrome 149 Origin Trial over SecureContext), all 40 CareCanvas tools (`src/tools/index.ts:69-123`) must be registered via the native **Promise** API and be discoverable via the **spec method** `await document.modelContext.getTools()` returning length 40, each with `name` in expected set, `description` non-empty, `inputSchema` JSON-parsable and matching supplied JSON Schema. In standard browsers without native (jsdom, Safari/Firefox pre-ship), same observable must hold via a **spec-shaped polyfill fallback** so judges without flag still see 40.

**Non-goals (don't assume):** No headless-only scenario, no backend MCP server duplication, no static manifest-only layer.

### R2 — Spec dictionary conformance: `inputSchema`, `title`, `annotations`, name/description validation

**Requirement:** Each tool's registration must use spec dictionary fields: `name` (1-128, `^[a-zA-Z0-9_.-]+$`), `description` non-empty (spec would otherwise reject `InvalidStateError`), `inputSchema` (JSON Schema object derived from current `parameters` at `src/types/webmcp.ts:41` strucutre — must be **stringified** correctly, no `TypeError` on circular), optional `title` and `annotations.readOnlyHint` (mapping from `requiresHumanApproval: true` at `src/types/webmcp.ts:39` → `readOnlyHint false + approval gate` semantics). Duplicate name must be handled without crash (HMR / re-register should not throw `InvalidStateError` unhandled).

**User choice needed:** Should duplicate registration overwrite or abort via `AbortController.signal` dedup? (see Open Questions Q3).

### R3 — Discovery & execution API parity: Promise shapes, object-based invocation, serialization

**Requirement:** Consumers (built-in browser agent, in-page JS at `ConnectWebMCPModal.tsx:74-81`, Inspector `WebMCPInspector.tsx:45-46`) must be able to:
- `const tools = await document.modelContext.getTools()` (Promise) — returns `RegisteredTool[]` with `name, description, inputSchema (string), title, annotations, origin, window`
- `const t = tools.find(x=>x.name==='extract_fact'); const rawResult = await document.modelContext.executeTool(t, {documentId, rawText})` (first arg is **object** not string, second is inputObject, optional third `{signal}`) → resolves to **DOMString** (stringified JSON) of `WebMCPToolResult` (must JSON-parse).
- Polyfill fallback must **exactly mimic** these Promise shapes (`registerTool → Promise<undefined>`, `getTools → Promise<RegisteredTool[]>`, `executeTool → Promise<DOMString>`) plus provide legacy sync `getRegisteredTools` only if backward compat required (user to decide Q2). Current polyfill sync `getRegisteredTools:51` + `executeTool(name:string):54` is **not acceptable** for `await document.modelContext.getTools()` judge probe.

### R4 — Origin, SecureContext, Permissions-Policy & `toolchange` correctness

**Requirement:** Registration must respect `SecureContext` (https:// or localhost) and `Permissions-Policy: tools` — when disabled or insecure, must **not crash** with unhandled `NotAllowedError`/`SecurityError` but instead gracefully fallback to polyfill or show empty with clear error, so submission is reviewable. By default tools must be exposed to same-origin callers and the **built-in browser agent** (spec default top-level exposes), without requiring `exposedTo`. Optional cross-origin exposure via `exposedTo`/`fromOrigins` + iframe `allow="tools"` must be supported if hackathon demo embeds agent iframe (user to confirm Q4). Whenever a tool is added/removed, `document.modelContext` must fire `toolchange` (`EventTarget`, `ontoolchange`) so agents listening at `spec §4.4` refresh via `getTools()`; Inspector `WebMCPInspector.tsx:76-83` must reflect native `toolchange` as well.

### R5 — Invariants preserved: no regression of product, vault, approval gates, UI

**Requirement:** After protocol fix:
- No regression of **build & types**: `npm run lint` (`tsc --noEmit` EXIT 0), `npm run build` (`vite build`) still produces 1660±delta modules with dist valid (prior 1660 at success auditor).
- No regression of **tests**: `npm test` (vitest 172+ PASS) + `npx tsx test/test-runner.ts` (231 PASS incl. Tier1 40 tools 200 tests, Tier2, Tier3 INT-01..12, Tier4 workloads, E2E Flows A-E) stay PASS — any new native-mock harness must not break existing `test/unit/WebMCPEngine.test.ts:13-35` polyfill assertions.
- No regression of **product flows**: Create Account gate / Sign-In still required, empty vault 0 facts before any upload, real `FileReader` drop (`src/components/vault/DocumentDropzone.tsx`) → `webMCPEngine.execute('extract_fact', {documentId, rawText})` still creates vault facts for **correct `patientId`** derived from `localStorage carecanvas_active_user` (`WebMCPEngine.ts:187-200` derivation, not hard-coded `patient-s-devi`), visible in FactStream; `extract_fact` with `rawText` length 0 still returns 0 facts not mock; approval gates `requiresHumanApproval: true` still stage `pendingApprovalId` + `humanApprovalRequired: true` via `WebMCPEngine.ts:265-292`.
- No regression of **UI responsive**: Inspector and Connect modal remain functional at 320/375/768/1024/1280/1440, Inspector label `WebMCPInspector.tsx:265-267` now accurately shows **Native WebMCP** vs **Polyfill Adapter** based on actual `document.modelContext` presence, not stale `engine.isNative`; Connect modal code examples at `ConnectWebMCPModal.tsx:68-81` now show **spec-correct** `document.modelContext` examples, not `window.modelContext`.
- Prior generic hard-code cleanup (`ST. JUDE`, `Dr. Patel`, `Raj Devi`, `Shanti` removal) remains intact — `grep` gates from prior milestones (`grep -R "ST. JUDE" src` 0, `grep -R "Dr. Anita Patel" src/components` 0 etc.) must stay 0.

## Independent Verification (per requirement — objective check)

> Each verification is **mechanical, reproducible**, runnable by reviewer without human judgment. Must be run in both **native-capable browser** (Chrome 149 Origin Trial / flag enabled over https:// or localhost:5173) and **fallback jsdom/standard browser** (no native).

### R1 — Verification: 40 tools via native probe + fallback parity

- **Native browser probe:** In Chrome 149 with Origin Trial (serve `npm run dev` over `http://localhost:5173` which is SecureContext), DevTools console: `isSecureContext === true` PASS; `typeof document.modelContext.registerTool === 'function'` true/false logged; `await document.modelContext.getTools()` → `Array.length === 40` PASS; each `tool.name` in expected set `['extract_fact','confirm_fact','compile_health_record','extract_labs','correlate_meds','add_medication','check_interactions',... total 40 at src/tools/index.ts:69-123]` PASS; `JSON.parse(tool.inputSchema)` succeeds for all 40 PASS; `tool.description.length>0` PASS; `tool.origin === location.origin` PASS.
- **Iframed probe (if applicable):** Create `document.createElement('iframe')` with/without `allow="tools"` and `src` same-origin vs `https://trusted.example` (if `exposedTo` opted). In child frame, `await child.contentDocument.modelContext.getTools()` and `await child.contentDocument.modelContext.getTools({fromOrigins:[location.origin]})` filtered counts as spec expects — logged under `.teamwork/verification/webmcp-native-probe.log`.
- **Fallback parity:** In jsdom (`npx tsx` or `vitest` environment where `isNative false`), same calls via polyfill: `await document.modelContext.getTools()` (polyfill Promise shim) length 40 PASS; `globalThis.__CareCanvas_WebMCP__` legacy count 40 if backward compat kept else 0 — decision logged per Q2.
- **Grep gates:** `grep -R "registerTool" src/core/webmcp/` count >=1 with `document.modelContext.registerTool` delegation PASS; `grep -R "exposedTo" src` count per policy (0 if default only, >=1 if cross-origin) logged.
- **Screenshots:** ≥2 per verification (desktop 1280 + mobile 375) under `.teamwork/snapshots/webmcp-native/` showing Inspector catalog count 40 and mode label "Native WebMCP" in native browser vs "Polyfill Adapter" in fallback.

### R2 — Verification: dictionary conformance + name/description validation

- **Schema check:** `for (const t of await document.modelContext.getTools()) { const s = JSON.parse(t.inputSchema); assert(s.type==='object' && s.properties)}` PASS for all 40 — proves `parameters` correctly mapped to `inputSchema`.
- **Name/description validation:** Try `await document.modelContext.registerTool({name:"", description:"x", inputSchema:{type:"object"}, execute:async()=>{}})` must reject with `InvalidStateError` (name empty) PASS; same for empty description PASS; duplicate `name:"extract_fact"` must reject with `InvalidStateError` not overwrite PASS; invalid name `"bad name!"` with space must reject PASS — all per spec §4.2 steps 8-11.
- **Annotations check:** Tools with `requiresHumanApproval:true` at `src/types/webmcp.ts:39` must have `annotations.readOnlyHint === false` and be discoverable with `readOnlyHint` present; those with `false` may have `true` hint — assert via `tool.annotations` JSON PASS.
- **Grep:** `grep -R "inputSchema" src/types/webmcp.ts src/core/webmcp/` count validates adapter layer exists; `grep -R "parameters" src/tools/` count still 40 but adapter converts — not bare native.

### R3 — Verification: discovery & execution API shapes

- **Shape probe:** `typeof document.modelContext.getTools === 'function'` and result is Promise PASS; `await document.modelContext.getTools()` is Array of objects with keys `name, description, inputSchema, title, annotations, origin, window` PASS; `typeof document.modelContext.executeTool === 'function'` PASS.
- **Object-based invocation probe:** `const tools=await document.modelContext.getTools(); const t=tools.find(x=>x.name==='extract_fact'); assert(typeof t==='object' && t.name==='extract_fact')` PASS; `const raw=await document.modelContext.executeTool(t, {documentId:"doc-test-001", rawText:"Aspirin 100mg daily"});` → `typeof raw==='string'` PASS; `const parsed=JSON.parse(raw); assert(parsed.success===true && parsed.tool==='extract_fact')` PASS; `await document.modelContext.executeTool("extract_fact", {...})` string-based must throw `TypeError` PASS — proves spec object-based.
- **Legacy parity probe:** `webMCPEngine.getRegisteredTools()` sync still 40 and `globalThis.modelContext` fallback still 40 if Q2 backward compat, else polyfill Promise shapes only — decision reflected in test expectations `test/unit/WebMCPEngine.test.ts:13-35`.
- **Invocation with correct patientId:** After setting `localStorage carecanvas_active_user = {userId:"test-patient-webmcp-001", name:"WebMCP Tester", role:"patient"}` and empty vault, `await document.modelContext.executeTool(t, {documentId:"doc-001", rawText:"Aspirin 100mg daily"})` must create vault fact with `patientId==="test-patient-webmcp-001"` not `''` not `patient-s-devi` — verify via `localVault.getFactsByPatient("test-patient-webmcp-001")` length 1+ and UI FactStream pending card visible at 1280/375/768 screenshots under `.teamwork/snapshots/webmcp-invoke-*.jpg`.
- **AbortSignal probe:** `const c=new AbortController(); const p=document.modelContext.executeTool(t, {documentId:"doc-abort-001", rawText:"x"}, {signal:c.signal}); c.abort(); await p.catch(e=>{assert(e.name==='AbortError' || e.reason)})` — proves signal plumbing.

### R4 — Verification: origin, SecureContext, Permissions-Policy, toolchange

- **SecureContext gate:** `isSecureContext` logged; `window.location.protocol` === `https:` or `localhost` PASS; attempt `document.modelContext.registerTool` in insecure `http://` (if tester uses file://) must reject `SecurityError` and fallback not crash — log under verification.
- **Permissions-Policy:** If `Permissions-Policy: tools=()` header present, `registerTool` must reject `NotAllowedError` and `getTools` same — tested via meta header or iframe `allow` absence; without header, calls succeed PASS.
- **toolchange event:** `let changeCount=0; document.modelContext.addEventListener("toolchange", ()=>changeCount++); await document.modelContext.registerTool({name:"webmcp_probe_temp", description:"probe", inputSchema:{type:"object"}, execute:async()=>({})}); assert(changeCount>=1)` after register and after `controller.abort()` unregister PASS; Inspector `WebMCPInspector.tsx:76-83` listeners must increment telemetry on `toolchange` as well — verified via UI log count.
- **Grep:** `grep -Rn "toolchange|ontoolchange" src` count >=1 after fix PASS; prior 0 was bug.

### R5 — Verification: no regression gates (must all PASS)

- **Build & types:** `npm run lint` (`tsc --noEmit`) EXIT 0 PASS — log `.teamwork/logs/lint.log`; `npm run build` (`vite build`) produced `dist/` with 1660±delta modules (check `dist/assets` JS 793kB prior) PASS — log `build.log`; `grep -R "mockShanti\|p_devi_78\|ST. JUDE" src` 0 PASS; `grep -R "Dr. Anita Patel\|Raj Devi" src` 0 in components (tools heuristic already addressed at `.teamwork/workstreams/ws-m2-auth-gate-result.md:80` pending, now resolved) PASS.
- **Tests:** `npm test` (vitest) 172+ PASS — log `vitest.log`; `npx tsx test/test-runner.ts` 231 PASS (Tier1 200 + Tier2 + Tier3 INT-01..12 + Tier4 + E2E Flows A-E) — log `tier3.log`; no new failure due to native harness — assert `WebMCPEngine.test.ts:13-35` still 40 PASS.
- **Flows:** After `npm run dev`, browser automation (or manual) verifies Flows A-E each 1 PASS at `.teamwork/verification/flows.log` (Flow A vault 8 tests, LabStory 17, PillMap 25, RxBridge 18, HomeLab+Safety+CareCircle 22, Dossier 10) — same as success auditor prior.
- **Screenshots:** ≥2 per milestone (desktop 1280 + mobile 375) under `.teamwork/snapshots/webmcp-*` showing Create Account gate still required, empty vault `No records here yet` with generic headers (no `ST. JUDE` at `BoundingBoxViewer.tsx:118`), after `executeTool` pending fact visible with correct `patientId` at 1280/375/768 — JFIF valid >5K each.

## Acceptance Criteria (testable DONE)

- [ ] **R1 DONE — Native 40 exposed:** In Chrome 149 Origin Trial (or flag) over `http://localhost:5173`, `await document.modelContext.getTools()` length 40, each `JSON.parse(inputSchema)` PASS, descriptions non-empty, and same length 40 via polyfill fallback in jsdom/standard browser without flag — verified via `.teamwork/verification/webmcp-native-probe.log` + screenshots 1280/375.
- [ ] **R2 DONE — Dictionary conformance:** All 40 tools use `inputSchema` (not bare `parameters`) with JSON Schema `type:"object"`, name regex `^[a-zA-Z0-9_.-]{1,128}$` PASS, empty name/description correctly rejects `InvalidStateError` and duplicate rejects — logs under `webmcp-validation.log`; HMR duplicate handling does not crash dev server.
- [ ] **R3 DONE — API parity:** `getTools()` returns `Promise<RegisteredTool[]>` with `origin, window`, `executeTool(toolObject, inputObject, {signal})` returns `Promise<DOMString>` stringified result — probe `const t=await document.modelContext.getTools().then(a=>a.find(x=>x.name==='extract_fact')); const raw=await document.modelContext.executeTool(t,{documentId:"doc-test-001", rawText:"Aspirin 100mg daily"}); JSON.parse(raw).success===true` PASS with correct `patientId` (not '' nor `patient-s-devi`) and orphan-free vault fact visible at 1280/375/768.
- [ ] **R4 DONE — Origin/permission/toolchange:** `isSecureContext===true` over localhost/https PASS, insecure gracefully fallback not crash; `toolchange` fires on register/unregister and `allow="tools"` iframe exposure respects `exposedTo`/`fromOrigins` — logged via `toolchange.log`; Permissions-Policy disabled case correctly rejects `NotAllowedError`.
- [ ] **R5 DONE — No regression:** `npm run lint` 0, `npm test` 172+ PASS, `npx tsx test/test-runner.ts` 231 PASS, `npm run build` 1660±delta modules `dist/` valid, Flows A-E each 1 PASS, 6-viewport screenshots (320/375/768/1024/1280/1440) with no gaps and Inspector label correctly "Native WebMCP" vs "Polyfill Adapter", Connect modal shows spec-correct `document.modelContext` examples.

## Working Directory

**`.teamwork` in current project (`/Users/sujal/Projects/proj1/.teamwork`)** — confirmed from `state.json:metadata.artifactsDir=".teamwork"` and `request.workingDirectory=".teamwork"` — no isolation requested. All artifacts must live under `.teamwork/` (research, prompt_draft.md, verification logs, snapshots) — never under `~/.config/opencode/teamwork` (that tree stays clean per dispatcher constraint). If user later requests isolation, alternative would be `~/teamwork_projects/{PROJECT_NAME}/.teamwork` — confirm default ".teamwork in current project" is intended (see Open Questions Q1).

## Integrity Mode

Ask which shortcuts are off-limits — mapped to:

- **development** (rapid, default per `state.json:request.integrityMode="development"` — mocks tolerated, quick polyfill overwrite allowed)
- **demo** (**recommended for hackathon submission** — reproducible, verifiable, not mocked — judges must be able to clone + `npm install && npm run build && npm test` + open `http://localhost:5173` + `await document.modelContext.getTools()` → 40 with real vault behavior, no hard-coded patient `Shanti` fixtures leaking — prior M3 already achieved `mockShanti 0` + `p_devi_78 0` + real FileReader path)
- **benchmark** (from-scratch, stdlib only, most strict — unnecessary for submission)

**Recommendation: `demo`** — reproducible, verifiable, not mocked. Please confirm or choose `development` vs `benchmark` in feedback (Q7).

## Execution Path

**`distributed-coding`** (per `state.json:metadata.pattern="distributed-coding"` and `request.executionPath="distributed-coding"` — pattern at `teamwork/patterns/distributed-coding/pattern.json`). Alternative paths are `iterative-coding`, `document-review`, etc., but hackathon protocol-correctness with preservation of 1660 build + 231 tests + 6-viewport screenshots maps to distributed-coding DAG: Sentinel → Orchestrator → Explorer/Workers (engine adapter + types + bootstrap + UI) → Critic → Challenger → Auditor → Success Auditor with per-workstream file ownership, worktrees, gates. Confirm `distributed-coding` or propose alternative (Q8).

## Open Questions for User

- **Q1 — Working directory confirmation:** Default `.teamwork` in current project `/Users/sujal/Projects/proj1/.teamwork` (per `state.json:metadata.artifactsDir`) vs isolated `~/teamwork_projects/{PROJECT_NAME}/.teamwork` for hackathon branch. No isolation was requested — confirm ".teamwork in current project" is intended before Orchestrator init (avoids stray docs).
- **Q2 — Polyfill legacy globals deprecation:** Should `window.__CareCanvas_WebMCP__` + `globalThis.modelContext` + `navigator.modelContext` be **kept for backward compat** (keep `grep -R "window.__CareCanvas_WebMCP__" src` count 1-2, tests at `test/unit/WebMCPEngine.test.ts:13-35` via engine direct unaffected) or **removed after fix** so only `document.modelContext` remains (grep 0 — spec-correct but breaks any existing in-page agents using `window.modelContext`)? Current `ConnectWebMCPModal.tsx:67` lists 4 globals — choose grep 0 vs kept.
- **Q3 — HMR duplicate handling:** On Vite HMR re-import, `registerAllWebMCPTools():66` will be called again. Spec `registerTool` duplicate → `InvalidStateError`. Should dedup via **AbortController signal per tool** (store controllers, `controller.abort()` on cleanup before re-register) — spec-recommended via `ModelContextRegisterToolOptions.signal` — or via try/catch swallow duplicate? Signal approach enables proper `toolchange` unnotify.
- **Q4 — `exposedTo` policy for hackathon demo:** Should all 40 tools be exposed by default to **built-in browser agent only** (spec default top-level) or also to **specific cross-origin origins** for iframe demo (e.g., `https://chat.openai.com`, `https://claude.ai` if judges embed agent)? If cross-origin, list `exposedTo: string[]` origins to include and whether demo page uses `<iframe allow="tools">`.
- **Q5 — Tool naming stay `snake_case` vs `kebab-case`:** All 40 currently `snake_case` (`extract_fact`, `add_medication`) — valid per `^[a-zA-Z0-9_.-]{1,128}$`. Spec examples use `filter-templates` kebab. Keep snake_case (no rename risk) or align to kebab for spec example familiarity? Keep vs rename — judges may copy-paste `tools.find(x=>x.name==='extract_fact')` must match.
- **Q6 — Approval gates with native spec:** `requiresHumanApproval: true` tools (`src/types/webmcp.ts:39` + `WebMCPEngine.ts:265-292` currently return `pendingApprovalId` + `humanApprovalRequired:true` stringified). Native spec has no built-in approval; Current maps to stringified pending result + later `confirmStagedInvocation:384-430`. For hackathon, should approval-gated tools **still return staged pending object** (preserves trust boundary per `isTrustedAutoApproved` check `:262-264`) or should they use spec issue #165 elicitation via `outputSchema` + agent prompt? Keep staged pending vs spec elicitation.
- **Q7 — Integrity Mode confirmation:** Confirm `demo` (reproducible, verifiable, not mocked — recommended) vs `development` (rapid) vs `benchmark` (stdlib only). Impacts whether polyfill may mock `inputSchema` vs must reuse real vault FileReader path.
- **Q8 — Execution Path confirmation:** Confirm `distributed-coding` (DAG with worktrees, Critic→Challenger→Auditor per milestone) vs `iterative-coding` (single worker loop) vs `document-review` — for WebMCP protocol correctness we recommend `distributed-coding` with explicit ownership: engine core `src/core/webmcp/`, types, catalog, bootstrap, UI, tests.
- **Q9 — Browser target for judges:** Will hackathon judges test on **Chrome 149 Origin Trial (https:// + token)** vs **Chrome with `chrome://flags#web-mcp` enabled** vs **Brave Leo / ChatGPT Desktop** (different discovery mechanism)? Determines whether deployment needs `Origin-Trial` HTTP header + token and `isSecureContext` guard.
- **Q10 — Timing confirmation:** Should `registerAllWebMCPTools()` remain at `src/main.tsx:66` after `localVault.init()` + before `ReactDOM.createRoot` (current) or move to after React mount / wait for `window.isSecureContext` + `document.modelContext` readiness with async await per tool Promise? Current is fine if made async with `await Promise.all(tools.map(t=>document.modelContext.registerTool(...)))` before `toolchange` resolves.

> **Blocking:** Reply with `RequestFeedback` (or comment on this artifact) to approve or request changes. Orchestrator will NOT start until you resolve feedback.

> Status: AWAITING_FEEDBACK
