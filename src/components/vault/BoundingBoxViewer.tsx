/**
 * CareCanvas Vault BoundingBoxViewer — CLEAN (M1 Mock Removal)
 * Re-exports common viewer with no hardcoded documentId.
 * Dynamic documentId is supplied via props or EventBus highlight; empty state when none.
 */
export { BoundingBoxViewer } from '@/components/common/BoundingBoxViewer';
